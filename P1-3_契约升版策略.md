# P1-3 · 契约升版策略

> 目的：让契约**能演进，但不会悄悄改坏消费方**。
> 适用对象：FP / TP / Case 三层产物字段、枚举取值、追溯键、八要素。

---

## 一、一句话原则

> **只增不破**：新版本可以**加**字段，绝不能**改**已有字段的含义、**删**已有字段、**改**已有取值的语义。

只要有消费方（旧用例库、旧脚本、技能、其他服务）还在读，就不能破。

---

## 二、版本号规则

| 变更类型 | 版本动作 | 示例 |
|---|---|---|
| 新增**可选**字段（有默认值） | `v1.0 → v1.1`（次版本） | 加 `evidence`、`confidence` |
| 新增**枚举取值**（不删旧的） | `v1.0 → v1.1` | `verify_layer` 增加 `混合` |
| **修正**已有取值词表（等价映射） | `v1.0 → v1.1` + **别名兼容** | `新增` 统一为 `更新`，仍接受读入 `新增` |
| 新增**必填**字段 | ⚠️ 需同时提供默认值，否则缓做 | — |
| 改字段**含义** / 删字段 / 改取值语义 | ❌ **禁止**（须开 `v2.0` 且写迁移方案） | — |

**版本号落点**：`core/contracts.py` 的 `CONTRACT_VERSION` 常量 + 每份产物 JSON 的 `contract_version` 字段。

---

## 三、已规划的 v1.1 增量（**只加不改**）

| 字段 | 类型 | 归属 | 含义 | 缺省行为 |
|---|---|---|---|---|
| `evidence` | list[str] | TP / Case | 证据引用，形如 `文件:行号`，标注该条生成的依据 | 空列表 |
| `confidence` | float | TP / Case | 置信度 0–1；规则引擎恒 `1.0`，LLM 补全项按校验结果打分 | `1.0` |
| `origin` | str | TP | 产出通道：`rule` / `llm` / `llm_verified` | `rule` |
| `unverified` | bool | TP | 是否为「疑似隐性规则，待人工确认」条目 | `false` |

> 这四项正是 P3「防胡说校验层」的产物载体——**只新增，不改动既有字段**。

---

## 四、兼容性保障机制（三道防线）

### 防线 1 · 契约测试（新服务自带）
- 位置：`tests/test_contract.py`
- 内容：对固定输入跑完整链路，断言产物**字段名集合 ⊇ v1.0 约定集合**、取值域合法、追溯孤儿为 0。
- 触发：每次提交由 `gate_all` 的 pytest 环执行。

### 防线 2 · 黄金样本比对（跨实现）
- 用 `backup/golden/20260911/` 作为标尺，新旧实现同输入跑，逐字段 diff。
- 差异分类：**允许**（新增字段、编号算法修正） / **禁止**（字段缺失、取值越界）。

### 防线 3 · 门禁哈希校验（防双源分叉）
- 新服务与 legacy 的门禁配置（`pyproject.toml` + `scripts/*.py`）若两处存在，哈希不一致即门禁失败。

---

## 五、消费方约定

**读取方（宽容）**
- 遇未知字段：忽略，不报错。
- 遇缺失的可选字段：用默认值。
- 遇未知枚举取值：降级为 `unknown` 并记录告警，**不崩**。

**产出方（严格）**
- 必须写全 v1.0 的必填字段，缺一即契约测试失败。
- 不得输出未登记的字段（避免"野生字段"扩散）。

---

## 六、废弃流程（三步走，不可跳步）

1. **标记**：字段/取值标记 `@deprecated`，文档加迁移说明，**代码仍正常产出**。
2. **双写**：新字段上线，旧字段继续写，至少保留 **1 个迭代周期**。
3. **移除**：确认无消费方读取后移除，并**升主版本** `v2.0`。

> 本次范围内**没有**需要废弃的契约字段。D-1（标签词表统一）走的是"**修正 + 别名兼容**"，属次版本变更，**不删 `新增`**。

---

## 七、版本记录表

| 版本 | 日期 | 变更 | 兼容性 |
|---|---|---|---|
| **v1.0** | 2026-09-11 | 冻结：FP/TP/Case 三层字段、八要素、追溯键、枚举真值表 | 基线 |
| v1.1（规划） | P3 落地时 | 新增 `evidence` / `confidence` / `origin` / `unverified`；标签词表统一为 `全量/更新`（兼容读 `新增`） | ✅ 向后兼容 |
| v2.0 | 未规划 | 如出现字段语义重构 | ❌ 需迁移 |

---

*本文件为 P1 交付物之一。P2 建立骨架时，本策略的第 4 节「三道防线」将直接落为代码。*
