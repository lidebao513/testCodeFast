# P2–P4 新服务交付说明（testgen-service）

> 交付日期：2026-09-11
> 范围：**P0 封存 → P1 契约冻结 → P2 骨架 + 规范同步 → P3 引擎重做 → P4 双通道 + 只读加固**
> 终点：**能真实产出测试用例**（P5 影子双跑 / P6 第三方引擎不做，接口已预留）
> 位置：`work/testgen-service/`（当前仓库内新目录，门禁与规范只有一份，不会两边分叉）

---

## 一、一句话结论

老项目原样封存不动，新服务已独立建成并**跑通"代码 → 功能点 → 测试点 → 测试用例"全链路**，全套质量门禁（八环）**全绿**，真实仓库实测产出了 **1151 条用例**（其中 183 条标"更新"）。并且——**这次验证抓出并修掉了 5 个真缺陷**（见第六节），这是本次最有价值的产出。

---

## 二、P2 / P3 / P4 各做了什么

### P2 盖骨架（含"代码规范同步"）

| 交付项 | 说明 |
|---|---|
| 六层目录 | `core`（契约/配置/日志/错误/库/仓储）→ `engine`（引擎）→ `service`（服务壳）→ `cli`（命令行）+ `workspace`（只读工作区）+ `output`（产物写出） |
| 服务壳 | FastAPI；`/health` 存活、`/ready` 就绪（含配置自检，密钥只输出"是否已配置"）；`/api/v1/*` 业务接口；统一错误结构、请求 ID 贯穿、CORS 显式来源、优雅停机 |
| 配置 | **全部来自环境变量**，启动时集中校验、快速失败；只提交 `.env.example`（占位值），真实 `.env` 进 `.gitignore` |
| 质量门禁（八环） | ① 密钥扫描 ② ruff 检查 ③ ruff 格式 ④ 枚举单源 ⑤ 分层结构 ⑥ mypy 类型 ⑦ bandit 安全 ⑧ pytest + 覆盖率 —— **与老项目同源同配置** |
| 规范文本 | `ARCHITECTURE.md`（分层铁律）、`规范文档.md`、`README.md`、`PIPELINE.md` 同源更新 |

> 门禁脚本从老项目整体搬入并按新分层改写（`check_enums.py` 改查 `core.enums`、`check_structure.py` 改查新分层、`install_hooks.sh` 能按项目分别装钩子）。**唯一真值源**：枚举只有 `core/enums.py` 一份。

### P3 装引擎（代码 → 用例）

引擎拆成 6 个小模块，每个都能单测：

| 模块 | 职责 |
|---|---|
| `engine/scan.py` | 扫描源码，**同一文件只读一次、AST 只解析一次** |
| `engine/fp_extract.py` | 功能点提取：FastAPI/Flask 路由（AST 解析装饰器，不做正则猜测）+ 公开业务函数 + 前端路由 |
| `engine/tp_expand.py` | 测试点展开：正常 / 异常 / 安全 / 边界 四维度，规则表可见 |
| `engine/diff_tag.py` | 差异打标：`git diff base..target` → 变更文件 + hunk 行区间 → 命中标"更新" |
| `engine/semantic_enrich.py` | 语义增强：**规则为主**（默认不依赖大模型），大模型可选；带**防胡说层** |
| `engine/case_gen.py` | 用例生成：八要素落地、机器步只在 `steps[0]`、确定性（同输入同结果） |
| `engine/pipeline.py` | 编排：扫描 → 功能点 → 打标 → 测试点 → 增强 → 用例 → 落库（每阶段一个函数） |

**契约 v1.0**（`core/contracts.py`）：功能点/测试点/用例三层字段、`FP-`/`TP-` 稳定编号（内容指纹，非自增序号）、八要素校验、双向追溯。契约冻结依据见 `P1-1_契约冻结说明v1.0.md`。

**防胡说层**（`semantic_enrich.validate_candidates`）：大模型给出的接口必须**在代码里真实存在**，否则整条丢弃；无法从代码证实的规则一律标 `unverified`，进人工确认队列。

### P4 双通道 + 只读加固

| 能力 | 说明 |
|---|---|
| 全量通道 | 不做 diff，全部标"全量" |
| 增量通道 | `base..target` 真实 diff → 变更文件标"更新"，未变更标"全量"；**hunk 级打标函数已实现并单测** |
| 安全网 | ① 非 git 仓库/ref 不可解析 → **显式降级为全量并记录原因**（不静默）② 目标目录 `.git` 逃逸到祖先仓库 → **直接阻断**（防误伤本项目、防拿到错误 diff） |
| 只读工作区 | `chmod` 置只读后**真实写入探测**，把"是否真的只读"变成可验证事实（不靠声明） |
| 越界防护 | 工作区名 / 解析路径含 `..`、分隔符、空值 → 一律拒绝 |

---

## 三、交付物清单

```
work/testgen-service/
├── core/          契约 / 枚举 / 配置 / 日志 / 错误 / 库 / 仓储      (8 文件)
├── engine/        扫描 / 功能点 / 打标 / 测试点 / 增强 / 用例 / 编排 (8 文件)
├── service/       FastAPI 服务壳                                  (2)
├── cli/           命令行入口（pipeline / analyze / serve）          (2)
├── workspace/     工作区管理 + 只读加固                             (3)
├── output/        产物写出（JSON / Markdown / summary）            (2)
├── scripts/       八环门禁 + 钩子安装 + 黄金样本导出                  (5)
├── tools/         黄金样本导出                                     (1)
├── tests/         73 个测试                                        (9)
├── pyproject.toml 门禁配置（ruff/mypy/bandit/coverage/pytest）
├── requirements.txt / .env.example / .gitignore
└── ARCHITECTURE.md / 规范文档.md / README.md
```

规模：**40 个 Python 文件 / 6133 行**（含测试与门禁脚本）。

---

## 四、验收证据（都是实测，不是声明）

### 1）质量门禁 · 八环全绿

```
✅ 全部质量门禁通过（secret/lint/format/enum/structure/type/security/test）
```

| 环 | 结果 |
|---|---|
| 密钥扫描 | OK |
| ruff lint | OK |
| ruff format --check | OK（37+ 文件已格式化） |
| 枚举单源 + 孤儿反查 | OK（零硬性违规） |
| 分层结构（反向依赖） | OK |
| mypy 类型检查 | OK（31 个源文件） |
| bandit 安全（MEDIUM+） | OK |
| pytest + 覆盖率 | OK |

### 2）测试与覆盖率

| 测试文件 | 用例数 | 覆盖重点 |
|---|---|---|
| `test_api.py` | 9 | 服务端点、错误结构、健康检查 |
| `test_cli.py` | 2 | **CLI 契约：stdout 纯 JSON / 日志走 stderr** |
| `test_contract.py` | 11 | 契约冻结、D-1/D-2 缺陷回归、八要素 |
| `test_engine.py` | 21 | 扫描 / 功能点 / 打标 / 测试点 / 用例 |
| `test_log.py` | 3 | 日志保留字段、敏感脱敏、单行 JSON |
| `test_pipeline.py` | 13 | 端到端 / 幂等 / 增量 / 作废 / 防幻觉 |
| `test_store.py` | 5 | 对账幂等、唯一性防线、孤儿体检 |
| `test_workspace.py` | 9 | 工作区隔离、只读加固、越界拦截 |
| **合计** | **73** | **覆盖率 86.16%**（门槛当前 5%，建议分阶段抬到 30% → 50%） |

### 3）真实仓库端到端（research-agent，非玩具样本）

增量对比：`cacbd6b` → `722e198`（"kline开发"）

| 指标 | 结果 |
|---|---|
| 扫描文件 | 269 |
| 功能点 | 1034（提取数 = 落库数，**零丢失**） |
| 测试点 | 1151（范围：正常 + 异常） |
| 用例 | 1151 |
| 标签分布 | **全量 968 / 更新 183** |
| 变更文件 / hunk 覆盖 | 25 个 / 23 个文件 |
| 追溯体检 | 孤儿测试点 **0**，孤儿用例 **0** |
| 用词分布 | 正常 1034 / 异常 117 |

**幂等复核**：同参数再跑一次 → `created 0 / updated 0 / reused 1151 / obsolete 0`（不重复插、不错位）。
**全量通道复核**：`tag_summary = {全量: 1151}`，孤儿 0。

### 4）服务冒烟（HTTP）

| 请求 | 结果 |
|---|---|
| `GET /health` | 200，`{"status":"ok","service":"testgen-service","contract_version":"1.0"}` |
| `GET /ready` | 200，配置自检输出（**密钥只给 `llm_key_configured: false`，不泄露值**） |
| `POST /api/v1/pipeline` | 200，返回 counts / tag_summary / traceability / case_stats / 产物路径 |
| `POST /api/v1/pipeline`（非法维度） | **422**，`{"code":"validation_error","message":"非法行为维度：['不存在']，允许 [...]"}` —— 结构化、**无堆栈泄露** |
| `GET /nope` | 404 |

### 5）只读加固（如实报告，不粉饰）

在一份副本上实测：

| 字段 | 值 | 含义 |
|---|---|---|
| `entries_chmod` | 3 | 权限位已改 |
| `mode_bit_readonly` | True | 权限位确实变成只读 |
| `writable_probe` | **True** | **实测仍然写得进去** |
| `enforced` | **False** | **所以结论是：没真正生效** |

结论要写清楚：**Windows 下 `chmod` 不是硬约束**（只影响文件只读属性，目录不生效）。生产上必须靠**容器 `:ro` 挂载 + 非 root 运行 + 输出目录物理分离**——这条已写进 `ARCHITECTURE.md`。这个模块的价值就在于**不做"声明式安全"**，而是把真实结果报出来。

### 6）越界防护

| 输入 | 结果 |
|---|---|
| `../evil` | 已拦截（工作区名非法） |
| `a/b` | 已拦截 |
| `..` | 已拦截 |
| `''` | 已拦截（不能为空） |
| `good/../../etc` | 已拦截 |

---

## 五、跟老项目的关系

| 维度 | 老项目 `work/test-accel` | 新服务 `work/testgen-service` |
|---|---|---|
| 状态 | **原样封存**（标签 `legacy-pipeline-v1` + 分支 `legacy-phase2-3-baseline` + 数据库副本 + 黄金样本） | 新建，独立 |
| 质量门禁 | 一套 | **同一套**（同源脚本 + 同配置） |
| 数据库 | `data/test_accel.db`（SQLite） | `data/testgen.db`（SQLite，全新，不读老库） |
| 依赖 | 含 playwright | **不含**（执行层不在本次范围） |
| 枚举真值源 | `backend/core/enums.py` | `core/enums.py`（取值域**逐值一致**，已冻结） |

**随时可回滚**：老项目全程只读未动；新目录删掉即"什么都没发生"。

---

## 六、⚠️ 本次验证抓出并修复的 5 个真缺陷

这部分是本次交付**最有价值**的产出。它们全都是"看起来能跑、实际结果错了"的静默缺陷——正是"跑一遍验证"才暴露出来的。

| 编号 | 缺陷 | 后果 | 修复 |
|---|---|---|---|
| **D-3** | 结构化日志被写到 **stdout** | CLI 的 JSON 输出被日志行污染，下游按行 `json.loads` 直接崩（`Extra data`） | 日志改走 stderr；新增 `test_cli.py` 把"stdout 纯 JSON"钉死 |
| **D-4** | `refs_available` 用 `git rev-parse --verify --quiet A B` 校验两个 ref | 该命令**一次只接受一个** revision → 永远判定"ref 不可解析" → **增量通道 100% 静默降级为全量**，`更新` 标签恒为 0 | 改为**逐个**校验；测试改用**真 git 仓库**（原测试断言 `"增量通道" in note` 被降级文案误命中，反而掩盖了缺陷） |
| **D-5** | 业务功能点机器键只取**裸函数名** | 同文件内不同类的同名方法（`sort_key`/`put`/`flush`…）算出**相同 `fp_id`** → 落库被去重**静默合并**（真实仓库上 1034 个功能点被吞掉 11 个） | 机器键改为**类名限定**（`Cls.method`）；模块级函数保持裸名；新增碰撞回归测试 |
| **D-6** | `log_extra()` 未防 logging **保留属性** | 传 `name=` 等保留键时抛 `KeyError: Attempt to overwrite 'name' in LogRecord` | 保留键自动加 `field_` 前缀；4 处直写 `extra={...}` 的调用收敛到 `log_extra()`（顺带修好"字段既不脱敏也不被渲染"） |
| **D-7** | `cases` 表缺 `(project_id, tp_id)` **唯一约束** | 编号一旦碰撞就会插出**重复用例行**；对账以 `tp_id` 为键，重复行永远匹配不上 → **永久残留成"孤儿用例"**（实测 11 条） | 加唯一索引（`cases`/`test_points`/`functional_points` 三处）；`reconcile_cases` 遇到重复编号**直接报错**而非静默插入；新增 `test_store.py` 哨兵测试 |

> D-5 / D-7 是**同一条因果链**：编号碰撞 → 重复行 → 缺唯一约束 → 永久孤儿。链条上每一环现在都有防线。

---

## 七、已知限制（没做 / 做不到，如实列出）

| 项 | 状态 | 说明 |
|---|---|---|
| **测试执行（跑浏览器/发请求）** | ❌ 未做（属 P5） | 本次终点是"出用例"，执行器不在范围；用例里已带机器可执行步（`steps[0]`）供将来直接消费 |
| **符号级 hunk 打标未接线** | ⚠️ 函数已实现 + 已单测，但编排里走的是**文件级**打标 | 原因：符号级要求工作树**检出到 target** 才能保证行号对齐；当前工作树可能不在 target 上，硬接会给出**错的**行号结论。接法已明确：先 `checkout target` 再启用 `tag_of_symbol` |
| **认证鉴权** | ⚠️ 占位 | `require_auth` 是占位实现（`AUTH_TOKEN` 未设即放行）；JWT + RBAC 属服务化阶段 |
| **Windows 只读加固** | ⚠️ 不生效 | 见第四节第 5 点；生产走容器 `:ro` |
| **覆盖率门槛** | ⚠️ 仍为 5% | 实际 86.16%，门槛**分阶段抬**（建议 5 → 30 → 50），避免卡死日常提交 |
| **异步化 / webhook / 监控** | ❌ 未做（属 P6 服务化） | 现有端点均同步执行 |
| **新服务尚未提交 git** | ⏳ 本次提交 | 见下节提交信息 |

---

## 八、怎么跑

```bash
cd work/testgen-service

# 1) 装依赖（首次）
python -m venv venv
venv/Scripts/python.exe -m pip install --no-cache-dir -r requirements.txt

# 2) 一键质量门禁（八环）
venv/Scripts/python.exe scripts/gate_all.py

# 3) 代码 → 用例（全量）
venv/Scripts/python.exe -m cli.main pipeline --path <被测目录> --mode full --name 项目名 --scopes 正常,异常

# 4) 代码 → 用例（增量：只把变更文件标"更新"）
venv/Scripts/python.exe -m cli.main pipeline --path <被测目录> \
  --mode incremental --base <基线commit> --target <目标commit> --name 项目名 --scopes 正常,异常

# 5) 起服务
venv/Scripts/python.exe -m cli.main serve --host 127.0.0.1 --port 8100
```

产物落在 `outputs/<project_id>/`：`test_points.json`、`test_cases.json`、`TEST_CASES.md`、`summary.json`。

> 本地探活请用 `curl --noproxy '*'`——本机设了全局代理，不加会 502。

---

## 九、下一步建议（按优先级）

1. **提交入库**（本次）：把 `work/testgen-service/` 纳入版本控制，提交信息走 `feat(testgen): ...` 规范。
2. **接符号级打标**：在 pipeline 里加"检出到 target"步骤，然后把 `tag_of_symbol` 接上（函数已就绪），让"更新"精确到函数而不是整文件。
3. **抬覆盖率门槛**：5% → 30%，CI 里逐步收紧。
4. **P5 影子双跑**：老项目与新服务**同输入跑一遍，逐条比对**测试点/用例，差异清零后再谈切换。
5. **P6 服务化**：端点异步化 + webhook（发布流水线触发）+ 鉴权 + 监控。

---

## 十、相关文档

| 文档 | 内容 |
|---|---|
| `P0-P3执行确认书.md` | 开工前的范围/边界/规范同步清单 |
| `P1-1_契约冻结说明v1.0.md` | 契约字段表、D-1/D-2 缺陷、枚举真值表 |
| `P1-2_资产盘点表.md` | 老项目每个文件的处置（直接引用/移植/重写/废弃） |
| `P1-3_契约升版策略.md` | 只增不破的升版规则 |
| `备份说明.md` | P0 封存细节（标签、分支、库副本、黄金样本校验值） |
| `work/testgen-service/ARCHITECTURE.md` | 分层铁律 + 只读加固生产建议 |
| `work/testgen-service/规范文档.md` | 六类规范 + 通俗说明 |
