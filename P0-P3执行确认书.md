# P0–P3 执行确认书（开工前确认）

> 用途：把你这句话——「**继续要备份，并且把后续的过程全部执行，但到"用例生成"这一步为止，后面的先不做，代码规范也要一起同步**」——翻译成可核对、可验收的执行清单。
> 结论：**可执行 ✅**，但开工前有 **3 件事需要你拍板**（见第四节）。你确认后我立即开始。

---

## 一、先复述一遍你的意思（确认我没理解歪）

| 你说的 | 我理解成 |
|---|---|
| "继续要备份" | 把**现在这个老项目**完整封存（代码打标签冻结、数据库存副本、样本存副本），保证以后随时能回到今天这个状态 |
| "把后续的过程全部执行" | 按之前那份整合方案，从 P0 一路往下做 |
| "到根据方案到测试用例生成的步骤" | **做到"代码 → 测试点 → 测试用例"这一步就停**；对应方案里的 P0 / P1 / P2 / P3 |
| "后续的步骤先不需要生成" | **测试执行、报告、影子双跑、第三方引擎（P4/P5/P6）先不做** |
| "代码规范等要求也要一起同步" | 新项目**必须自带**老项目那套质量规矩（八环门禁、分层铁律、枚举单源、提交规范、密钥扫描），不能因为是新项目就"裸奔" |

**一句话**：老房子原样封存 → 盖新房（骨架 + 规范先立）→ 新房只盖到"能出测试用例"这一层 → 上层的执行/报告先不盖。

---

## 二、我已经核实过的现状（事实，不是我猜的）

| 项目 | 实际情况 | 影响 |
|---|---|---|
| 基线快照提交 | ✅ **已完成**：`16e4bb8 chore(release): 冻结阶段1-2基线并纳入质量门禁与密钥扫描` | P0 的"代码封存"已完成一大半 |
| 工作区 | 干净（0 待提交、0 未暂存），仅 1 个新文件未纳入版控：`work/test-accel/scripts/install_hooks.sh` | 需补一次小提交 |
| 标签 / 冻结分支 | ❌ **还没有**：`git tag` 为空，只有 `main` 分支 | P0 剩余待做 |
| 数据库 | `work/test-accel/data/test_accel.db`，2.5 MB，**未纳入版控**（`.db` 在 .gitignore 里） | 备份必须走"文件副本"，不能靠 git |
| 数据量 | 项目 5 / 功能点 237 / 测试点 415 / 用例 540 / 执行记录 2010 / 报告 38 | 这些就是"黄金样本"的来源 |
| 质量门禁 | ✅ 已成型，共 **8 环**：密钥扫描 → ruff 检查 → ruff 格式 → 枚举一致性 → 目录结构 → mypy 类型 → bandit 安全 → pytest+覆盖率 | 可整体搬到新项目 |
| 门禁脚本 | `scripts/` 下齐全：`gate_all.py`、`check_secrets.py`、`check_enums.py`、`check_structure.py`、`check_commit_msg.py`、`install_hooks.sh`、`pull_code.py` | 同上 |
| 规范文档 | ✅ `规范文档.md`（6 类规范 + 通俗说明 + 不足分析）、`ARCHITECTURE.md`（分层铁律）、`PIPELINE.md`、`pyproject.toml` | 直接作为新项目的规范底座 |
| 覆盖率门槛 | 目前 `fail_under = 5`（很低，属于"先把环跑通"阶段） | 新项目建议**分阶段抬高**，不一次卡死 |
| 备份目录 | `backup/` 尚不存在（`.gitignore` 已预留规则 `backup/*.db`） | 本次创建 |

---

## 三、执行范围（做哪些 / 不做哪些）

### ✅ 本次要做（P0 → P3）

| 阶段 | 大白话 | 具体动作 | 产出 | 风险 |
|---|---|---|---|---|
| **P0 封存**（收尾） | 给老房子拍照存档、配把备用钥匙 | ① 补提交 `install_hooks.sh` ② 打标签 `legacy-pipeline-v1`（不可变）③ 建冻结分支 `legacy/phase2-3-baseline` ④ 数据库存副本到 `backup/` ⑤ 导出黄金样本（TP/Case 的 JSON）⑥ 备份 `~/.workbuddy/skills/qa-*` 技能 | 标签+分支、DB 副本、黄金样本目录、《备份说明》 | **零风险**（只读、只加不改） |
| **P1 立契约** | 把"房产证"条款写清楚，免得搬家时丢东西 | ① 冻结数据契约 v1.0（测试点/用例字段、8 要素、追溯键）② 出《资产盘点表》：老项目每个文件标"直接搬 / 移植 / 重写 / 废弃" ③ 定契约升版规则（只增不破） | 3 份文档 | **零风险**（纯文档） |
| **P2 盖骨架** | 先把新房的框架和规矩立起来 | ① 建新项目目录，落 6 层结构（契约 / 引擎 / 服务壳 / 命令行 / 只读工作区 / 输出）② **同步整套质量门禁** ③ 服务壳：`/health` + 版本化接口占位 ④ 只读工作区管理（落盘即置只读） | 能启动的空壳服务 + 门禁全绿 | 低（新目录，删掉即恢复） |
| **P3 装引擎** | 把老房子的核心家具按新图纸重做，做到"能出用例" | ① 代码解析拆成 5 个小模块（扫描 / 功能点 / 测试点 / 语义增强 / 差异打标）② 双引擎：规则先出骨架 + 大模型补语义 ③ **防胡说层**：接口必须真实存在、字段以代码为准、隐性规则只能标"待人工确认" ④ 测试点生成（范围标签 + 全量/更新标签不变）⑤ **用例生成**（8 要素、稳定编号、幂等增量）⑥ 双向追溯，孤儿率归零 | 三个模块新实现 + 单测/集成测试 + 可直接产出用例 | 中（工作量最大；**这是本次终点**） |

### ⛔ 本次不做（已明确暂缓）

| 阶段 | 内容 | 为什么先不做 |
|---|---|---|
| P4 | 全量/增量双通道 + 只读加固 | 属于"输入通道"，你没要求 |
| P5 | 影子双跑 + 切换上线 | 属于"验收替换"环节 |
| P6 | 第三方引擎（dsh 等）PoC | 属于可选增强 |

> 说明：P4/P5/P6 的开关和接口会在 P2/P3 里**预留位置**，将来要接随时能接，不用返工。

---

## 四、需要你拍板的 3 件事

### ① 范围边界
按你原话，我默认**做到"用例生成"为止（P0–P3）**。备选：把 P4 的"全量/增量双通道"也一起做掉（它是用例生成的上游配套）。

### ② 新项目放哪里
- **方案 A（推荐）**：放在当前仓库的新目录 `work/testgen-service/`。优点：**质量门禁只有一份**，不会两边不一致；一处提交、一处管理。
- **方案 B**：新建一个**完全独立**的 git 仓库。优点：彻底隔离；代价：门禁和规范要**各维护一套**，容易分叉。

### ③ 用哪个大模型
新服务里"读懂代码、补测试点、写用例步骤"要用大模型。
- **方案 A（推荐）**：沿用现有配置（通义千问），本地 `.env` 已有 key，**零配置成本**；同时预留 DeepSeek 开关。
- **方案 B**：直接切 DeepSeek（需要你提供 key）。

---

## 五、"代码规范同步"到底同步什么（明细清单）

新项目会**原样继承**下面这一整套，缺一不可：

| # | 规范项 | 来源文件 | 新项目怎么落地 |
|---|---|---|---|
| 1 | **八环质量门禁** | `scripts/gate_all.py` | 同一套脚本搬入，一条命令全跑 |
| 2 | 密钥/明文扫描 | `scripts/check_secrets.py` | 提交前自动拦 |
| 3 | 代码风格（lint + 格式） | `pyproject.toml` [tool.ruff] | 同配置 |
| 4 | 类型检查 | `pyproject.toml` [tool.mypy] | 同配置（含严格度） |
| 5 | 安全扫描 | `pyproject.toml` [tool.bandit] | 同配置 |
| 6 | 单元测试 + 覆盖率 | `pyproject.toml` [tool.coverage] + `tests/` | 同配置，覆盖率门槛**分阶段抬高** |
| 7 | **枚举单源 + 孤儿反查** | `backend/core/enums.py` + `scripts/check_enums.py` | 枚举仍为唯一定义处，门禁反查孤儿 |
| 8 | **分层铁律（机器强制）** | `ARCHITECTURE.md` + `scripts/check_structure.py` | 分层违规直接门禁失败 |
| 9 | 提交信息规范 | `scripts/check_commit_msg.py` | 提交时强制校验 |
| 10 | 提交前钩子 | `scripts/install_hooks.sh` | 新项目一键安装 |
| 11 | 密钥不入库 / `.env.example` | `.gitignore` | 同规则 |
| 12 | 规范文本 | `规范文档.md`、`PIPELINE.md` | 同源更新，不各写一份 |
| 13 | 依赖清单 | `requirements.txt` | 显式声明，不隐式依赖 |
| 14 | 防"双源分叉" | 新增校验 | 门禁配置若两处存在，自动比对，不一致即失败 |

---

## 六、执行顺序与验收标准

1. **P0**（先做，零风险）→ 验收：`git checkout legacy/phase2-3-baseline` 能回到旧状态；黄金样本能稳定复现。
2. **P1**（纯文档）→ 验收：契约无歧义；盘点表覆盖老项目全部文件。
3. **P2** → 验收：门禁八环全绿；`/health` 可用；只读工作区**实测写不进去**。
4. **P3** → 验收：同一份输入，新实现产出的测试点/用例能通过契约校验；门禁全绿；**能真实产出用例**（本次终点）。

**随时可回滚**：老项目全程只读不动；新项目删掉目录即"什么都没发生"。

---

## 七、等你确认后，我第一步会做什么

1. 补提交 `install_hooks.sh`
2. 打 `legacy-pipeline-v1` 标签 + 建冻结分支
3. 快照数据库到 `backup/`，导出黄金样本
4. 写《备份说明》
5. 然后依次进入 P1 → P2 → P3

> 本文档只做"确认"，**尚未开始执行**。
