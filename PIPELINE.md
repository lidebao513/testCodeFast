# 测试加速平台 · 全链路阶段划分（代码获取 → 报告生成）

> 用途：作为后续「分批次全面优化」的对照基线。每个阶段列出【目的 / 具体过程 / 是否需 skill】。
> 注：本文件为优化参考索引，不进入运行时；改动请按阶段分批进行并在 commit 中标注阶段号。

---

## 阶段 1：代码获取与就位
**目的**：把被测仓库准备到「目标版本」工作树，并产出变更文件集供下游打标签/对比。
- 1.1 仓库克隆/更新：本地缺失则 `git clone`，已存在则准备 fetch
- 1.2 浅克隆加深：`--deepen` + `protocol.version=0` 解决浅克隆 `git diff` 失真
- 1.3 确保 base/target ref 可解析：不可解析则按分支/标签 fetch
- 1.4 `checkout -f target`：使工作树 = 待测目标版本
- 1.5 计算变更文件集：`git diff --name-only base..target`
- 1.6 输出产物：`.pull_result.json`（local_path / base / target / changed_files / checked_out 等）
- **skill** ✅ `qa-code-pull`（封装 clone/fetch/加深/ref 确认/checkout/变更集计算的全部细节与离线降级）。本阶段完全由该 skill 覆盖，无平台代码依赖。

## 阶段 2：代码静态分析（测试点 + 功能点）
**目的**：对完整代码派生全面测试点，并抽取功能点（api/page/component）用于增量回归。
- 2.1 全面测试点生成（v2）：扫描完整仓库，按四维（模块/API/业务/前端）展开，产出 `by_type`(正常/异常/安全/边界) + `by_tag`(全量/更新)
- 2.2 范围过滤：按 `Scope` ∈ {正常,异常,安全,边界}（默认 正常+异常）过滤测试点
- 2.3 功能点抽取：从代码结构提取 api/page/component 功能点（供阶段 3 与 diff 对比）
- 2.4 增量功能点差异（可选）：`diff_analyzer` 对 base..target 做 new/removed/updated 三向分类，聚焦"这次改了什么"
- **skill** ✅ `qa-test-points`（封装 Scope 解析、标签打标、HTML/MD 输出规则；覆盖 2.1/2.2）；2.3/2.4 为平台内部模块 `code_analyzer` / `diff_analyzer`，**不强制 skill**，可复用阶段 1 的 `changed_files` 提速。

## 阶段 3：测试用例生成
**目的**：把功能点转为可执行、结构化的 case 规格。
- 3.1 功能点→case 映射：api→HTTP 探测 case；page→路由可达断言；component→交互元素断言，`steps` 为结构化 JSON
- 3.2 幂等增量生成：`generate_for_fp_ids` 按功能点幂等生成，避免重复
- 3.3 业务场景设计（实际路径）：research-agent 由测试点映射出「24 业务场景 F-A1~F-I5」，落 `live_test_results.json`
- **skill** ❌ 平台内部模块 `case_generator`，不强制 skill；若要把"场景设计"也技能化，可扩展 `qa-test-points`。
- ⚠️ **评审结论（2026-09-08）**：本阶段完整度约 **25%**，测试点（263 条）未进入用例链路、用例要素不完整、
  追溯在此断链、无用例交付物、评审门无流转入口——共 **5 项必须项**。详见
  `阶段3评审与未完成任务清单.md`。

## 阶段 4：测试执行
**目的**：在线上/被测环境实际运行用例，产出结果与截图。
- 4.1 复用优先判定：先查既有 harness/cases/REUSE 清单，确认是否复用而非重写
- 4.2 静态断言执行（平台内置）：`executor` 对路由/组件做结构性断言 + 单次 `/health` 探活，不依赖被测运行
- 4.3 真实浏览器 E2E 执行（实际用）：`exec_live.py` 用 Playwright 按「每场景独立 context + 复用登录态」跑 24 场景，落截图与判定
- 4.4 结果落盘：用例结果、截图路径、判定理由写入 `live_test_results.json` / `resp_*.txt` / `shot_*.png`
- **skill** ✅ `qa-reuse-validation`（4.1，强制复用既有能力、禁止重复造轮子、仅确无覆盖时新生成）+ ✅ `windows-browser-e2e-selftest`（4.3，本机沙箱用真实浏览器跑 E2E 自测、登录态复用、截图）。4.2 为平台模块 `executor`，不需 skill。
- ⚠️ **评审结论（2026-09-09）**：本阶段完整度约 **30%**。流程可跑通（263 条 + 报告），
  但**结论可信度实质阻断**——动态 HTTP 结果不参与判定、`expect`/异常维度从未校验（假阳性）；
  平台内无真实浏览器执行与截图（`exec_live.py` 在平台外且硬编码凭据）；
  另有无异常防护（单条异常中断整轮）、无 `batch_id`、无子集筛选等，共 **6 项必须项 D-★1~★6**。
  详见 `阶段4评审与未完成任务清单.md`。

## 阶段 5：执行监控与记录
**目的**：实时记录执行状态、进度、异常，支撑"任务不停、周期性汇报"。
- 5.1 执行日志：`exec_*.log` / `run_*.txt` 记录每场景耗时与返回
- 5.2 监控看板：`EXEC_MONITOR.md` 记录 24 场景进度与结论
- 5.3 状态快照：`state.json` 保存运行态，支持断点续跑（`exec_live_resume.py`）
- **skill** ❌ 均为阶段 4 执行脚本的副产物/辅助，不需独立 skill。

## 阶段 6：报告生成
**目的**：整合变更摘要/测试点/用例/结果/监控/结论，产出最终报告。
- 6.1 报告数据聚合：汇总变更面、测试点、用例清册、通过/失败统计、截图引用
- 6.2 报告模板渲染：用 `report_templates/` 把数据渲染为 HTML/MD
- 6.3 章节化输出：9 章节（变更摘要→测试点→用例清册→明细→统计→结论风险→监控→复用占比→截图）；research-agent 产物为 `VERIFICATION_REPORT.md/.html`
- 6.4 最终交付：报告系列 + 截图内嵌
- **skill** ❌ 平台内部能力（`reporter` / `html_report` / `gen_full_report` + `report_templates`），不需要独立 skill；如需"报告功能复用为技能"可后续抽取。

## 阶段 7：质量度量与复用占比量化
**目的**：量化「复用优先」成效与整体质量，闭环。
- 7.1 复用占比统计：构件复用率、代码量复用率（research-agent 实测 构件≈79%、代码量≈90%+）
- 7.2 通过率/失败归因：24/24 PASS、失败项根因分类（报告第五章）
- 7.3 结论与风险：是否可发布、遗留风险（如前端未回归）
- **skill** ❌ 报告延伸度量，由 `EXEC_MONITOR` + 报告推导，不需独立 skill。

---

## 跨阶段数据流（确保无遗漏）
- `qa-code-pull` 的 `.pull_result.json.changed_files` → 流入**阶段 2** 给测试点打「更新」标签、加速 2.4 增量对比
- **阶段 2** 测试点/功能点 → 流入**阶段 3** 生成 case、流入**阶段 6** 报告"测试点设计"章
- **阶段 4** 执行结果（`live_test_results.json` + 截图）→ 流入**阶段 5** 监控、**阶段 6** 报告、**阶段 7** 度量
- **阶段 6** 报告 → **阶段 7** 度量输入
- 全链路由 `qa-code-pull`（先）→ `qa-test-points`（后）的解耦顺序约定保证互不干扰

## skill 覆盖总览
| skill | 覆盖阶段 | 用途 |
|---|---|---|
| `qa-code-pull` | 阶段 1（全部） | 拉取代码、浅克隆加深、ref 确认、checkout、变更集 |
| `qa-test-points` | 阶段 2（2.1/2.2） | 测试点生成、Scope 过滤、标签、输出规则 |
| `qa-reuse-validation` | 阶段 4（4.1） | 复用优先策略，禁止重复造轮子 |
| `windows-browser-e2e-selftest` | 阶段 4（4.3） | 真实浏览器 E2E 自测、登录态复用、截图 |

> 阶段 3/5/6/7 目前均为平台内部模块或脚本副产物，未拆为独立 skill——这是后续可优化的落点之一（例如把"报告生成"或"场景设计"也技能化）。
