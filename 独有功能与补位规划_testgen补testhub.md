# testCodeFast（testgen）独有功能清单与对 testhub 的补位/服务化集成规划

> 对比对象：
> - **当前项目**：`testCodeFast/work/testgen-service`（简 **testgen**）—— Python + FastAPI + SQLite 的**代码→测试资产生成引擎**，含安全专项与生成链编排。
> - **对照项目**：`testhub_platform`（简 **testhub**）—— Django + DRF + Vue 的**测试中心平台**，21 个 app，覆盖用例库/执行/报告/缺陷/需求分析/LLM 评判/性能等。
>
> 本文是「重叠分析」「优化 vs 调接口」两份报告的**补集**：聚焦 **testgen 有、testhub 缺失** 的能力，并基于「testgen 封装为独立服务、由 testhub 调用以弥补功能不足」的未来规划，给出补位与服务化集成方案。
>
> 证据口径：testgen 侧以实际存在的源码文件为准；testhub 侧以全仓关键词检索零命中 + 既有报告（testhub `requirement_analysis` 为文档级、执行/报告/缺陷/资产为成熟实现）综合判定。

---

## 一、testgen 具备、testhub 缺失的能力清单

> 已排除与前两份报告重合的能力（执行驱动、报告导出、缺陷、资产持久化、性能、文档→用例、对话入口——这些 testhub 更强，属「调 testhub」而非「testgen 独有」）。下表仅列 testhub **确实没有** 或 **仅有自然语言占位、无结构化实现** 的能力。

| # | 能力 | testgen 实现（文件） | testhub 缺失证据 | 独有强度 |
|---|------|---------------------|----------------|---------|
| **U1** | **源码静态解析** | `engine/scan.py`（AST/调用图解析源码） | 全仓 `import ast`/`静态分析`/`代码扫描`/`源码解析` **0 命中** | 强独有 |
| **U2** | **功能点结构化提取** | `engine/fp_extract.py`，产出 `fp_id`（md5 内容指纹） | testhub「功能点」仅出现在 LLM prompt 文本里（PDF→LLM 时嘴上说），**无持久化、无代码派生、无 md5 指纹** | 强独有 |
| **U3** | **Diff 增量分析** | `engine/diff_tag.py` + F7 增量基线校验；代码变更衍生**全量/更新**测试点，带「类型」标签 | 全仓 `diff_tag`/`增量`/`全量测试点`/`git diff`/`base..target` **0 命中** | 强独有 |
| **U4** | **语义级功能点合并 + 结构化冲突** | `engine/fp_merge.py`（F5）：语义合并 + `stats["conflicts"]`（semantic_key/ftype/name/survivor/dropped/reason） | 全仓 `semantic_merge`/`fp_merge`/`结构化冲突`/`conflict`(testcases) **0 命中** | 强独有 |
| **U5** | **安全专项生成** | `engine/auth_scan.py`（安全期望值真实鉴权推导 F8）+ `engine/tenant_retest.py`（跨租户越权复测专项通道 G-9） | 全仓 `auth_scan`/`越权`/`tenant_retest`/`privilege`/`安全扫描` **0 命中**，无对应 app | 强独有（壁垒） |
| **U6** | **五维安全判定层** | `engine/executor.py` + `engine/runtime_ui.py`：NORMAL/ABNORMAL/**AUTH**/**PRIV_ESC**/**BOUNDARY** 五维结果判读 | testhub 执行引擎按 locator/assertion 判通过失败，**不懂「无凭证应拒/越权应拒」安全语义** | 强独有（大脑） |
| **U7** | **双通道输入 + 合并** | `output/channel_writer.py`：按 FP `file_path` 是否 `runtime:` 前缀拆 **code / url** 两通道独立记录 + 始终写 `channels_summary.json`；runtime 项恒脱敏 `runtime:<url>` | testhub 无「同一任务同时接收「代码仓库 + 活地址 URL/账号密码」并分通道产出再合并」的入口 | 差异化 |
| **U8** | **运行时 URL UI 元素分解** | 经 #221 元素分解 + #222 scope + #223 XHR 拦截 + #224 护栏，从活地址解析 DOM/网络请求生成结构化用例（16× 提升） | testhub 有 `ai_agent`（browser-use 自然语言执行）+ `playwright_engine`（录脚本执行），**走「自然语言/录脚本」路线，非「DOM+网络捕获结构化分解」路线** | 差异化（路线不同） |
| **U9** | **统一契约（数据指纹）** | `core/contracts.py`：fp_id/tp_id/case = 内容 md5（非自增）；三表唯一索引 `(project_id, id)`，碰撞写入侧失败不留孤儿 | testhub 用 Django 自增主键，无内容指纹去重/幂等写入机制 | 差异化（数据层） |
| **U10** | **智能输入框混合接收** | `cli` 的 A1 入口：自由文本混合接收「仓库地址 + 目标地址 + 账号 + 密码」，反对拆分多输入框 | testhub 无等价「一个框喂齐仓库+凭据即出测试资产」的统一入口（其用例来源是手工/导入/文档） | 差异化 |
| **U11** | **凭证/OTP 生命周期** | G-10：`_fetch_otp` + `RUNTIME_LOGIN_OTP_REFRESH_CMD` + `_login_with_retry`/`_relogin_if_needed`（两屏 OTP 流程已修复） | testhub `ui_automation` 有登录执行但未检索到等价的「OTP 刷新 + 自动重登」健壮生命周期 | 差异化（待精确核验） |
| **U12** | **调用图去噪** | F6：调用图噪声剔除，提升 FP 提取精度 | 无等价（testhub 不做调用图分析，谈不上去噪） | 强独有（内部） |

> 说明：**PRD 通道**（`engine/prd_ingest.py`，默认关）与 testhub `requirement_analysis` 同质，属重叠而非独有，不列入上表。

---

## 二、适合补充到 testhub 的功能筛选（用途 + 价值）

不是 testgen 的独有能力都该搬进 testhub。按「testhub 零替代 + 能显著补强平台」原则筛选：

### 2.1 强烈建议补充（testhub 零替代，补上即闭环「代码变更→测试资产」）

| 能力 | 用途 | 对 testhub 的价值 |
|------|------|-----------------|
| **U1 源码静态解析** | 把任意代码仓库解析成结构化中间表示（函数/调用关系） | testhub 当前只能从「文档」生成用例；补上后可**直接从源码**产出测试资产，覆盖无 PRD/无需求文档的纯代码项目 |
| **U2 功能点提取** | 从代码派生结构化、可持久化、带 md5 指纹的功能点 | 让 testhub 的用例库拥有**代码级来源的「功能点」实体**（而非 LLM 嘴上的词），可溯源、可 diff、可合并 |
| **U3 Diff 增量分析** | 给定 base..target，只产出**变更相关**的全量/更新测试点 | 支持 testhub 接入**发布流水线做部署后自动验证**——每次提交只跑受影响用例，省时且精准 |
| **U4 语义合并 + 结构化冲突** | 多源（如 code+url 双通道、多次扫描）功能点语义去重，输出冲突清单 | 让 testhub 的用例库在「多次生成/多源合并」时不膨胀、不丢真冲突，可审计 |
| **U5 安全专项生成** | 自动产出「越权/鉴权/跨租户」安全测试点 | testhub **完全没有安全测试生成能力**；补上后平台具备「业务功能 + 安全」双轨覆盖，是差异化卖点 |
| **U6 五维安全判定** | 把执行原始结果判读为 5 类（含 AUTH/PRIV_ESC/BOUNDARY） | 即使执行驱动交给 testhub，安全语义判读仍由 testgen 提供，保证安全测试**不会退化成只点页面** |

### 2.2 选择性补充（补强入口/执行健壮性，非核心但体验增益）

| 能力 | 用途 | 价值与取舍 |
|------|------|-----------|
| **U7 双通道 + U10 智能输入框** | 一个入口喂齐「仓库 + 活地址 + 账号密码」，分通道产出再合并 | 给 testhub 增加一个**低门槛、高收敛**的生成入口；但凭证传递需安全设计（见 3.2），建议由 testhub 转发而非 testgen 落明文 |
| **U11 凭证/OTP 生命周期** | 健壮的登录 + OTP 刷新 + 自动重登 | 若 testhub `ui_automation` 登录较弱，可借鉴；**敏感，建议对齐 testhub 既有凭证管理，避免双份明文** |
| **U9 统一契约** | 内容指纹 + 幂等写入 | 不必全量搬进 testhub，但 testgen 暴露给 testhub 的服务接口**输出应采用内容指纹**，便于 testhub 侧幂等入库、防重复 |

### 2.3 不必搬（保留为 testgen 内部能力）

- **U8 运行时 URL UI 分解**：与 testhub `ai_agent`（自然语言执行）路线不同，属 testgen 内部「生成侧」手法，可由 testgen 内部使用、产物（用例）再回流 testhub，不必把分解逻辑搬过去。
- **U12 调用图去噪**：纯内部精度优化，不对外暴露。

---

## 三、服务化集成评估（未来规划：testgen 封装为独立服务，testhub 调用）

> 规划：把 testgen 封装为**独立生成服务**，testhub 远程调用它以弥补上述功能缺口。下面逐能力评估「是否适合服务化调用」，并区分**直接复用**与**需改造**。

评判维度：① 是否无状态/可远程（输入→输出，不依赖 testgen 本地交互）；② 是否需 testgen 专有运行环境（git 仓库、浏览器、LLM key、目标系统凭证）；③ 接口形态建议。

| 能力 | 服务化适配度 | 直接复用 / 需改造 | 改造要点 | 建议接口形态 |
|------|------------|-----------------|---------|-------------|
| **U1+U2+U3+U4 生成主链路**（scan→fp→tp→case + diff + merge） | ⭐⭐⭐ 最高 | **大部分直接复用**：testgen 已有 `/api/v1/generate` 输出统一契约 JSON | 1. 契约对齐 testhub 资产模型（`TestCase`/`TestSuite`，md5→可映射）；2. 增加 `base`/`target` 增量参数；3. 鉴权（JWT 或服务间 token）；4. 脱敏（runtime 项不落明文） | `POST /api/v1/generate` 入参：repo/code/PRD + base/target + scope；回参：fp/tp/case 结构化数组 |
| **U5 安全专项生成** | ⭐⭐⭐ 高 | **生成部分直接复用**；**执行/复测部分需改造** | 把「生成安全测试点」与「执行+判定」解耦：生成可远程返回安全测试点；真正越权复测由 testhub 执行引擎跑、testgen 五维判定判读 | `POST /api/v1/generate?scope=安全` 或独立 `/api/v1/security-points` |
| **U6 五维安全判定** | ⭐⭐⭐ 高（适合做「判定微服务」） | **需改造为独立 verdict API** | 从 executor/runtime_ui 抽取纯判定函数，输入「执行原始结果 + 测试点安全语义」，输出五维 verdict，脱离具体执行驱动 | `POST /api/v1/verdict`（或 `/judge`）入参：raw_result + tp；回参：五维判定 |
| **U7+U10 双通道/智能输入框** | ⭐⭐ 中 | **需改造（安全）** | 入口收敛为「提交生成任务」接口；凭证由 testhub 经安全通道转发、testgen 不持久化明文；返回的合并产物含脱敏通道摘要 | `POST /api/v1/pipeline`（混合输入）→ 异步任务，轮询状态 |
| **U9 统一契约** | ⭐⭐ 中（非功能，是契约） | **需改造为映射层** | 服务化时 testgen 输出保留 md5 指纹；在 testhub 侧建「指纹→TestCase」映射，保证幂等入库、碰撞可检出 | 映射在 testhub 适配层实现，非 testgen 改动 |
| **U11 凭证/OTP 生命周期** | ⭐ 低（敏感，不建议通用暴露） | **需改造 / 受限暴露** | 属「边缘执行侧」能力（要真登录目标系统）。建议：仅在 testgen 边缘节点保留，testhub 不直连；或对齐 testhub 凭证保险箱，testgen 借用而非自存 | 不单独服务化；作为执行编排的内部步骤 |
| **U8 URL UI 分解** | ⭐⭐ 中 | **直接复用（内部）** | testgen 内部用其产出用例，产物经生成主链路回流 testhub；不必把分解逻辑单独服务化 | 作为 `/api/v1/generate` 的 `source_kind=url` 内部实现 |
| **U12 调用图去噪** | — | 内部优化，不暴露 | 无 | 不服务化 |

### 3.1 结论：适合「服务化调用」集成的核心集

**首选服务化的 4 块（testhub 零替代 + 天然可远程）：**
1. **生成主链路**（U1+U2+U3+U4）—— 直接复用 `/api/v1/generate`，契约对齐即可，是价值最大的补位。
2. **安全专项生成**（U5）—— 直接复用生成部分，执行复测解耦给 testhub。
3. **五维安全判定**（U6）—— 改造为独立 verdict 微服务，是 testgen 作为「安全大脑」的核心出口。
4. **双通道/智能输入框**（U7+U10）—— 改造为安全凭证转发的「提交任务」接口，给 testhub 一个统一低门槛入口。

**需谨慎/受限的 2 块：**
- **U11 凭证/OTP**：敏感，不通用暴露，仅边缘节点内部使用或对齐 testhub 凭证管理。
- **U9 统一契约**：不是功能，是数据契约，在 testhub 适配层做映射即可。

**不必服务化的 2 块：** U8（内部生成手法）、U12（内部精度优化）—— 产物回流即可。

### 3.2 直接复用 vs 需改造 — 一句话汇总

- **可直接复用**：生成主链路产出、安全测试点生成、URL UI 分解（内部）、调用图去噪（内部）。
- **需改造后集成**：① 输出契约对齐 testhub 资产模型 + 鉴权 + 脱敏；② 五维判定抽成独立 verdict API；③ 双通道入口改安全凭证转发；④ 越权复测解耦「生成(远程) / 执行(走 testhub)」；⑤ 凭证/OTP 对齐 testhub 凭证管理、不双份明文；⑥ 统一契约在 testhub 侧建映射层。

### 3.3 推荐落地顺序（服务化视角）

1. **Phase 1 — 生成服务暴露**：testgen 把 `/api/v1/generate`（已存在）补齐 `base/target/scope` 参数与契约，作为 testhub 的「代码→用例」远程能力；testhub 建 JWT/服务 token 客户端 + 资产映射层。→ 直接复用为主。
2. **Phase 2 — 安全大脑**：暴露安全测试点生成 + 五维 verdict API；testhub 执行引擎跑、testgen 判读。→ 生成复用、判定改造。
3. **Phase 3 — 统一入口**：双通道/智能输入框改安全凭证转发，作为 testhub 的「一键生成」入口。→ 改造。
4. **Phase 4 — 凭证收口**：U11 对齐 testhub 凭证保险箱，消除明文双份。→ 受限改造。

---

## 四、TL;DR（直接结论）

- **testgen 独有、testhub 缺失的能力共 12 项（U1–U12）**，集中在：**代码级生成（解析/提取/diff 增量/语义合并）、安全专项（auth_scan/tenant_retest/五维判定）、生成链编排（双通道/智能输入/统一契约/凭证 OTP/调用图去噪）**。
- **最该补进 testhub 的 6 项**（零替代 + 高价值）：源码解析、功能点提取、Diff 增量、语义合并、安全专项生成、五维安全判定。
- **服务化集成首选 4 块**：生成主链路、安全专项生成、五维判定（改 verdict 微服务）、双通道/智能输入框（改安全凭证转发）。其中**生成主链路与安全测试点生成可直接复用**，其余需做契约对齐/解耦/安全改造。
- **敏感与内部能力不通用服务化**：凭证/OTP 仅边缘节点或对齐 testhub 凭证管理；URL 分解与调用图去噪保留为 testgen 内部手法，产物回流即可。
- 这与前两篇结论一致且自洽：testgen 退守为「**代码→测试资产生成微服务 + 安全大脑**」，执行/报告/缺陷/资产/性能/对话下沉或复用 testhub。
