# 新版测试点清单（依据完整代码生成 · v2 全面覆盖）

> 生成方式：`code_analyzer.generate_comprehensive_test_points()` 对完整仓库静态扫描派生。

> **测试点范围（Scope）**：正常、边界（可选项：正常 / 异常 / 安全 / 边界；默认 正常+边界）。

> 总计 **259** 条测试点；按类型：正常 165、边界 94


> ## 本次测试范围概览

> - <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> **全量（本次完整测试内容）**：**259** 条 —— 代码未变更部分，常规全量回归执行。

> - <span style="color:#ffb454;font-weight:700;border:1px solid #ffb454;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">更新</span> **更新（本次仅变更/增量测试内容）**：**0** 条 —— 其底层路由/函数的源码行区间被 `test-20260906..test-20260907` 的 hunk 覆盖（hunk 级精准打标，避免整文件误标），优先回归 / 变更影响面验证。

> - 本次 diff 变更文件数：**0**（命中即标『更新』）。


> ## 类型标识图例（每一项测试点旁均带此标识）

> - <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> ＝ **全量**：本次完整测试的内容。

> - <span style="color:#ffb454;font-weight:700;border:1px solid #ffb454;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">更新</span> ＝ **更新**：本次仅变更或增量测试的内容。

> 在下方「逐条测试点」表的 **类型标识** 列，可一眼区分每条的归属；也可按该列筛选「全量 / 更新」两类。


> ## 业务语义说明（B2）

> 每条测试点的 **业务语义** 列 ＝ **业务域 · 业务动作**（如 `对话/会话 · 提交/创建`），由接口路径关键词与 HTTP 方法推导；**预期** 列同时给出「实现函数名 + docstring」参考，便于阶段3用例生成与阶段6报告锚定真实业务场景。


## 模块汇总

| 模块 | 测试点总数 | 更新数 | API维 | 业务函数维 | 前端维 | 覆盖面积(路由/函数) |
|---|---|---|---|---|---|---|
| 账户/用户/鉴权 Auth | 68 | 0 | 68 | 0 | 0 | /accounts、/accounts/{account_id}、/accounts/{account_id}/start、/accounts/{account_id}/stop、/accounts/{account_id}/whitelist、/accounts/{account_id}/whitelist/{user_id}、/api/v1/auth/login、/api/v1/auth/logout … |
| 产物/文档 Artifact | 40 | 0 | 40 | 0 | 0 | /api/v1/artifact/office-onlyoffice-callback、/api/v1/artifact/office-onlyoffice-config、/api/v1/onlyoffice-status、/api/v1/wps/config、/api/v1/wps/dl/{token}、/api/v1/wps/put/{token}、/api/v1/wps/register、/api/v1/wps/v3/3rd/files/{file_id} … |
| 审计/审核看板 Audit | 32 | 0 | 32 | 0 | 0 | /api/audit/selfcheck、/catalog、/items、/items/{item_id}、/items/{item_id}/events、/logs、/mock/config、/mock/state … |
| 线程 Thread | 18 | 0 | 18 | 0 | 0 | /api/v1/threads、/api/v1/threads/{tid}、/api/v1/threads/{tid}/abort、/api/v1/threads/{tid}/artifact/preview、/api/v1/threads/{tid}/result/{name}、/{schedule_id}、/{schedule_id}/run、/{schedule_id}/toggle |
| 对话/会话 Chat | 14 | 0 | 14 | 0 | 0 | /api/v1/chat、/ask-context、/ask-quota、/cancel、/diag-log、/summarize |
| 业务函数·主服务编排 | 12 | 0 | 0 | 12 | 0 | AskUserAnswer、ChatRequest、ContributionSubmitRequest、ThreadTitleUpdate、chat、chat_resume、custom_openapi、diag_log … |
| 业务函数·流水线接口 | 10 | 0 | 0 | 10 | 0 | DeployIn、EventIn、ItemIn、PushIn、add_event、create_item、get_item、hook_deploy … |
| 业务函数·邮件实时处理 | 9 | 0 | 0 | 9 | 0 | email_auth_status、fetch_live_messages、query_recent_emails、reset_request_bmp_email、reset_request_bmp_token、send_email、send_email_sync、set_request_bmp_email … |
| 业务函数·网关加解密 | 8 | 0 | 0 | 8 | 0 | GatewayKeys、decrypt_envelope、decrypt_request、decrypt_response、encrypt_envelope、encrypt_request、encrypt_response、load_keys |
| 业务函数·统一身份鉴权 | 8 | 0 | 0 | 8 | 0 | UnifiedLoginRequest、UnifiedLoginResponse、bmp_circuit_open、encrypt_password、gateway_renew、gateway_user、maybe_schedule_renew、unified_login |
| 业务函数·通知推送 | 7 | 0 | 0 | 7 | 0 | list_notifications、mark_read、push、seed_mock_approvals、sse_stream、subscribe、unsubscribe |
| 业务函数·内容总结 | 5 | 0 | 0 | 5 | 0 | configured、extract_json、llm_complete、llm_json、prescreen_mails |
| SQL/查询 Query | 4 | 0 | 4 | 0 | 0 | /api/sql/translate、/api/sql_explanation/retry |
| 工具/集成 Tool | 4 | 0 | 4 | 0 | 0 | /hook/deploy、/hook/push |
| 业务函数·晨会简报 | 3 | 0 | 0 | 3 | 0 | get_briefing_script、get_briefing_speech_path、get_morning_briefing |
| 业务函数·执行大厅指标 | 3 | 0 | 0 | 3 | 0 | get_summary、load_cached_summary、refresh_summary |
| 智能体 Agent | 2 | 0 | 2 | 0 | 0 | /api/v1/agents |
| 健康/其它 | 2 | 0 | 2 | 0 | 0 | /api/v1/health |
| 未归类 Other | 2 | 0 | 2 | 0 | 0 | /proxy |
| 简报 Briefing | 2 | 0 | 2 | 0 | 0 | /today |
| 业务函数·晨报生成 | 2 | 0 | 0 | 2 | 0 | load_cached_brief、refresh_brief |
| 前端 Frontend | 2 | 0 | 0 | 0 | 2 | 交互组件 KbPage.vue、交互组件 TasksTab.vue |
| 业务函数·执行大厅性能 | 1 | 0 | 0 | 1 | 0 | get_perf_cards |
| 业务函数·网关中间件 | 1 | 0 | 0 | 1 | 0 | encrypted_json_transport |

## 逐条测试点（每项旁带类型标识 + 业务语义 + 来源功能点）

| ID | 来源功能点 | 类型标识 | 业务语义 | 模块 | 区域/路由/函数 | 方法 | 类型 | 维度 | 预期 |
|---|---|---|---|---|---|---|---|---|---|
| TP-001 | `FP-91b068ac` 账户/用户/鉴权 · 登录鉴权（POST /api/v1/auth/login） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/api/v1/auth/login` | POST | 正常 | 正常-可用性 | 账户/用户/鉴权·登录鉴权 功能可用：POST /api/v1/auth/login 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 login |
| TP-002 | `FP-91b068ac` 账户/用户/鉴权 · 登录鉴权（POST /api/v1/auth/login） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/api/v1/auth/login` | POST | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·登录鉴权 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 login |
| TP-003 | `FP-57704045` 账户/用户/鉴权 · 登录鉴权（POST /api/v1/auth/refresh） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/api/v1/auth/refresh` | POST | 正常 | 正常-可用性 | 账户/用户/鉴权·登录鉴权 功能可用：POST /api/v1/auth/refresh 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 refresh |
| TP-004 | `FP-57704045` 账户/用户/鉴权 · 登录鉴权（POST /api/v1/auth/refresh） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/api/v1/auth/refresh` | POST | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·登录鉴权 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 refresh |
| TP-005 | `FP-fcc8d684` 账户/用户/鉴权 · 登录鉴权（POST /api/v1/auth/logout） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/api/v1/auth/logout` | POST | 正常 | 正常-可用性 | 账户/用户/鉴权·登录鉴权 功能可用：POST /api/v1/auth/logout 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 logout |
| TP-006 | `FP-fcc8d684` 账户/用户/鉴权 · 登录鉴权（POST /api/v1/auth/logout） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/api/v1/auth/logout` | POST | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·登录鉴权 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 logout |
| TP-007 | `FP-0cab29dd` 账户/用户/鉴权 · 登录鉴权（GET /api/v1/auth/me） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/api/v1/auth/me` | GET | 正常 | 正常-可用性 | 账户/用户/鉴权·登录鉴权 功能可用：GET /api/v1/auth/me 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 me |
| TP-008 | `FP-0cab29dd` 账户/用户/鉴权 · 登录鉴权（GET /api/v1/auth/me） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/api/v1/auth/me` | GET | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·登录鉴权 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 me |
| TP-009 | `FP-b7cd0f90` 线程 · 会话线程（GET /api/v1/threads） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 线程 · 会话线程 | 线程 Thread | `/api/v1/threads` | GET | 正常 | 正常-可用性 | 线程·会话线程 功能可用：GET /api/v1/threads 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 list_threads |
| TP-010 | `FP-b7cd0f90` 线程 · 会话线程（GET /api/v1/threads） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 线程 · 会话线程 | 线程 Thread | `/api/v1/threads` | GET | 边界 | 边界-参数缺失/非法 | 线程·会话线程 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 list_threads |
| TP-011 | `FP-c79dd3d3` 智能体 · 智能体编排（GET /api/v1/agents） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 智能体 · 智能体编排 | 智能体 Agent | `/api/v1/agents` | GET | 正常 | 正常-可用性 | 智能体·智能体编排 功能可用：GET /api/v1/agents 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 list_agents |
| TP-012 | `FP-c79dd3d3` 智能体 · 智能体编排（GET /api/v1/agents） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 智能体 · 智能体编排 | 智能体 Agent | `/api/v1/agents` | GET | 边界 | 边界-参数缺失/非法 | 智能体·智能体编排 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 list_agents |
| TP-013 | `FP-d08046c2` 线程 · 会话线程（GET /api/v1/threads/{tid}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 线程 · 会话线程 | 线程 Thread | `/api/v1/threads/{tid}` | GET | 正常 | 正常-可用性 | 线程·会话线程 功能可用：GET /api/v1/threads/{tid} 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 get_thread |
| TP-014 | `FP-d08046c2` 线程 · 会话线程（GET /api/v1/threads/{tid}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 线程 · 会话线程 | 线程 Thread | `/api/v1/threads/{tid}` | GET | 边界 | 边界-参数缺失/非法 | 线程·会话线程 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 get_thread |
| TP-015 | `FP-f6cd93f4` 线程 · 取消中止（POST /api/v1/threads/{tid}/abort） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 线程 · 取消中止 | 线程 Thread | `/api/v1/threads/{tid}/abort` | POST | 正常 | 正常-可用性 | 线程·取消中止 功能可用：POST /api/v1/threads/{tid}/abort 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 abort_thread：中断运行中任务（对齐真后端 POST /threads/{tid}/abort）： |
| TP-016 | `FP-f6cd93f4` 线程 · 取消中止（POST /api/v1/threads/{tid}/abort） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 线程 · 取消中止 | 线程 Thread | `/api/v1/threads/{tid}/abort` | POST | 边界 | 边界-参数缺失/非法 | 线程·取消中止 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 abort_thread：中断运行中任务（对齐真后端 POST /threads/{tid}/abort）： |
| TP-017 | `FP-4f4c303d` 线程 · 会话线程（GET /api/v1/threads/{tid}/result/{name}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 线程 · 会话线程 | 线程 Thread | `/api/v1/threads/{tid}/result/{name}` | GET | 正常 | 正常-可用性 | 线程·会话线程 功能可用：GET /api/v1/threads/{tid}/result/{name} 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 get_result |
| TP-018 | `FP-4f4c303d` 线程 · 会话线程（GET /api/v1/threads/{tid}/result/{name}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 线程 · 会话线程 | 线程 Thread | `/api/v1/threads/{tid}/result/{name}` | GET | 边界 | 边界-参数缺失/非法 | 线程·会话线程 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 get_result |
| TP-019 | `FP-b65000bd` 线程 · 审批审核（GET /api/v1/threads/{tid}/artifact/preview） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 线程 · 审批审核 | 线程 Thread | `/api/v1/threads/{tid}/artifact/preview` | GET | 正常 | 正常-可用性 | 线程·审批审核 功能可用：GET /api/v1/threads/{tid}/artifact/preview 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 artifact_preview |
| TP-020 | `FP-b65000bd` 线程 · 审批审核（GET /api/v1/threads/{tid}/artifact/preview） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 线程 · 审批审核 | 线程 Thread | `/api/v1/threads/{tid}/artifact/preview` | GET | 边界 | 边界-参数缺失/非法 | 线程·审批审核 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 artifact_preview |
| TP-021 | `FP-56d862c1` 对话/会话 · 对话交互（POST /api/v1/chat） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 对话/会话 · 对话交互 | 对话/会话 Chat | `/api/v1/chat` | POST | 正常 | 正常-可用性 | 对话/会话·对话交互 功能可用：POST /api/v1/chat 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 chat |
| TP-022 | `FP-56d862c1` 对话/会话 · 对话交互（POST /api/v1/chat） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 对话/会话 · 对话交互 | 对话/会话 Chat | `/api/v1/chat` | POST | 边界 | 边界-参数缺失/非法 | 对话/会话·对话交互 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 chat |
| TP-023 | `FP-0e12e38e` 审计/审核看板 · 提交/创建（POST /mock/config） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 提交/创建 | 审计/审核看板 Audit | `/mock/config` | POST | 正常 | 正常-可用性 | 审计/审核看板·提交/创建 功能可用：POST /mock/config 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 mock_config |
| TP-024 | `FP-0e12e38e` 审计/审核看板 · 提交/创建（POST /mock/config） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 提交/创建 | 审计/审核看板 Audit | `/mock/config` | POST | 边界 | 边界-参数缺失/非法 | 审计/审核看板·提交/创建 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 mock_config |
| TP-025 | `FP-2874864c` 审计/审核看板 · 查询（GET /mock/state） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 查询 | 审计/审核看板 Audit | `/mock/state` | GET | 正常 | 正常-可用性 | 审计/审核看板·查询 功能可用：GET /mock/state 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 mock_state |
| TP-026 | `FP-2874864c` 审计/审核看板 · 查询（GET /mock/state） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 查询 | 审计/审核看板 Audit | `/mock/state` | GET | 边界 | 边界-参数缺失/非法 | 审计/审核看板·查询 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 mock_state |
| TP-027 | `FP-f74dac84` 对话/会话 · 提交/创建（POST /diag-log） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 对话/会话 · 提交/创建 | 对话/会话 Chat | `/diag-log` | POST | 正常 | 正常-可用性 | 对话/会话·提交/创建 功能可用：POST /diag-log 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 diag_log_push |
| TP-028 | `FP-f74dac84` 对话/会话 · 提交/创建（POST /diag-log） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 对话/会话 · 提交/创建 | 对话/会话 Chat | `/diag-log` | POST | 边界 | 边界-参数缺失/非法 | 对话/会话·提交/创建 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 diag_log_push |
| TP-029 | `FP-d9dc1169` 对话/会话 · 查询（GET /diag-log） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 对话/会话 · 查询 | 对话/会话 Chat | `/diag-log` | GET | 正常 | 正常-可用性 | 对话/会话·查询 功能可用：GET /diag-log 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 diag_log_read |
| TP-030 | `FP-d9dc1169` 对话/会话 · 查询（GET /diag-log） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 对话/会话 · 查询 | 对话/会话 Chat | `/diag-log` | GET | 边界 | 边界-参数缺失/非法 | 对话/会话·查询 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 diag_log_read |
| TP-031 | `FP-85b27ee2` 健康/其它 · 健康检查（GET /api/v1/health） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 健康/其它 · 健康检查 | 健康/其它 | `/api/v1/health` | GET | 正常 | 正常-可用性 | 健康/其它·健康检查 功能可用：GET /api/v1/health 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 health |
| TP-032 | `FP-85b27ee2` 健康/其它 · 健康检查（GET /api/v1/health） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 健康/其它 · 健康检查 | 健康/其它 | `/api/v1/health` | GET | 边界 | 边界-参数缺失/非法 | 健康/其它·健康检查 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 health |
| TP-033 | `FP-aa2d2477` SQL/查询 · 检索查询（POST /api/sql/translate） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | SQL/查询 · 检索查询 | SQL/查询 Query | `/api/sql/translate` | POST | 正常 | 正常-可用性 | SQL/查询·检索查询 功能可用：POST /api/sql/translate 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 sql_translate：SQL → 中文翻译。SQLGlot 拆 AST + minimax M3 翻译。 |
| TP-034 | `FP-aa2d2477` SQL/查询 · 检索查询（POST /api/sql/translate） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | SQL/查询 · 检索查询 | SQL/查询 Query | `/api/sql/translate` | POST | 边界 | 边界-参数缺失/非法 | SQL/查询·检索查询 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 sql_translate：SQL → 中文翻译。SQLGlot 拆 AST + minimax M3 翻译。 |
| TP-035 | `FP-2b43392b` SQL/查询 · 检索查询（POST /api/sql_explanation/retry） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | SQL/查询 · 检索查询 | SQL/查询 Query | `/api/sql_explanation/retry` | POST | 正常 | 正常-可用性 | SQL/查询·检索查询 功能可用：POST /api/sql_explanation/retry 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 sql_explanation_retry：v0.7.2 · 注释版「重新生成」端点（与流内翻译共用 _do_translate_sync + 缓存）。 |
| TP-036 | `FP-2b43392b` SQL/查询 · 检索查询（POST /api/sql_explanation/retry） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | SQL/查询 · 检索查询 | SQL/查询 Query | `/api/sql_explanation/retry` | POST | 边界 | 边界-参数缺失/非法 | SQL/查询·检索查询 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 sql_explanation_retry：v0.7.2 · 注释版「重新生成」端点（与流内翻译共用 _do_translate_sync + 缓存）。 |
| TP-037 | `FP-156a8149` 审计/审核看板 · 审批审核（GET /api/audit/selfcheck） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 审批审核 | 审计/审核看板 Audit | `/api/audit/selfcheck` | GET | 正常 | 正常-可用性 | 审计/审核看板·审批审核 功能可用：GET /api/audit/selfcheck 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 audit_selfcheck：审计模块自检（收尾固化 · PM 2026-08-21 拍板）。 |
| TP-038 | `FP-156a8149` 审计/审核看板 · 审批审核（GET /api/audit/selfcheck） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 审批审核 | 审计/审核看板 Audit | `/api/audit/selfcheck` | GET | 边界 | 边界-参数缺失/非法 | 审计/审核看板·审批审核 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 audit_selfcheck：审计模块自检（收尾固化 · PM 2026-08-21 拍板）。 |
| TP-039 | `FP-cccee0b8` 审计/审核看板 · 查询（GET /items） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 查询 | 审计/审核看板 Audit | `/items` | GET | 正常 | 正常-可用性 | 审计/审核看板·查询 功能可用：GET /items 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 list_items |
| TP-040 | `FP-cccee0b8` 审计/审核看板 · 查询（GET /items） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 查询 | 审计/审核看板 Audit | `/items` | GET | 边界 | 边界-参数缺失/非法 | 审计/审核看板·查询 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 list_items |
| TP-041 | `FP-b1df251b` 审计/审核看板 · 查询（GET /items/{item_id}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 查询 | 审计/审核看板 Audit | `/items/{item_id}` | GET | 正常 | 正常-可用性 | 审计/审核看板·查询 功能可用：GET /items/{item_id} 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 get_item |
| TP-042 | `FP-b1df251b` 审计/审核看板 · 查询（GET /items/{item_id}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 查询 | 审计/审核看板 Audit | `/items/{item_id}` | GET | 边界 | 边界-参数缺失/非法 | 审计/审核看板·查询 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 get_item |
| TP-043 | `FP-4eccde88` 审计/审核看板 · 提交/创建（POST /items） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 提交/创建 | 审计/审核看板 Audit | `/items` | POST | 正常 | 正常-可用性 | 审计/审核看板·提交/创建 功能可用：POST /items 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 create_item |
| TP-044 | `FP-4eccde88` 审计/审核看板 · 提交/创建（POST /items） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 提交/创建 | 审计/审核看板 Audit | `/items` | POST | 边界 | 边界-参数缺失/非法 | 审计/审核看板·提交/创建 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 create_item |
| TP-045 | `FP-b28a44f4` 审计/审核看板 · 提交/创建（POST /items/{item_id}/events） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 提交/创建 | 审计/审核看板 Audit | `/items/{item_id}/events` | POST | 正常 | 正常-可用性 | 审计/审核看板·提交/创建 功能可用：POST /items/{item_id}/events 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 add_event |
| TP-046 | `FP-b28a44f4` 审计/审核看板 · 提交/创建（POST /items/{item_id}/events） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 提交/创建 | 审计/审核看板 Audit | `/items/{item_id}/events` | POST | 边界 | 边界-参数缺失/非法 | 审计/审核看板·提交/创建 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 add_event |
| TP-047 | `FP-84bfda66` 工具/集成 · 提交/创建（POST /hook/push） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 工具/集成 · 提交/创建 | 工具/集成 Tool | `/hook/push` | POST | 正常 | 正常-可用性 | 工具/集成·提交/创建 功能可用：POST /hook/push 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 hook_push：Gitee push 事件（webhook 服务转发）：提交信息含 fx-N 的事项自动打点。 |
| TP-048 | `FP-84bfda66` 工具/集成 · 提交/创建（POST /hook/push） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 工具/集成 · 提交/创建 | 工具/集成 Tool | `/hook/push` | POST | 边界 | 边界-参数缺失/非法 | 工具/集成·提交/创建 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 hook_push：Gitee push 事件（webhook 服务转发）：提交信息含 fx-N 的事项自动打点。 |
| TP-049 | `FP-105993e2` 工具/集成 · 提交/创建（POST /hook/deploy） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 工具/集成 · 提交/创建 | 工具/集成 Tool | `/hook/deploy` | POST | 正常 | 正常-可用性 | 工具/集成·提交/创建 功能可用：POST /hook/deploy 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 hook_deploy：部署完成回调：点亮⑦合并发布（若未绿）。线上验证(⑧)由冒烟脚本另行回写。 |
| TP-050 | `FP-105993e2` 工具/集成 · 提交/创建（POST /hook/deploy） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 工具/集成 · 提交/创建 | 工具/集成 Tool | `/hook/deploy` | POST | 边界 | 边界-参数缺失/非法 | 工具/集成·提交/创建 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 hook_deploy：部署完成回调：点亮⑦合并发布（若未绿）。线上验证(⑧)由冒烟脚本另行回写。 |
| TP-051 | `FP-e75c7259` 账户/用户/鉴权 · 登录鉴权（POST /unified-login） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/unified-login` | POST | 正常 | 正常-可用性 | 账户/用户/鉴权·登录鉴权 功能可用：POST /unified-login 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 unified_login：统一身份登录：校验太平统一登录 → upsert 本地用户 → 签发平台 JWT。 |
| TP-052 | `FP-e75c7259` 账户/用户/鉴权 · 登录鉴权（POST /unified-login） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/unified-login` | POST | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·登录鉴权 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 unified_login：统一身份登录：校验太平统一登录 → upsert 本地用户 → 签发平台 JWT。 |
| TP-053 | `FP-b0656585` 账户/用户/鉴权 · 提交/创建（POST /gateway-user） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 提交/创建 | 账户/用户/鉴权 Auth | `/gateway-user` | POST | 正常 | 正常-可用性 | 账户/用户/鉴权·提交/创建 功能可用：POST /gateway-user 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 gateway_user：调用文档第二个接口：校验 BMP token，并返回 token 对应邮箱。 |
| TP-054 | `FP-b0656585` 账户/用户/鉴权 · 提交/创建（POST /gateway-user） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 提交/创建 | 账户/用户/鉴权 Auth | `/gateway-user` | POST | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·提交/创建 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 gateway_user：调用文档第二个接口：校验 BMP token，并返回 token 对应邮箱。 |
| TP-055 | `FP-8a27aa92` 账户/用户/鉴权 · 登录鉴权（POST /gateway-renew） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/gateway-renew` | POST | 正常 | 正常-可用性 | 账户/用户/鉴权·登录鉴权 功能可用：POST /gateway-renew 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 gateway_renew：调用文档第三个接口：在 token 有效时续期。 |
| TP-056 | `FP-8a27aa92` 账户/用户/鉴权 · 登录鉴权（POST /gateway-renew） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/gateway-renew` | POST | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·登录鉴权 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 gateway_renew：调用文档第三个接口：在 token 有效时续期。 |
| TP-057 | `FP-aa176e17` 产物/文档 · 产物文档（GET /api/v1/wps/config） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 产物文档 | 产物/文档 Artifact | `/api/v1/wps/config` | GET | 正常 | 正常-可用性 | 产物/文档·产物文档 功能可用：GET /api/v1/wps/config 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 wps_config |
| TP-058 | `FP-aa176e17` 产物/文档 · 产物文档（GET /api/v1/wps/config） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 产物文档 | 产物/文档 Artifact | `/api/v1/wps/config` | GET | 边界 | 边界-参数缺失/非法 | 产物/文档·产物文档 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 wps_config |
| TP-059 | `FP-f9891eb6` 产物/文档 · 产物文档（POST /api/v1/wps/register） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 产物文档 | 产物/文档 Artifact | `/api/v1/wps/register` | POST | 正常 | 正常-可用性 | 产物/文档·产物文档 功能可用：POST /api/v1/wps/register 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 wps_register：把某个产物文件登记为 WPS file_id。body: {thread_id, path} |
| TP-060 | `FP-f9891eb6` 产物/文档 · 产物文档（POST /api/v1/wps/register） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 产物文档 | 产物/文档 Artifact | `/api/v1/wps/register` | POST | 边界 | 边界-参数缺失/非法 | 产物/文档·产物文档 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 wps_register：把某个产物文件登记为 WPS file_id。body: {thread_id, path} |
| TP-061 | `FP-b82f3b65` 产物/文档 · 产物文档（GET /api/v1/wps/v3/3rd/files/{file_id}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 产物文档 | 产物/文档 Artifact | `/api/v1/wps/v3/3rd/files/{file_id}` | GET | 正常 | 正常-可用性 | 产物/文档·产物文档 功能可用：GET /api/v1/wps/v3/3rd/files/{file_id} 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 cb_file_info |
| TP-062 | `FP-b82f3b65` 产物/文档 · 产物文档（GET /api/v1/wps/v3/3rd/files/{file_id}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 产物文档 | 产物/文档 Artifact | `/api/v1/wps/v3/3rd/files/{file_id}` | GET | 边界 | 边界-参数缺失/非法 | 产物/文档·产物文档 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 cb_file_info |
| TP-063 | `FP-0becabda` 产物/文档 · 导出下载（GET /api/v1/wps/v3/3rd/files/{file_id}/download） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 导出下载 | 产物/文档 Artifact | `/api/v1/wps/v3/3rd/files/{file_id}/download` | GET | 正常 | 正常-可用性 | 产物/文档·导出下载 功能可用：GET /api/v1/wps/v3/3rd/files/{file_id}/download 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 cb_download |
| TP-064 | `FP-0becabda` 产物/文档 · 导出下载（GET /api/v1/wps/v3/3rd/files/{file_id}/download） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 导出下载 | 产物/文档 Artifact | `/api/v1/wps/v3/3rd/files/{file_id}/download` | GET | 边界 | 边界-参数缺失/非法 | 产物/文档·导出下载 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 cb_download |
| TP-065 | `FP-1babeffe` 产物/文档 · 导出下载（GET /api/v1/wps/dl/{token}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 导出下载 | 产物/文档 Artifact | `/api/v1/wps/dl/{token}` | GET | 正常 | 正常-可用性 | 产物/文档·导出下载 功能可用：GET /api/v1/wps/dl/{token} 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 dl_file |
| TP-066 | `FP-1babeffe` 产物/文档 · 导出下载（GET /api/v1/wps/dl/{token}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 导出下载 | 产物/文档 Artifact | `/api/v1/wps/dl/{token}` | GET | 边界 | 边界-参数缺失/非法 | 产物/文档·导出下载 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 dl_file |
| TP-067 | `FP-e829009f` 产物/文档 · 产物文档（GET /api/v1/wps/v3/3rd/files/{file_id}/permission） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 产物文档 | 产物/文档 Artifact | `/api/v1/wps/v3/3rd/files/{file_id}/permission` | GET | 正常 | 正常-可用性 | 产物/文档·产物文档 功能可用：GET /api/v1/wps/v3/3rd/files/{file_id}/permission 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 cb_permission |
| TP-068 | `FP-e829009f` 产物/文档 · 产物文档（GET /api/v1/wps/v3/3rd/files/{file_id}/permission） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 产物文档 | 产物/文档 Artifact | `/api/v1/wps/v3/3rd/files/{file_id}/permission` | GET | 边界 | 边界-参数缺失/非法 | 产物/文档·产物文档 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 cb_permission |
| TP-069 | `FP-0ebac371` 产物/文档 · 产物文档（GET /api/v1/wps/v3/3rd/users） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 产物文档 | 产物/文档 Artifact | `/api/v1/wps/v3/3rd/users` | GET | 正常 | 正常-可用性 | 产物/文档·产物文档 功能可用：GET /api/v1/wps/v3/3rd/users 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 cb_users |
| TP-070 | `FP-0ebac371` 产物/文档 · 产物文档（GET /api/v1/wps/v3/3rd/users） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 产物文档 | 产物/文档 Artifact | `/api/v1/wps/v3/3rd/users` | GET | 边界 | 边界-参数缺失/非法 | 产物/文档·产物文档 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 cb_users |
| TP-071 | `FP-01ad8d5f` 产物/文档 · 上传导入（GET /api/v1/wps/v3/3rd/files/{file_id}/upload/prepare） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 上传导入 | 产物/文档 Artifact | `/api/v1/wps/v3/3rd/files/{file_id}/upload/prepare` | GET | 正常 | 正常-可用性 | 产物/文档·上传导入 功能可用：GET /api/v1/wps/v3/3rd/files/{file_id}/upload/prepare 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 cb_upload_prepare |
| TP-072 | `FP-01ad8d5f` 产物/文档 · 上传导入（GET /api/v1/wps/v3/3rd/files/{file_id}/upload/prepare） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 上传导入 | 产物/文档 Artifact | `/api/v1/wps/v3/3rd/files/{file_id}/upload/prepare` | GET | 边界 | 边界-参数缺失/非法 | 产物/文档·上传导入 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 cb_upload_prepare |
| TP-073 | `FP-df341d15` 产物/文档 · 上传导入（POST /api/v1/wps/v3/3rd/files/{file_id}/upload/address） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 上传导入 | 产物/文档 Artifact | `/api/v1/wps/v3/3rd/files/{file_id}/upload/address` | POST | 正常 | 正常-可用性 | 产物/文档·上传导入 功能可用：POST /api/v1/wps/v3/3rd/files/{file_id}/upload/address 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 cb_upload_address |
| TP-074 | `FP-df341d15` 产物/文档 · 上传导入（POST /api/v1/wps/v3/3rd/files/{file_id}/upload/address） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 上传导入 | 产物/文档 Artifact | `/api/v1/wps/v3/3rd/files/{file_id}/upload/address` | POST | 边界 | 边界-参数缺失/非法 | 产物/文档·上传导入 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 cb_upload_address |
| TP-075 | `FP-39969e96` 产物/文档 · 登录鉴权（PUT /api/v1/wps/put/{token}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 登录鉴权 | 产物/文档 Artifact | `/api/v1/wps/put/{token}` | PUT | 正常 | 正常-可用性 | 产物/文档·登录鉴权 功能可用：PUT /api/v1/wps/put/{token} 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 put_file |
| TP-076 | `FP-39969e96` 产物/文档 · 登录鉴权（PUT /api/v1/wps/put/{token}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 登录鉴权 | 产物/文档 Artifact | `/api/v1/wps/put/{token}` | PUT | 边界 | 边界-参数缺失/非法 | 产物/文档·登录鉴权 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 put_file |
| TP-077 | `FP-74397962` 产物/文档 · 上传导入（POST /api/v1/wps/v3/3rd/files/{file_id}/upload/complete） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 上传导入 | 产物/文档 Artifact | `/api/v1/wps/v3/3rd/files/{file_id}/upload/complete` | POST | 正常 | 正常-可用性 | 产物/文档·上传导入 功能可用：POST /api/v1/wps/v3/3rd/files/{file_id}/upload/complete 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 cb_upload_complete |
| TP-078 | `FP-74397962` 产物/文档 · 上传导入（POST /api/v1/wps/v3/3rd/files/{file_id}/upload/complete） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 上传导入 | 产物/文档 Artifact | `/api/v1/wps/v3/3rd/files/{file_id}/upload/complete` | POST | 边界 | 边界-参数缺失/非法 | 产物/文档·上传导入 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 cb_upload_complete |
| TP-079 | `FP-c0ed59cb` 产物/文档 · 产物文档（GET /document） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 产物文档 | 产物/文档 Artifact | `/document` | GET | 正常 | 正常-可用性 | 产物/文档·产物文档 功能可用：GET /document 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 get_audit_kb_document |
| TP-080 | `FP-c0ed59cb` 产物/文档 · 产物文档（GET /document） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 产物文档 | 产物/文档 Artifact | `/document` | GET | 边界 | 边界-参数缺失/非法 | 产物/文档·产物文档 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 get_audit_kb_document |
| TP-081 | `FP-720a39fc` 产物/文档 · 产物文档（GET /document_info） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 产物文档 | 产物/文档 Artifact | `/document_info` | GET | 正常 | 正常-可用性 | 产物/文档·产物文档 功能可用：GET /document_info 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 get_audit_kb_document_info：返回文件元信息（类型、总页数）。轻量、不渲染大内容。 |
| TP-082 | `FP-720a39fc` 产物/文档 · 产物文档（GET /document_info） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 产物文档 | 产物/文档 Artifact | `/document_info` | GET | 边界 | 边界-参数缺失/非法 | 产物/文档·产物文档 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 get_audit_kb_document_info：返回文件元信息（类型、总页数）。轻量、不渲染大内容。 |
| TP-083 | `FP-6b26c560` 审计/审核看板 · 检索查询（GET /regulations/search） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 检索查询 | 审计/审核看板 Audit | `/regulations/search` | GET | 正常 | 正常-可用性 | 审计/审核看板·检索查询 功能可用：GET /regulations/search 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 search_regulations_for_replacement：废止 chunk「查看现行版本」跳转用。 |
| TP-084 | `FP-6b26c560` 审计/审核看板 · 检索查询（GET /regulations/search） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 检索查询 | 审计/审核看板 Audit | `/regulations/search` | GET | 边界 | 边界-参数缺失/非法 | 审计/审核看板·检索查询 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 search_regulations_for_replacement：废止 chunk「查看现行版本」跳转用。 |
| TP-085 | `FP-99cc9204` 审计/审核看板 · 审批审核（GET /pending/count） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 审批审核 | 审计/审核看板 Audit | `/pending/count` | GET | 正常 | 正常-可用性 | 审计/审核看板·审批审核 功能可用：GET /pending/count 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 pending_count：返回待处理任务数量，供前端任务红点 badge 使用。 |
| TP-086 | `FP-99cc9204` 审计/审核看板 · 审批审核（GET /pending/count） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 审批审核 | 审计/审核看板 Audit | `/pending/count` | GET | 边界 | 边界-参数缺失/非法 | 审计/审核看板·审批审核 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 pending_count：返回待处理任务数量，供前端任务红点 badge 使用。 |
| TP-087 | `FP-df3b362f` 审计/审核看板 · 审批审核（GET /pending） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 审批审核 | 审计/审核看板 Audit | `/pending` | GET | 正常 | 正常-可用性 | 审计/审核看板·审批审核 功能可用：GET /pending 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 list_pending：列出待处理任务。 |
| TP-088 | `FP-df3b362f` 审计/审核看板 · 审批审核（GET /pending） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 审批审核 | 审计/审核看板 Audit | `/pending` | GET | 边界 | 边界-参数缺失/非法 | 审计/审核看板·审批审核 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 list_pending：列出待处理任务。 |
| TP-089 | `FP-13621e82` 审计/审核看板 · 审批审核（POST /pending/{task_id}/confirm） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 审批审核 | 审计/审核看板 Audit | `/pending/{task_id}/confirm` | POST | 正常 | 正常-可用性 | 审计/审核看板·审批审核 功能可用：POST /pending/{task_id}/confirm 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 confirm_pending：审计老师确认任务（废止核验 / 政策查阅）。 |
| TP-090 | `FP-13621e82` 审计/审核看板 · 审批审核（POST /pending/{task_id}/confirm） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 审批审核 | 审计/审核看板 Audit | `/pending/{task_id}/confirm` | POST | 边界 | 边界-参数缺失/非法 | 审计/审核看板·审批审核 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 confirm_pending：审计老师确认任务（废止核验 / 政策查阅）。 |
| TP-091 | `FP-a63a8123` 审计/审核看板 · 审批审核（POST /pending/{task_id}/dismiss） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 审批审核 | 审计/审核看板 Audit | `/pending/{task_id}/dismiss` | POST | 正常 | 正常-可用性 | 审计/审核看板·审批审核 功能可用：POST /pending/{task_id}/dismiss 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 dismiss_pending：审计老师驳回/忽略任务。 |
| TP-092 | `FP-a63a8123` 审计/审核看板 · 审批审核（POST /pending/{task_id}/dismiss） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 审批审核 | 审计/审核看板 Audit | `/pending/{task_id}/dismiss` | POST | 边界 | 边界-参数缺失/非法 | 审计/审核看板·审批审核 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 dismiss_pending：审计老师驳回/忽略任务。 |
| TP-093 | `FP-695b8ad8` 产物/文档 · 产物文档（GET /documents） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 产物文档 | 产物/文档 Artifact | `/documents` | GET | 正常 | 正常-可用性 | 产物/文档·产物文档 功能可用：GET /documents 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 list_documents：列出知识库文件清单（读现有 audit_kb sqlite）。 |
| TP-094 | `FP-695b8ad8` 产物/文档 · 产物文档（GET /documents） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 产物文档 | 产物/文档 Artifact | `/documents` | GET | 边界 | 边界-参数缺失/非法 | 产物/文档·产物文档 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 list_documents：列出知识库文件清单（读现有 audit_kb sqlite）。 |
| TP-095 | `FP-e245cd13` 产物/文档 · 产物文档（GET /documents/{src_id}/content） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 产物文档 | 产物/文档 Artifact | `/documents/{src_id}/content` | GET | 正常 | 正常-可用性 | 产物/文档·产物文档 功能可用：GET /documents/{src_id}/content 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 document_content：直接读取知识库数据库中的文档内容（用于无物理文件的案例预览）。 |
| TP-096 | `FP-e245cd13` 产物/文档 · 产物文档（GET /documents/{src_id}/content） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 产物文档 | 产物/文档 Artifact | `/documents/{src_id}/content` | GET | 边界 | 边界-参数缺失/非法 | 产物/文档·产物文档 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 document_content：直接读取知识库数据库中的文档内容（用于无物理文件的案例预览）。 |
| TP-097 | `FP-326f35f8` 产物/文档 · 产物文档（GET /documents/{src_id}/diff） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 产物文档 | 产物/文档 Artifact | `/documents/{src_id}/diff` | GET | 正常 | 正常-可用性 | 产物/文档·产物文档 功能可用：GET /documents/{src_id}/diff 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 document_diff：版本比对：返回段落级 diff（当前实现返回全文，前端做段落比对）。 |
| TP-098 | `FP-326f35f8` 产物/文档 · 产物文档（GET /documents/{src_id}/diff） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 产物文档 | 产物/文档 Artifact | `/documents/{src_id}/diff` | GET | 边界 | 边界-参数缺失/非法 | 产物/文档·产物文档 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 document_diff：版本比对：返回段落级 diff（当前实现返回全文，前端做段落比对）。 |
| TP-099 | `FP-ad480b34` 审计/审核看板 · 查询（GET /logs） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 查询 | 审计/审核看板 Audit | `/logs` | GET | 正常 | 正常-可用性 | 审计/审核看板·查询 功能可用：GET /logs 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 list_logs：审计日志查询。 |
| TP-100 | `FP-ad480b34` 审计/审核看板 · 查询（GET /logs） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 查询 | 审计/审核看板 Audit | `/logs` | GET | 边界 | 边界-参数缺失/非法 | 审计/审核看板·查询 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 list_logs：审计日志查询。 |
| TP-101 | `FP-560839ff` 账户/用户/鉴权 · 提交/创建（POST /users） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 提交/创建 | 账户/用户/鉴权 Auth | `/users` | POST | 正常 | 正常-可用性 | 账户/用户/鉴权·提交/创建 功能可用：POST /users 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 create_user：新建 user。 |
| TP-102 | `FP-560839ff` 账户/用户/鉴权 · 提交/创建（POST /users） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 提交/创建 | 账户/用户/鉴权 Auth | `/users` | POST | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·提交/创建 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 create_user：新建 user。 |
| TP-103 | `FP-0ac402e0` 账户/用户/鉴权 · 查询（GET /users） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 查询 | 账户/用户/鉴权 Auth | `/users` | GET | 正常 | 正常-可用性 | 账户/用户/鉴权·查询 功能可用：GET /users 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 list_users：列 user。 |
| TP-104 | `FP-0ac402e0` 账户/用户/鉴权 · 查询（GET /users） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 查询 | 账户/用户/鉴权 Auth | `/users` | GET | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·查询 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 list_users：列 user。 |
| TP-105 | `FP-78327682` 账户/用户/鉴权 · 部分更新（PATCH /users/{user_id}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 部分更新 | 账户/用户/鉴权 Auth | `/users/{user_id}` | PATCH | 正常 | 正常-可用性 | 账户/用户/鉴权·部分更新 功能可用：PATCH /users/{user_id} 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 patch_user：改 is_admin / status / password。 |
| TP-106 | `FP-78327682` 账户/用户/鉴权 · 部分更新（PATCH /users/{user_id}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 部分更新 | 账户/用户/鉴权 Auth | `/users/{user_id}` | PATCH | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·部分更新 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 patch_user：改 is_admin / status / password。 |
| TP-107 | `FP-49b47738` 账户/用户/鉴权 · 删除（DELETE /users/{user_id}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 删除 | 账户/用户/鉴权 Auth | `/users/{user_id}` | DELETE | 正常 | 正常-可用性 | 账户/用户/鉴权·删除 功能可用：DELETE /users/{user_id} 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 delete_user：删除 user。admin 不能删自己。 |
| TP-108 | `FP-49b47738` 账户/用户/鉴权 · 删除（DELETE /users/{user_id}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 删除 | 账户/用户/鉴权 Auth | `/users/{user_id}` | DELETE | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·删除 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 delete_user：删除 user。admin 不能删自己。 |
| TP-109 | `FP-56627f76` 账户/用户/鉴权 · 查询（GET /me） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 查询 | 账户/用户/鉴权 Auth | `/me` | GET | 正常 | 正常-可用性 | 账户/用户/鉴权·查询 功能可用：GET /me 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 my_permissions |
| TP-110 | `FP-56627f76` 账户/用户/鉴权 · 查询（GET /me） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 查询 | 账户/用户/鉴权 Auth | `/me` | GET | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·查询 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 my_permissions |
| TP-111 | `FP-3d7331e6` 审计/审核看板 · 查询（GET /catalog） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 查询 | 审计/审核看板 Audit | `/catalog` | GET | 正常 | 正常-可用性 | 审计/审核看板·查询 功能可用：GET /catalog 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 catalog |
| TP-112 | `FP-3d7331e6` 审计/审核看板 · 查询（GET /catalog） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 查询 | 审计/审核看板 Audit | `/catalog` | GET | 边界 | 边界-参数缺失/非法 | 审计/审核看板·查询 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 catalog |
| TP-113 | `FP-d4811a98` 账户/用户/鉴权 · 查询（GET /roles） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 查询 | 账户/用户/鉴权 Auth | `/roles` | GET | 正常 | 正常-可用性 | 账户/用户/鉴权·查询 功能可用：GET /roles 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 list_roles |
| TP-114 | `FP-d4811a98` 账户/用户/鉴权 · 查询（GET /roles） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 查询 | 账户/用户/鉴权 Auth | `/roles` | GET | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·查询 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 list_roles |
| TP-115 | `FP-1cdb3157` 账户/用户/鉴权 · 提交/创建（POST /roles） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 提交/创建 | 账户/用户/鉴权 Auth | `/roles` | POST | 正常 | 正常-可用性 | 账户/用户/鉴权·提交/创建 功能可用：POST /roles 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 create_role |
| TP-116 | `FP-1cdb3157` 账户/用户/鉴权 · 提交/创建（POST /roles） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 提交/创建 | 账户/用户/鉴权 Auth | `/roles` | POST | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·提交/创建 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 create_role |
| TP-117 | `FP-194b0998` 账户/用户/鉴权 · 更新（PUT /roles/{role_id}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 更新 | 账户/用户/鉴权 Auth | `/roles/{role_id}` | PUT | 正常 | 正常-可用性 | 账户/用户/鉴权·更新 功能可用：PUT /roles/{role_id} 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 update_role |
| TP-118 | `FP-194b0998` 账户/用户/鉴权 · 更新（PUT /roles/{role_id}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 更新 | 账户/用户/鉴权 Auth | `/roles/{role_id}` | PUT | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·更新 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 update_role |
| TP-119 | `FP-2eaeabc3` 账户/用户/鉴权 · 删除（DELETE /roles/{role_id}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 删除 | 账户/用户/鉴权 Auth | `/roles/{role_id}` | DELETE | 正常 | 正常-可用性 | 账户/用户/鉴权·删除 功能可用：DELETE /roles/{role_id} 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 delete_role：仅允许删除自定义角色；两个系统角色始终保留。 |
| TP-120 | `FP-2eaeabc3` 账户/用户/鉴权 · 删除（DELETE /roles/{role_id}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 删除 | 账户/用户/鉴权 Auth | `/roles/{role_id}` | DELETE | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·删除 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 delete_role：仅允许删除自定义角色；两个系统角色始终保留。 |
| TP-121 | `FP-0ae4b797` 账户/用户/鉴权 · 查询（GET /users） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 查询 | 账户/用户/鉴权 Auth | `/users` | GET | 正常 | 正常-可用性 | 账户/用户/鉴权·查询 功能可用：GET /users 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 list_role_users |
| TP-122 | `FP-0ae4b797` 账户/用户/鉴权 · 查询（GET /users） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 查询 | 账户/用户/鉴权 Auth | `/users` | GET | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·查询 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 list_role_users |
| TP-123 | `FP-d38ecd31` 账户/用户/鉴权 · 更新（PUT /users/{user_id}/role） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 更新 | 账户/用户/鉴权 Auth | `/users/{user_id}/role` | PUT | 正常 | 正常-可用性 | 账户/用户/鉴权·更新 功能可用：PUT /users/{user_id}/role 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 assign_role |
| TP-124 | `FP-d38ecd31` 账户/用户/鉴权 · 更新（PUT /users/{user_id}/role） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 更新 | 账户/用户/鉴权 Auth | `/users/{user_id}/role` | PUT | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·更新 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 assign_role |
| TP-125 | `FP-81ad1b1d` 账户/用户/鉴权 · 登录鉴权（POST /login） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/login` | POST | 正常 | 正常-可用性 | 账户/用户/鉴权·登录鉴权 功能可用：POST /login 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 login：账号密码登录。成功返回 access + refresh token pair。 |
| TP-126 | `FP-81ad1b1d` 账户/用户/鉴权 · 登录鉴权（POST /login） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/login` | POST | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·登录鉴权 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 login：账号密码登录。成功返回 access + refresh token pair。 |
| TP-127 | `FP-e6187b9d` 账户/用户/鉴权 · 提交/创建（POST /refresh） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 提交/创建 | 账户/用户/鉴权 Auth | `/refresh` | POST | 正常 | 正常-可用性 | 账户/用户/鉴权·提交/创建 功能可用：POST /refresh 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 refresh：用 refresh token 换新的 access + refresh pair。 |
| TP-128 | `FP-e6187b9d` 账户/用户/鉴权 · 提交/创建（POST /refresh） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 提交/创建 | 账户/用户/鉴权 Auth | `/refresh` | POST | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·提交/创建 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 refresh：用 refresh token 换新的 access + refresh pair。 |
| TP-129 | `FP-d6e94f1e` 账户/用户/鉴权 · 登录鉴权（POST /logout） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/logout` | POST | 正常 | 正常-可用性 | 账户/用户/鉴权·登录鉴权 功能可用：POST /logout 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 logout：把当前 access token 加入 blacklist。 |
| TP-130 | `FP-d6e94f1e` 账户/用户/鉴权 · 登录鉴权（POST /logout） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/logout` | POST | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·登录鉴权 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 logout：把当前 access token 加入 blacklist。 |
| TP-131 | `FP-af977b94` 账户/用户/鉴权 · 查询（GET /me） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 查询 | 账户/用户/鉴权 Auth | `/me` | GET | 正常 | 正常-可用性 | 账户/用户/鉴权·查询 功能可用：GET /me 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 me：返回当前 user info（iOS App 启动时调一次拿 user 信息 + 验证 token 仍有效）。 |
| TP-132 | `FP-af977b94` 账户/用户/鉴权 · 查询（GET /me） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 查询 | 账户/用户/鉴权 Auth | `/me` | GET | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·查询 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 me：返回当前 user info（iOS App 启动时调一次拿 user 信息 + 验证 token 仍有效）。 |
| TP-133 | `FP-1e11d8a6` 账户/用户/鉴权 · 登录鉴权（POST /change-password） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/change-password` | POST | 正常 | 正常-可用性 | 账户/用户/鉴权·登录鉴权 功能可用：POST /change-password 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 change_password：改密码。验证旧密码后写新 hash，同时把该 user 所有未过期 token 加进 blacklist。 |
| TP-134 | `FP-1e11d8a6` 账户/用户/鉴权 · 登录鉴权（POST /change-password） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/change-password` | POST | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·登录鉴权 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 change_password：改密码。验证旧密码后写新 hash，同时把该 user 所有未过期 token 加进 blacklist。 |
| TP-135 | `FP-f937ddbc` 未归类 · 提交/创建（POST /proxy） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 未归类 · 提交/创建 | 未归类 Other | `/proxy` | POST | 正常 | 正常-可用性 | 未归类·提交/创建 功能可用：POST /proxy 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 cockpit_proxy |
| TP-136 | `FP-f937ddbc` 未归类 · 提交/创建（POST /proxy） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 未归类 · 提交/创建 | 未归类 Other | `/proxy` | POST | 边界 | 边界-参数缺失/非法 | 未归类·提交/创建 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 cockpit_proxy |
| TP-137 | `FP-50731db7` 审计/审核看板 · 健康检查（POST /status） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 健康检查 | 审计/审核看板 Audit | `/status` | POST | 正常 | 正常-可用性 | 审计/审核看板·健康检查 功能可用：POST /status 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 auth_status：查询授权状态（卢总逻辑第一步：任何操作前先验证）。 |
| TP-138 | `FP-50731db7` 审计/审核看板 · 健康检查（POST /status） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 健康检查 | 审计/审核看板 Audit | `/status` | POST | 边界 | 边界-参数缺失/非法 | 审计/审核看板·健康检查 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 auth_status：查询授权状态（卢总逻辑第一步：任何操作前先验证）。 |
| TP-139 | `FP-d1cb1963` 账户/用户/鉴权 · 登录鉴权（POST /authorize） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/authorize` | POST | 正常 | 正常-可用性 | 账户/用户/鉴权·登录鉴权 功能可用：POST /authorize 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 authorize：授权邮箱。先查状态：已授权 → 直接返回不重复添加；未授权/授权过期 → 添加（过期=续期）。 |
| TP-140 | `FP-d1cb1963` 账户/用户/鉴权 · 登录鉴权（POST /authorize） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/authorize` | POST | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·登录鉴权 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 authorize：授权邮箱。先查状态：已授权 → 直接返回不重复添加；未授权/授权过期 → 添加（过期=续期）。 |
| TP-141 | `FP-23b49b51` 对话/会话 · 取消中止（POST /cancel） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 对话/会话 · 取消中止 | 对话/会话 Chat | `/cancel` | POST | 正常 | 正常-可用性 | 对话/会话·取消中止 功能可用：POST /cancel 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 cancel：取消授权。先查状态：存在有效/过期记录 → 软删除；未授权 → 无需取消。 |
| TP-142 | `FP-23b49b51` 对话/会话 · 取消中止（POST /cancel） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 对话/会话 · 取消中止 | 对话/会话 Chat | `/cancel` | POST | 边界 | 边界-参数缺失/非法 | 对话/会话·取消中止 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 cancel：取消授权。先查状态：存在有效/过期记录 → 软删除；未授权 → 无需取消。 |
| TP-143 | `FP-ab3827f0` 简报 · 查询（GET /today） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 简报 · 查询 | 简报 Briefing | `/today` | GET | 正常 | 正常-可用性 | 简报·查询 功能可用：GET /today 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 list_today：最近 3 天邮件清单（不含正文，供列表页展示）。路由名 /today 保留兼容前端。 |
| TP-144 | `FP-ab3827f0` 简报 · 查询（GET /today） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 简报 · 查询 | 简报 Briefing | `/today` | GET | 边界 | 边界-参数缺失/非法 | 简报·查询 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 list_today：最近 3 天邮件清单（不含正文，供列表页展示）。路由名 /today 保留兼容前端。 |
| TP-145 | `FP-ccc7212b` 对话/会话 · 总结简报（POST /summarize） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 对话/会话 · 总结简报 | 对话/会话 Chat | `/summarize` | POST | 正常 | 正常-可用性 | 对话/会话·总结简报 功能可用：POST /summarize 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 summarize：单封邮件 AI 总结（主动触发）。会议类自动写入日程库。 |
| TP-146 | `FP-ccc7212b` 对话/会话 · 总结简报（POST /summarize） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 对话/会话 · 总结简报 | 对话/会话 Chat | `/summarize` | POST | 边界 | 边界-参数缺失/非法 | 对话/会话·总结简报 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 summarize：单封邮件 AI 总结（主动触发）。会议类自动写入日程库。 |
| TP-147 | `FP-bd8f759f` 对话/会话 · 对话交互（GET /ask-quota） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 对话/会话 · 对话交互 | 对话/会话 Chat | `/ask-quota` | GET | 正常 | 正常-可用性 | 对话/会话·对话交互 功能可用：GET /ask-quota 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 ask_quota：「问福享」今日剩余次数（每人每天 1 次）。 |
| TP-148 | `FP-bd8f759f` 对话/会话 · 对话交互（GET /ask-quota） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 对话/会话 · 对话交互 | 对话/会话 Chat | `/ask-quota` | GET | 边界 | 边界-参数缺失/非法 | 对话/会话·对话交互 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 ask_quota：「问福享」今日剩余次数（每人每天 1 次）。 |
| TP-149 | `FP-f3750201` 对话/会话 · 对话交互（POST /ask-context） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 对话/会话 · 对话交互 | 对话/会话 Chat | `/ask-context` | POST | 正常 | 正常-可用性 | 对话/会话·对话交互 功能可用：POST /ask-context 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 ask_context：生成「问福享」开场白（含邮件正文与回复纪律），并消耗今日额度。 |
| TP-150 | `FP-f3750201` 对话/会话 · 对话交互（POST /ask-context） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 对话/会话 · 对话交互 | 对话/会话 Chat | `/ask-context` | POST | 边界 | 边界-参数缺失/非法 | 对话/会话·对话交互 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 ask_context：生成「问福享」开场白（含邮件正文与回复纪律），并消耗今日额度。 |
| TP-151 | `FP-179adbe5` 产物/文档 · 健康检查（GET /api/v1/onlyoffice-status） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 健康检查 | 产物/文档 Artifact | `/api/v1/onlyoffice-status` | GET | 正常 | 正常-可用性 | 产物/文档·健康检查 功能可用：GET /api/v1/onlyoffice-status 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 onlyoffice_status：返回 DS 是否可达 + URL,供前端探测后决定走 DS 编辑器还是 PDF 回退。 |
| TP-152 | `FP-179adbe5` 产物/文档 · 健康检查（GET /api/v1/onlyoffice-status） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 健康检查 | 产物/文档 Artifact | `/api/v1/onlyoffice-status` | GET | 边界 | 边界-参数缺失/非法 | 产物/文档·健康检查 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 onlyoffice_status：返回 DS 是否可达 + URL,供前端探测后决定走 DS 编辑器还是 PDF 回退。 |
| TP-153 | `FP-d0186c09` 产物/文档 · 产物文档（GET /api/v1/artifact/office-onlyoffice-config） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 产物文档 | 产物/文档 Artifact | `/api/v1/artifact/office-onlyoffice-config` | GET | 正常 | 正常-可用性 | 产物/文档·产物文档 功能可用：GET /api/v1/artifact/office-onlyoffice-config 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 office_onlyoffice_config：构造 OnlyOffice 编辑器配置(含 JWT),支持 docx / xlsx / pptx。 |
| TP-154 | `FP-d0186c09` 产物/文档 · 产物文档（GET /api/v1/artifact/office-onlyoffice-config） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 产物文档 | 产物/文档 Artifact | `/api/v1/artifact/office-onlyoffice-config` | GET | 边界 | 边界-参数缺失/非法 | 产物/文档·产物文档 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 office_onlyoffice_config：构造 OnlyOffice 编辑器配置(含 JWT),支持 docx / xlsx / pptx。 |
| TP-155 | `FP-ea62955c` 产物/文档 · 产物文档（POST /api/v1/artifact/office-onlyoffice-callback） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 产物文档 | 产物/文档 Artifact | `/api/v1/artifact/office-onlyoffice-callback` | POST | 正常 | 正常-可用性 | 产物/文档·产物文档 功能可用：POST /api/v1/artifact/office-onlyoffice-callback 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 office_onlyoffice_callback：DS 在保存 / 关闭时回调;下载新文件后原子覆盖原文件(不备份)。 |
| TP-156 | `FP-ea62955c` 产物/文档 · 产物文档（POST /api/v1/artifact/office-onlyoffice-callback） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 产物文档 | 产物/文档 Artifact | `/api/v1/artifact/office-onlyoffice-callback` | POST | 边界 | 边界-参数缺失/非法 | 产物/文档·产物文档 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 office_onlyoffice_callback：DS 在保存 / 关闭时回调;下载新文件后原子覆盖原文件(不备份)。 |
| TP-157 | `FP-6ba2cb9b` 线程 · 调度同步（PUT /{schedule_id}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 线程 · 调度同步 | 线程 Thread | `/{schedule_id}` | PUT | 正常 | 正常-可用性 | 线程·调度同步 功能可用：PUT /{schedule_id} 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 update_schedule：部分更新一条 schedule(仅 owner);不传 schedule 不更新。 |
| TP-158 | `FP-6ba2cb9b` 线程 · 调度同步（PUT /{schedule_id}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 线程 · 调度同步 | 线程 Thread | `/{schedule_id}` | PUT | 边界 | 边界-参数缺失/非法 | 线程·调度同步 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 update_schedule：部分更新一条 schedule(仅 owner);不传 schedule 不更新。 |
| TP-159 | `FP-5e173d23` 线程 · 调度同步（DELETE /{schedule_id}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 线程 · 调度同步 | 线程 Thread | `/{schedule_id}` | DELETE | 正常 | 正常-可用性 | 线程·调度同步 功能可用：DELETE /{schedule_id} 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 delete_schedule：删除一条 schedule(仅 owner)。 |
| TP-160 | `FP-5e173d23` 线程 · 调度同步（DELETE /{schedule_id}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 线程 · 调度同步 | 线程 Thread | `/{schedule_id}` | DELETE | 边界 | 边界-参数缺失/非法 | 线程·调度同步 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 delete_schedule：删除一条 schedule(仅 owner)。 |
| TP-161 | `FP-59d19239` 线程 · 调度同步（POST /{schedule_id}/run） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 线程 · 调度同步 | 线程 Thread | `/{schedule_id}/run` | POST | 正常 | 正常-可用性 | 线程·调度同步 功能可用：POST /{schedule_id}/run 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 run_schedule：立即触发一次该 schedule（不修改 schedule 配置；后台跑，不阻塞返回）。 |
| TP-162 | `FP-59d19239` 线程 · 调度同步（POST /{schedule_id}/run） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 线程 · 调度同步 | 线程 Thread | `/{schedule_id}/run` | POST | 边界 | 边界-参数缺失/非法 | 线程·调度同步 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 run_schedule：立即触发一次该 schedule（不修改 schedule 配置；后台跑，不阻塞返回）。 |
| TP-163 | `FP-2405813e` 线程 · 调度同步（POST /{schedule_id}/toggle） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 线程 · 调度同步 | 线程 Thread | `/{schedule_id}/toggle` | POST | 正常 | 正常-可用性 | 线程·调度同步 功能可用：POST /{schedule_id}/toggle 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 toggle_schedule：切换 enabled 状态(仅 owner)。 |
| TP-164 | `FP-2405813e` 线程 · 调度同步（POST /{schedule_id}/toggle） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 线程 · 调度同步 | 线程 Thread | `/{schedule_id}/toggle` | POST | 边界 | 边界-参数缺失/非法 | 线程·调度同步 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 toggle_schedule：切换 enabled 状态(仅 owner)。 |
| TP-165 | `FP-ecb687b5` 账户/用户/鉴权 · 登录鉴权（POST /login/start） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/login/start` | POST | 正常 | 正常-可用性 | 账户/用户/鉴权·登录鉴权 功能可用：POST /login/start 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 login_start：申请一个扫码登录二维码。 |
| TP-166 | `FP-ecb687b5` 账户/用户/鉴权 · 登录鉴权（POST /login/start） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/login/start` | POST | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·登录鉴权 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 login_start：申请一个扫码登录二维码。 |
| TP-167 | `FP-e14195fe` 账户/用户/鉴权 · 登录鉴权（GET /login/{session_key}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/login/{session_key}` | GET | 正常 | 正常-可用性 | 账户/用户/鉴权·登录鉴权 功能可用：GET /login/{session_key} 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 login_status：轮询扫码状态（一次 poll_qr_status 调用即返回）。 |
| TP-168 | `FP-e14195fe` 账户/用户/鉴权 · 登录鉴权（GET /login/{session_key}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/login/{session_key}` | GET | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·登录鉴权 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 login_status：轮询扫码状态（一次 poll_qr_status 调用即返回）。 |
| TP-169 | `FP-5bc8f455` 账户/用户/鉴权 · 登录鉴权（DELETE /login/{session_key}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/login/{session_key}` | DELETE | 正常 | 正常-可用性 | 账户/用户/鉴权·登录鉴权 功能可用：DELETE /login/{session_key} 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 login_cancel：主动取消扫码会话。 |
| TP-170 | `FP-5bc8f455` 账户/用户/鉴权 · 登录鉴权（DELETE /login/{session_key}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 登录鉴权 | 账户/用户/鉴权 Auth | `/login/{session_key}` | DELETE | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·登录鉴权 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 login_cancel：主动取消扫码会话。 |
| TP-171 | `FP-e7472679` 账户/用户/鉴权 · 查询（GET /accounts） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 查询 | 账户/用户/鉴权 Auth | `/accounts` | GET | 正常 | 正常-可用性 | 账户/用户/鉴权·查询 功能可用：GET /accounts 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 list_accounts：列出所有已登录账号 + 每个账号的 monitor 状态。 |
| TP-172 | `FP-e7472679` 账户/用户/鉴权 · 查询（GET /accounts） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 查询 | 账户/用户/鉴权 Auth | `/accounts` | GET | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·查询 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 list_accounts：列出所有已登录账号 + 每个账号的 monitor 状态。 |
| TP-173 | `FP-3e755d06` 账户/用户/鉴权 · 删除（DELETE /accounts/{account_id}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 删除 | 账户/用户/鉴权 Auth | `/accounts/{account_id}` | DELETE | 正常 | 正常-可用性 | 账户/用户/鉴权·删除 功能可用：DELETE /accounts/{account_id} 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 remove_account：删除账号（先 stop monitor，再删除凭据）。 |
| TP-174 | `FP-3e755d06` 账户/用户/鉴权 · 删除（DELETE /accounts/{account_id}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 删除 | 账户/用户/鉴权 Auth | `/accounts/{account_id}` | DELETE | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·删除 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 remove_account：删除账号（先 stop monitor，再删除凭据）。 |
| TP-175 | `FP-df4e1244` 账户/用户/鉴权 · 提交/创建（POST /accounts/{account_id}/start） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 提交/创建 | 账户/用户/鉴权 Auth | `/accounts/{account_id}/start` | POST | 正常 | 正常-可用性 | 账户/用户/鉴权·提交/创建 功能可用：POST /accounts/{account_id}/start 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 start_account_monitor：启动指定账号的 monitor（加入 MonitorPool）。 |
| TP-176 | `FP-df4e1244` 账户/用户/鉴权 · 提交/创建（POST /accounts/{account_id}/start） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 提交/创建 | 账户/用户/鉴权 Auth | `/accounts/{account_id}/start` | POST | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·提交/创建 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 start_account_monitor：启动指定账号的 monitor（加入 MonitorPool）。 |
| TP-177 | `FP-6b3b1099` 账户/用户/鉴权 · 取消中止（POST /accounts/{account_id}/stop） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 取消中止 | 账户/用户/鉴权 Auth | `/accounts/{account_id}/stop` | POST | 正常 | 正常-可用性 | 账户/用户/鉴权·取消中止 功能可用：POST /accounts/{account_id}/stop 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 stop_account_monitor：停止指定账号的 monitor（保留凭据）。 |
| TP-178 | `FP-6b3b1099` 账户/用户/鉴权 · 取消中止（POST /accounts/{account_id}/stop） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 取消中止 | 账户/用户/鉴权 Auth | `/accounts/{account_id}/stop` | POST | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·取消中止 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 stop_account_monitor：停止指定账号的 monitor（保留凭据）。 |
| TP-179 | `FP-e79dfaa6` 审计/审核看板 · 健康检查（GET /status） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 健康检查 | 审计/审核看板 Audit | `/status` | GET | 正常 | 正常-可用性 | 审计/审核看板·健康检查 功能可用：GET /status 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 wechat_status：整体状态概览。 |
| TP-180 | `FP-e79dfaa6` 审计/审核看板 · 健康检查（GET /status） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 审计/审核看板 · 健康检查 | 审计/审核看板 Audit | `/status` | GET | 边界 | 边界-参数缺失/非法 | 审计/审核看板·健康检查 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 wechat_status：整体状态概览。 |
| TP-181 | `FP-d8ab6080` 账户/用户/鉴权 · 查询（GET /accounts/{account_id}/whitelist） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 查询 | 账户/用户/鉴权 Auth | `/accounts/{account_id}/whitelist` | GET | 正常 | 正常-可用性 | 账户/用户/鉴权·查询 功能可用：GET /accounts/{account_id}/whitelist 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 get_whitelist：列出某账号的白名单。 |
| TP-182 | `FP-d8ab6080` 账户/用户/鉴权 · 查询（GET /accounts/{account_id}/whitelist） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 查询 | 账户/用户/鉴权 Auth | `/accounts/{account_id}/whitelist` | GET | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·查询 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 get_whitelist：列出某账号的白名单。 |
| TP-183 | `FP-3ce048fe` 账户/用户/鉴权 · 提交/创建（POST /accounts/{account_id}/whitelist） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 提交/创建 | 账户/用户/鉴权 Auth | `/accounts/{account_id}/whitelist` | POST | 正常 | 正常-可用性 | 账户/用户/鉴权·提交/创建 功能可用：POST /accounts/{account_id}/whitelist 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 add_to_whitelist：把 user_id 加入某账号白名单。 |
| TP-184 | `FP-3ce048fe` 账户/用户/鉴权 · 提交/创建（POST /accounts/{account_id}/whitelist） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 提交/创建 | 账户/用户/鉴权 Auth | `/accounts/{account_id}/whitelist` | POST | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·提交/创建 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 add_to_whitelist：把 user_id 加入某账号白名单。 |
| TP-185 | `FP-ac92ea8d` 账户/用户/鉴权 · 删除（DELETE /accounts/{account_id}/whitelist/{user_id}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 删除 | 账户/用户/鉴权 Auth | `/accounts/{account_id}/whitelist/{user_id}` | DELETE | 正常 | 正常-可用性 | 账户/用户/鉴权·删除 功能可用：DELETE /accounts/{account_id}/whitelist/{user_id} 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 remove_from_whitelist：从某账号白名单移除 user_id。 |
| TP-186 | `FP-ac92ea8d` 账户/用户/鉴权 · 删除（DELETE /accounts/{account_id}/whitelist/{user_id}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 账户/用户/鉴权 · 删除 | 账户/用户/鉴权 Auth | `/accounts/{account_id}/whitelist/{user_id}` | DELETE | 边界 | 边界-参数缺失/非法 | 账户/用户/鉴权·删除 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 remove_from_whitelist：从某账号白名单移除 user_id。 |
| TP-187 | `FP-942506ae` 产物/文档 · 导出下载（GET /dl/{short_id}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 导出下载 | 产物/文档 Artifact | `/dl/{short_id}` | GET | 正常 | 正常-可用性 | 产物/文档·导出下载 功能可用：GET /dl/{short_id} 路由存在且可调用，合法请求返回预期业务结果（2xx），关键字段完整；实现参考 download_artifact：短链 → 产物下载。强制 ``Content-Disposition: attachment``，避免 inline 打开。 |
| TP-188 | `FP-942506ae` 产物/文档 · 导出下载（GET /dl/{short_id}） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 产物/文档 · 导出下载 | 产物/文档 Artifact | `/dl/{short_id}` | GET | 边界 | 边界-参数缺失/非法 | 产物/文档·导出下载 入参校验：缺失必填 / 类型错误 / 越界值 → 422/400，错误信息可定位到具体字段；实现参考 download_artifact：短链 → 产物下载。强制 ``Content-Disposition: attachment``，避免 inline 打开。 |
| TP-189 | `FP-4c048ef0`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 主服务编排 · lifespan | 业务函数·主服务编排 | `lifespan` | FUNC | 正常 | 业务函数-逻辑可用 | 主服务编排 业务函数 lifespan 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-190 | `FP-90a2c8fb`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 主服务编排 · custom_openapi | 业务函数·主服务编排 | `custom_openapi` | FUNC | 正常 | 业务函数-逻辑可用 | 主服务编排 业务函数 custom_openapi 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-191 | `FP-ff3be445`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 主服务编排 · ChatRequest | 业务函数·主服务编排 | `ChatRequest` | FUNC | 正常 | 业务函数-逻辑可用 | 主服务编排 业务函数 ChatRequest 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-192 | `FP-9b90f748`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 主服务编排 · AskUserAnswer | 业务函数·主服务编排 | `AskUserAnswer` | FUNC | 正常 | 业务函数-逻辑可用 | 主服务编排 业务函数 AskUserAnswer 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：前端提交 ask_user 问题的回答。 |
| TP-193 | `FP-e2c254cf`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 主服务编排 · ThreadTitleUpdate | 业务函数·主服务编排 | `ThreadTitleUpdate` | FUNC | 正常 | 业务函数-逻辑可用 | 主服务编排 业务函数 ThreadTitleUpdate 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：前端提交会话重命名请求：title 为空字符串视为取消（保留原值）。 |
| TP-194 | `FP-59abdc46`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 主服务编排 · ContributionSubmitRequest | 业务函数·主服务编排 | `ContributionSubmitRequest` | FUNC | 正常 | 业务函数-逻辑可用 | 主服务编排 业务函数 ContributionSubmitRequest 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-195 | `FP-de585591`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 主服务编排 · issue_raw_ticket | 业务函数·主服务编排 | `issue_raw_ticket` | FUNC | 正常 | 业务函数-逻辑可用 | 主服务编排 业务函数 issue_raw_ticket 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：签发一次性 raw 访问 ticket(给 <iframe> / <img> / <a> 等浏览器被动请求使用)。 |
| TP-196 | `FP-dd92b00d`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 主服务编排 · chat | 业务函数·主服务编排 | `chat` | FUNC | 正常 | 业务函数-逻辑可用 | 主服务编排 业务函数 chat 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：启动 chat 流(P3 解耦后)。 |
| TP-197 | `FP-3297b4ae`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 主服务编排 · chat_resume | 业务函数·主服务编排 | `chat_resume` | FUNC | 正常 | 业务函数-逻辑可用 | 主服务编排 业务函数 chat_resume 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：接收 ask_user 问题的用户回答，用 `Command(resume=...)` 恢复 agent。 |
| TP-198 | `FP-6c086a57`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 主服务编排 · stream_chat_events | 业务函数·主服务编排 | `stream_chat_events` | FUNC | 正常 | 业务函数-逻辑可用 | 主服务编排 业务函数 stream_chat_events 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：SSE 端点：订阅某 thread 正在运行的 chat 事件流(P3 解耦后的 consumer)。 |
| TP-199 | `FP-8082ec57`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 主服务编排 · diag_log | 业务函数·主服务编排 | `diag_log` | FUNC | 正常 | 业务函数-逻辑可用 | 主服务编排 业务函数 diag_log 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：前端真机诊断遥测落盘（2026-09-04 卢总 iPhone standalone 视口问题定位用）。 |
| TP-200 | `FP-666a8e13`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 主服务编排 · ops_active_runs | 业务函数·主服务编排 | `ops_active_runs` | FUNC | 正常 | 业务函数-逻辑可用 | 主服务编排 业务函数 ops_active_runs 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：CI 部署探活：仅允许本机调用，返回活跃 run 数（免鉴权，部署脚本在停后端前轮询）。 |
| TP-201 | `FP-fbca58e2`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 网关加解密 · GatewayKeys | 业务函数·网关加解密 | `GatewayKeys` | FUNC | 正常 | 业务函数-逻辑可用 | 网关加解密 业务函数 GatewayKeys 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-202 | `FP-1253053c`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 网关加解密 · load_keys | 业务函数·网关加解密 | `load_keys` | FUNC | 正常 | 业务函数-逻辑可用 | 网关加解密 业务函数 load_keys 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：Load environment-appropriate keys from direct values or an ignored document. |
| TP-203 | `FP-29375b78`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 网关加解密 · encrypt_envelope | 业务函数·网关加解密 | `encrypt_envelope` | FUNC | 正常 | 业务函数-逻辑可用 | 网关加解密 业务函数 encrypt_envelope 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-204 | `FP-feb19f38`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 网关加解密 · decrypt_envelope | 业务函数·网关加解密 | `decrypt_envelope` | FUNC | 正常 | 业务函数-逻辑可用 | 网关加解密 业务函数 decrypt_envelope 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-205 | `FP-20efd2f7`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 网关加解密 · encrypt_request | 业务函数·网关加解密 | `encrypt_request` | FUNC | 正常 | 业务函数-逻辑可用 | 网关加解密 业务函数 encrypt_request 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-206 | `FP-b0582dfc`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 网关加解密 · decrypt_request | 业务函数·网关加解密 | `decrypt_request` | FUNC | 正常 | 业务函数-逻辑可用 | 网关加解密 业务函数 decrypt_request 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-207 | `FP-19b676e2`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 网关加解密 · encrypt_response | 业务函数·网关加解密 | `encrypt_response` | FUNC | 正常 | 业务函数-逻辑可用 | 网关加解密 业务函数 encrypt_response 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-208 | `FP-a90bfa32`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 网关加解密 · decrypt_response | 业务函数·网关加解密 | `decrypt_response` | FUNC | 正常 | 业务函数-逻辑可用 | 网关加解密 业务函数 decrypt_response 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-209 | `FP-8d82c1df`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 统一身份鉴权 · bmp_circuit_open | 业务函数·统一身份鉴权 | `bmp_circuit_open` | FUNC | 正常 | 业务函数-逻辑可用 | 统一身份鉴权 业务函数 bmp_circuit_open 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：熔断是否处于打开状态（拦截器据此降级放行）。 |
| TP-210 | `FP-29d4de44`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 统一身份鉴权 · maybe_schedule_renew | 业务函数·统一身份鉴权 | `maybe_schedule_renew` | FUNC | 正常 | 业务函数-逻辑可用 | 统一身份鉴权 业务函数 maybe_schedule_renew 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：每个 token 每 _GW_RENEW_INTERVAL 秒最多后台续期一次，不阻塞业务请求。 |
| TP-211 | `FP-4ffa87ca`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 统一身份鉴权 · encrypt_password | 业务函数·统一身份鉴权 | `encrypt_password` | FUNC | 正常 | 业务函数-逻辑可用 | 统一身份鉴权 业务函数 encrypt_password 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：AES-128-ECB/PKCS5 加密并 Base64，与统一登录侧 EncryptUtils.encryptByAes 一致。 |
| TP-212 | `FP-8648eecc`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 统一身份鉴权 · UnifiedLoginRequest | 业务函数·统一身份鉴权 | `UnifiedLoginRequest` | FUNC | 正常 | 业务函数-逻辑可用 | 统一身份鉴权 业务函数 UnifiedLoginRequest 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-213 | `FP-62b103cb`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 统一身份鉴权 · UnifiedLoginResponse | 业务函数·统一身份鉴权 | `UnifiedLoginResponse` | FUNC | 正常 | 业务函数-逻辑可用 | 统一身份鉴权 业务函数 UnifiedLoginResponse 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-214 | `FP-a09e2fb2`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 统一身份鉴权 · unified_login | 业务函数·统一身份鉴权 | `unified_login` | FUNC | 正常 | 业务函数-逻辑可用 | 统一身份鉴权 业务函数 unified_login 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：统一身份登录：校验太平统一登录 → upsert 本地用户 → 签发平台 JWT。 |
| TP-215 | `FP-add46bf0`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 统一身份鉴权 · gateway_user | 业务函数·统一身份鉴权 | `gateway_user` | FUNC | 正常 | 业务函数-逻辑可用 | 统一身份鉴权 业务函数 gateway_user 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：调用文档第二个接口：校验 BMP token，并返回 token 对应邮箱。 |
| TP-216 | `FP-6c3f37dc`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 统一身份鉴权 · gateway_renew | 业务函数·统一身份鉴权 | `gateway_renew` | FUNC | 正常 | 业务函数-逻辑可用 | 统一身份鉴权 业务函数 gateway_renew 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：调用文档第三个接口：在 token 有效时续期。 |
| TP-217 | `FP-3a6caacb`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 邮件实时处理 · set_request_bmp_token | 业务函数·邮件实时处理 | `set_request_bmp_token` | FUNC | 正常 | 业务函数-逻辑可用 | 邮件实时处理 业务函数 set_request_bmp_token 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：由 HTTP 中间件按请求注入，供 Agent 等非路由调用安全复用。 |
| TP-218 | `FP-7ccf200a`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 邮件实时处理 · reset_request_bmp_token | 业务函数·邮件实时处理 | `reset_request_bmp_token` | FUNC | 正常 | 业务函数-逻辑可用 | 邮件实时处理 业务函数 reset_request_bmp_token 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-219 | `FP-e24bb671`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 邮件实时处理 · set_request_bmp_email | 业务函数·邮件实时处理 | `set_request_bmp_email` | FUNC | 正常 | 业务函数-逻辑可用 | 邮件实时处理 业务函数 set_request_bmp_email 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-220 | `FP-54153a9d`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 邮件实时处理 · reset_request_bmp_email | 业务函数·邮件实时处理 | `reset_request_bmp_email` | FUNC | 正常 | 业务函数-逻辑可用 | 邮件实时处理 业务函数 reset_request_bmp_email 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-221 | `FP-7056cab4`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 邮件实时处理 · fetch_live_messages | 业务函数·邮件实时处理 | `fetch_live_messages` | FUNC | 正常 | 业务函数-逻辑可用 | 邮件实时处理 业务函数 fetch_live_messages 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：查询邮件网关，返回 (实时消息卡片列表, 是否实时成功)。 |
| TP-222 | `FP-7b5835db`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 邮件实时处理 · email_auth_status | 业务函数·邮件实时处理 | `email_auth_status` | FUNC | 正常 | 业务函数-逻辑可用 | 邮件实时处理 业务函数 email_auth_status 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：查询当前监控邮箱（EMAIL_ACCOUNT）的授权状态；失败返回 None。 |
| TP-223 | `FP-a91b1807`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 邮件实时处理 · query_recent_emails | 业务函数·邮件实时处理 | `query_recent_emails` | FUNC | 正常 | 业务函数-逻辑可用 | 邮件实时处理 业务函数 query_recent_emails 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：同步查询最近收件（供 agent 工具调用）。 |
| TP-224 | `FP-d6c97513`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 邮件实时处理 · send_email_sync | 业务函数·邮件实时处理 | `send_email_sync` | FUNC | 正常 | 业务函数-逻辑可用 | 邮件实时处理 业务函数 send_email_sync 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：同步发信（2026-08-16 回复闭环：供 agent 工具 email_send 在线程池调用）。 |
| TP-225 | `FP-30af09ba`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 邮件实时处理 · send_email | 业务函数·邮件实时处理 | `send_email` | FUNC | 正常 | 业务函数-逻辑可用 | 邮件实时处理 业务函数 send_email 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：调网关 /sendemail 发邮件（测试环境收发同邮箱）。返回解密后的 {code,msg,data}。 |
| TP-226 | `FP-5d95d8dc`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 晨会简报 · get_morning_briefing | 业务函数·晨会简报 | `get_morning_briefing` | FUNC | 正常 | 业务函数-逻辑可用 | 晨会简报 业务函数 get_morning_briefing 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：返回今日晨报 {date, content, mail_count, event_count}；无素材或 LLM 失败时 content 为兜底文案。 |
| TP-227 | `FP-275038d0`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 晨会简报 · get_briefing_script | 业务函数·晨会简报 | `get_briefing_script` | FUNC | 正常 | 业务函数-逻辑可用 | 晨会简报 业务函数 get_briefing_script 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：返回今日口播稿（LLM 凝练口语版晨报），按 (user_id, 日期) 缓存。 |
| TP-228 | `FP-fcce7548`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 晨会简报 · get_briefing_speech_path | 业务函数·晨会简报 | `get_briefing_speech_path` | FUNC | 正常 | 业务函数-逻辑可用 | 晨会简报 业务函数 get_briefing_speech_path 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：返回今日晨报语音 mp3 的本地路径；命中缓存直接给，否则 edge-tts 现合成。 |
| TP-229 | `FP-de88860b`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 晨报生成 · load_cached_brief | 业务函数·晨报生成 | `load_cached_brief` | FUNC | 正常 | 业务函数-逻辑可用 | 晨报生成 业务函数 load_cached_brief 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-230 | `FP-24321c84`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 晨报生成 · refresh_brief | 业务函数·晨报生成 | `refresh_brief` | FUNC | 正常 | 业务函数-逻辑可用 | 晨报生成 业务函数 refresh_brief 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：跑批入口：四板块拼装 → 落缓存 → 推通知中心。失败抛异常由 runner 记录， |
| TP-231 | `FP-f81005a1`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 内容总结 · configured | 业务函数·内容总结 | `configured` | FUNC | 正常 | 业务函数-逻辑可用 | 内容总结 业务函数 configured 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-232 | `FP-921ab091`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 内容总结 · llm_complete | 业务函数·内容总结 | `llm_complete` | FUNC | 正常 | 业务函数-逻辑可用 | 内容总结 业务函数 llm_complete 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：统一 LLM 调用：reasoning_split + 空 content 重试 + token 记录。 |
| TP-233 | `FP-681e2bcc`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 内容总结 · extract_json | 业务函数·内容总结 | `extract_json` | FUNC | 正常 | 业务函数-逻辑可用 | 内容总结 业务函数 extract_json 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：从模型输出里抠出 JSON 对象（容错：代码围栏 / 前后杂质）。 |
| TP-234 | `FP-f9a8dc6c`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 内容总结 · llm_json | 业务函数·内容总结 | `llm_json` | FUNC | 正常 | 业务函数-逻辑可用 | 内容总结 业务函数 llm_json 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：LLM 调用并要求输出 JSON；解析失败/空结果重试，彻底失败返回 None。 |
| TP-235 | `FP-c1692b1a`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 内容总结 · prescreen_mails | 业务函数·内容总结 | `prescreen_mails` | FUNC | 正常 | 业务函数-逻辑可用 | 内容总结 业务函数 prescreen_mails 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：元数据初筛：返回选中邮件的下标。失败/未配置时退化为前 keep 封（时间序）。 |
| TP-236 | `FP-73ae207d`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 通知推送 · push | 业务函数·通知推送 | `push` | FUNC | 正常 | 业务函数-逻辑可用 | 通知推送 业务函数 push 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：写一条通知并广播给在线 SSE 订阅者。kind 非法/空标题直接丢弃。 |
| TP-237 | `FP-bfa3e724`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 通知推送 · list_notifications | 业务函数·通知推送 | `list_notifications` | FUNC | 正常 | 业务函数-逻辑可用 | 通知推送 业务函数 list_notifications 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-238 | `FP-c99c6fa7`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 通知推送 · mark_read | 业务函数·通知推送 | `mark_read` | FUNC | 正常 | 业务函数-逻辑可用 | 通知推送 业务函数 mark_read 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-239 | `FP-cb8cfd04`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 通知推送 · seed_mock_approvals | 业务函数·通知推送 | `seed_mock_approvals` | FUNC | 正常 | 业务函数-逻辑可用 | 通知推送 业务函数 seed_mock_approvals 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-240 | `FP-875f823a`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 通知推送 · subscribe | 业务函数·通知推送 | `subscribe` | FUNC | 正常 | 业务函数-逻辑可用 | 通知推送 业务函数 subscribe 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-241 | `FP-4ec2a506`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 通知推送 · unsubscribe | 业务函数·通知推送 | `unsubscribe` | FUNC | 正常 | 业务函数-逻辑可用 | 通知推送 业务函数 unsubscribe 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-242 | `FP-58d0010f`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 通知推送 · sse_stream | 业务函数·通知推送 | `sse_stream` | FUNC | 正常 | 业务函数-逻辑可用 | 通知推送 业务函数 sse_stream 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：SSE 事件流：先发一条 hello（含当前未读数），之后推新通知；25s 心跳。 |
| TP-243 | `FP-cb42e7b5`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 执行大厅指标 · load_cached_summary | 业务函数·执行大厅指标 | `load_cached_summary` | FUNC | 正常 | 业务函数-逻辑可用 | 执行大厅指标 业务函数 load_cached_summary 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：同步读缓存（供 briefing 晨播报业绩段使用）；无缓存/损坏返回 None，绝不触发 LLM。 |
| TP-244 | `FP-17099bca`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 执行大厅指标 · refresh_summary | 业务函数·执行大厅指标 | `refresh_summary` | FUNC | 正常 | 业务函数-逻辑可用 | 执行大厅指标 业务函数 refresh_summary 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：跑批入口：提取最近 6 张问数表 → LLM 总结 → 写缓存。 |
| TP-245 | `FP-41eb06e5`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 执行大厅指标 · get_summary | 业务函数·执行大厅指标 | `get_summary` | FUNC | 正常 | 业务函数-逻辑可用 | 执行大厅指标 业务函数 get_summary 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：读缓存；不存在则同步生成一次（首次访问兜底），生成失败返回 empty 态不 500。 |
| TP-246 | `FP-d7bc79f1`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 执行大厅性能 · get_perf_cards | 业务函数·执行大厅性能 | `get_perf_cards` | FUNC | 正常 | 业务函数-逻辑可用 | 执行大厅性能 业务函数 get_perf_cards 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-247 | `FP-7be8b6ff`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 流水线接口 · list_items | 业务函数·流水线接口 | `list_items` | FUNC | 正常 | 业务函数-逻辑可用 | 流水线接口 业务函数 list_items 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-248 | `FP-46d9f674`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 流水线接口 · get_item | 业务函数·流水线接口 | `get_item` | FUNC | 正常 | 业务函数-逻辑可用 | 流水线接口 业务函数 get_item 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-249 | `FP-64261c69`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 流水线接口 · ItemIn | 业务函数·流水线接口 | `ItemIn` | FUNC | 正常 | 业务函数-逻辑可用 | 流水线接口 业务函数 ItemIn 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-250 | `FP-d715c4cd`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 流水线接口 · create_item | 业务函数·流水线接口 | `create_item` | FUNC | 正常 | 业务函数-逻辑可用 | 流水线接口 业务函数 create_item 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-251 | `FP-7f61d088`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 流水线接口 · EventIn | 业务函数·流水线接口 | `EventIn` | FUNC | 正常 | 业务函数-逻辑可用 | 流水线接口 业务函数 EventIn 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-252 | `FP-3897f240`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 流水线接口 · add_event | 业务函数·流水线接口 | `add_event` | FUNC | 正常 | 业务函数-逻辑可用 | 流水线接口 业务函数 add_event 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-253 | `FP-3950d0f7`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 流水线接口 · PushIn | 业务函数·流水线接口 | `PushIn` | FUNC | 正常 | 业务函数-逻辑可用 | 流水线接口 业务函数 PushIn 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-254 | `FP-b3a039fc`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 流水线接口 · hook_push | 业务函数·流水线接口 | `hook_push` | FUNC | 正常 | 业务函数-逻辑可用 | 流水线接口 业务函数 hook_push 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：Gitee push 事件（webhook 服务转发）：提交信息含 fx-N 的事项自动打点。 |
| TP-255 | `FP-4e15f183`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 流水线接口 · DeployIn | 业务函数·流水线接口 | `DeployIn` | FUNC | 正常 | 业务函数-逻辑可用 | 流水线接口 业务函数 DeployIn 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测 |
| TP-256 | `FP-6e3c69f3`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 流水线接口 · hook_deploy | 业务函数·流水线接口 | `hook_deploy` | FUNC | 正常 | 业务函数-逻辑可用 | 流水线接口 业务函数 hook_deploy 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：部署完成回调：点亮⑦合并发布（若未绿）。线上验证(⑧)由冒烟脚本另行回写。 |
| TP-257 | `FP-23d6f2ec`  | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 网关中间件 · encrypted_json_transport | 业务函数·网关中间件 | `encrypted_json_transport` | FUNC | 正常 | 业务函数-逻辑可用 | 网关中间件 业务函数 encrypted_json_transport 逻辑正确：合法输入输出符合预期，异常输入不崩溃且可观测；实现说明：Decrypt marked JSON requests and encrypt their JSON responses. |
| TP-258 | `FP-f4e4bde3` 前端组件 · 交互触发（交互组件 KbPage.vue） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 前端组件 · 交互 | 前端 Frontend | `交互组件 KbPage.vue` | UI | 正常 | 交互元素可用 | 交互组件 交互组件 KbPage.vue 可正常触发：含交互钩子(@click/表单/上传)，操作有响应 |
| TP-259 | `FP-76a78799` 前端组件 · 交互触发（交互组件 TasksTab.vue） | <span style="color:#3ecf8e;font-weight:700;border:1px solid #3ecf8e;border-radius:4px;padding:1px 7px;font-size:12px;white-space:nowrap">全量</span> | 前端组件 · 交互 | 前端 Frontend | `交互组件 TasksTab.vue` | UI | 正常 | 交互元素可用 | 交互组件 交互组件 TasksTab.vue 可正常触发：含交互钩子(@click/表单/上传)，操作有响应 |

> **双向追溯（A2）**：功能点 165 个，本轮范围内派生出测试点的 165 个（100.0%），孤儿测试点 0 条；完整「功能点 → 测试点」矩阵见 **FUNCTIONAL_POINTS.md**。
