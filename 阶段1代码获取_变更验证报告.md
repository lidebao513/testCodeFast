# 阶段 1【代码获取】变更验证报告

> **验证对象**：本轮「1. 代码获取」的全部更改（`pull_code` 评审 P0/P1/P2 落地成果）
> **验证时间**：2026-09-11 13:20~13:30
> **验证方式**：静态阅读 + 实际运行（含边界构造）+ 门禁复核；**未修改任何代码**
> **验证维度**：① 代码规范（命名 / 风格 / 复杂度 / 可读性）　② 功能正确性（逻辑准确性 / 边界处理 / 对既有功能影响）

---

## 一、更改范围概述

### 1.1 涉及文件（4 个）

| # | 文件 | 行数 | 变更性质 | 说明 |
|---|---|---|---|---|
| 1 | `work/test-accel/scripts/pull_code.py` | 584 | **新增**（迁入工程内唯一真源） | 核心实现，承载 P0/P1/P2 全部改造 |
| 2 | `work/test-accel/tests/test_pull_code.py` | 141 | **新增** | 12 条单元测试（脱敏 / 逃逸 / 变更集 / worktree / 失败态） |
| 3 | `~/.workbuddy/skills/qa-code-pull/pull_code.py` | 50 | **改写**（改为薄壳委托） | 仅定位并子进程调用工程内实现，`QA_PULL_CODE_IMPL` 可覆盖 |
| 4 | `~/.workbuddy/skills/qa-code-pull/SKILL.md` | — | **同步** | 默认值改 `HEAD`/`WORKTREE`、状态 schema 更新、实现位置更新 |

> `work/test-accel/scripts/` 与 `tests/` 整个目录均为新增（`git status` 显示为 `??`），故无 diff 基线，按**整文件**验证。

### 1.2 变更意图（对照上一轮评审 P0/P1/P2）

| 优先级 | 目标项 | 落地情况 |
|---|---|---|
| P0 | 跨平台杀进程树（原 `os.killpg` 在 Windows 失效） | ✅ 已实现 `_kill_proc_tree` |
| P0 | 默认 ref 改 `HEAD..WORKTREE`（避开已失效 ref） | ⚠️ 已改，但**默认路径解析错误**（见 P0-1） |
| P0 | 逃逸检查统一为唯一入口 | ⚠️ 已统一，但判定**语义过粗**（见 P1-1） |
| P1 | 枚举单源（PullStatus/PullMode） | ⚠️ 采用**本地枚举**，与平台枚举**不一致**（见 P1-3） |
| P1 | `success` 字段 + `--strict` 退出码 | ✅ 已实现并实测有效 |
| P1 | URL 脱敏增强 | ⚠️ 覆盖 userinfo，**query token 无 userinfo 时泄露**（见 P1-2） |
| P1 | 补单元测试 | ✅ 12 条，全通过 |
| P2 | 克隆残留清理 / 只读守卫 / 类型标注 / 文档同步 | 多数 ✅，少量遗留（见 P2/P3） |

---

## 二、问题清单（按严重程度分级）

### 🔴 严重（P0）— 1 项

#### P0-1　默认路径解析错误，默认运行开箱即不可用（并产生杂散目录）

- **位置**：`scripts/pull_code.py:60-62`
  ```python
  _THIS = Path(__file__).resolve()                 # …/work/test-accel/scripts/pull_code.py
  DEFAULT_LOCAL = str(_THIS.parent.parent / "targets" / "research-agent")
  #                    └ scripts ┘└─ test-accel ─┘
  ```
- **现象**：实际解析为 `work/**test-accel**/targets/research-agent`（不存在）；注释与设计意图是 `工程根/work/targets/research-agent`。**少了一级 `.parent`**（应为三级，或直接从工程根解析）。
- **实测证据**：
  - `python -c "...print(pc.DEFAULT_LOCAL)"` → `C:\...\work\test-accel\targets\research-agent`
  - `ls work/targets/` → 真实目标在 `work/targets/research-agent`
  - 无参运行 → `{"status": "clone_required_no_url", "success": false, "errors": ["仓库不存在且未提供 REPO_URL，无法克隆"]}`
  - 显式 `--path …/work/targets/research-agent` → `{"status": "ok", "success": true, "changed_count": 573}`
- **附加副作用**：失败路径仍会 `_dump()`，`Path(out_json).parent.mkdir(parents=True, exist_ok=True)` 在错误位置生成杂散目录 `work/test-accel/targets/.pull_result.json`。
- **定性**：这是「去掉硬编码绝对路径、改为相对解析」这一优化**引入的回归**——原技能使用绝对路径 `work/targets/research-agent` 时可用，改相对路径后默认入口失效。
- **影响面**：默认调用方（`python pull_code.py` 无参）全部失败；显式传 `--path`/`LOCAL_PATH` 时正常。

### 🟠 高（P1）— 3 项

#### P1-1　「既存普通目录」被误判为「逃逸父仓库」，克隆路径不可达

- **位置**：`scripts/pull_code.py:418-433`（阶段 0 阻断）
  ```python
  if os.path.isdir(local) and not _git_repo_usable(local):
      → status = repo_escape_blocked   # 一律按"逃逸到父仓库"处理
  ```
- **问题**：`_git_repo_usable()` 对「**不是 git 仓库**」与「**git 顶层逃逸到祖先仓库**」返回同一结果 `False`，而阶段 0 把两者合并为「逃逸」并直接阻断。
  - 后果 A：对**已存在的空目录 / 普通非 git 目录**，直接返回 `repo_escape_blocked`，报错文案「目录…的 git 顶层逃逸到父仓库」**与事实不符**（根本不存在父仓库）。
  - 后果 B：`clone()` 分支（`if not _git_repo_usable(local)`）**只对"目录不存在"可达**，**无法再克隆进已存在的目录**——相对原实现是行为收窄。
- **实测证据**：仓库外新建普通临时目录 → `{'status': 'repo_escape_blocked', 'success': False, 'errors': ['目录 C:\\...\\Temp\\tmp.XXXX 的 git 顶层逃逸到父仓库，…']}`
- **对照**：指向仓库子目录（如 `research-agent/backend`，git 顶层确为父级）→ `repo_escape_blocked`，**该场景判定正确**。
- **修复方向**：把判定拆成三态（目录缺失 → 克隆 / 存在但非 git → 克隆或明确报错 / git 顶层 ≠ 自身 → 阻断）。

#### P1-2　URL 脱敏对「无 userinfo + query token」失效，令牌可落盘泄露

- **位置**：`scripts/pull_code.py:75-94`
  ```python
  p = urlparse(url)
  if p.username or p.password:
      ... masked += "?..."      # 仅在"有 userinfo"时才剥离查询串
  return url                     # ← 无 userinfo 时原样返回
  ```
- **问题**：`https://host/path?token=SECRET`（无 `user:pass@`）**原样返回**，`SECRET` 会被写进 `.pull_result.json`。而 docstring 明确声称「覆盖 …以及 `?token=` 查询串」——**文档与实现不一致**。
- **测试反而固化了缺陷**：`tests/test_pull_code.py:38-40` 的 `test_mask_url_no_cred_passthrough` 断言「无凭据 URL 原样返回」，且**没有任何用例覆盖"无 userinfo 但带 query token"**。
- **修复方向**：无条件剥离 userinfo 与查询串（不依赖 userinfo 是否存在）。

#### P1-3　「枚举单源」未真正统一，两处 PullStatus 成员不一致

- **位置**：脚本 `scripts/pull_code.py:40-49`（本地 7 值枚举）vs 平台 `backend/core/enums.py:130-134`
  ```
  平台 PullStatus : ['ok', 'repo_escape_blocked']                      ← 仅 2 个
  脚本 PullStatus : ['ok','ok_offline','worktree_mode','clone_failed',
                     'refs_unavailable','repo_escape_blocked','clone_required_no_url']  ← 7 个
  → 二值一致性：**False**
  ```
- **问题**：P1 目标为「消除魔法串漂移」，实际落成「工程内两份定义 + 需手工同步」。脚本可返回的 `ok_offline / worktree_mode / clone_failed / refs_unavailable / clone_required_no_url` 在平台枚举中**均不存在**；下游（服务化接收入库 / testhub 消费状态）若按平台枚举映射将**缺成员**。
- **修复方向**：把平台 `PullStatus` 补全为 7 值，脚本改为引用平台枚举（或以脚本为单一来源反向收敛平台定义）。

### 🟡 中（P2）— 6 项

| 编号 | 问题 | 证据 | 影响 |
|---|---|---|---|
| **P2-1** | 迁入工程后**仅被 ruff 覆盖**，mypy / bandit 未纳入 | 门禁目标为 `mypy backend`、`bandit -r backend -x …`，均不含 `scripts/`；单独对脚本跑 mypy → **4 errors**（`os.getpgid`/`signal.SIGKILL` 属 POSIX 专用符号；`result["errors"]` 因字典值类型退化为 `object` 导致 `.append` 报错，L423/L440） | 「纳入统一门禁」仅部分兑现；脚本存在未被拦截的类型问题 |
| **P2-2** | 新增测试文件**不在 lint 覆盖内**，且自身有 ruff 告警 | 门禁 ruff 只扫 `backend scripts`；`ruff check tests` → 5 errors，其中本变更引入 `test_pull_code.py:2:1 I001`、`:129:12 PLR1714`（另有既有 `test_pure.py` I001/F401、`test_smoke.py` SIM115） | 测试代码质量无护栏 |
| **P2-3** | `pull()` 复杂度超阈值，靠豁免掩盖 | 隔离配置下触发 `PLR0912`（分支过多）+ `PLR0915`（语句 63 > 50，`scripts/pull_code.py:394`）；现由 `per-file-ignores` 的 `C90/PLR` 豁免 | 属"债务豁免"而非达标，后续维护成本高 |
| **P2-4** | 两处 `_git_repo_usable` 语义不一致 | 脚本版 `top == target or top.startswith(target+sep)`（宽松）；`code_analyzer.py:907-920` 版**严格相等**（`Path(top).resolve()==Path(repo_path).resolve()`） | 同一安全网两套判定；脚本宽松分支近乎死代码，可能放行 code_analyzer 会拦截的输入 |
| **P2-5** | 薄壳仍硬编码个人绝对路径 | `~/.workbuddy/skills/qa-code-pull/pull_code.py:21` 写死 `C:/Users/EDY/…`（有 `QA_PULL_CODE_IMPL` 兜底） | P0「去硬编码」在主实现已修，**薄壳遗漏**，跨机/换布局需手配环境变量 |
| **P2-6** | 只读副作用强、耗时明显 | 成功拉取会把整个目标仓库（含 `.git`，1.2k+ 跟踪文件 / 1.1w 文件）递归置只读；worktree 模式单次实测 **14.3s**，主要耗在两次 `os.walk` chmod；只读状态持续存在 | 可能影响后续需要写仓库的操作；大仓库耗时随文件数线性增长 |

### 🟢 低（P3）— 6 项

| 编号 | 问题 | 位置 / 说明 |
|---|---|---|
| **P3-1** | 死代码 `is_git_repo`：定义后**无人调用**（仅 docstring 提及） | `pull_code.py:165` |
| **P3-2** | `changed_vs_worktree` 对**重命名**处理不当：`status --porcelain` 的 `R  old -> new` 被 `ln[3:]` 取成 `"old -> new"` 整串 | `pull_code.py:363-372` |
| **P3-3** | `--target HEAD` 语义歧义：`_is_worktree_mode` 把 `""/WORKTREE/HEAD` 一律当工作树模式，显式传合法 ref `HEAD` 会被当 worktree，`target_sha` 恒空 | `pull_code.py:389-390` |
| **P3-4** | 凭据经 argv 传入 git：`clone` 把含 token 的 URL 放进命令行，POSIX 下 `ps` 可见（评审 safety#4 **未闭环**） | `pull_code.py:277-283` |
| **P3-5** | `_local_git` 命名与用途不符：docstring 称「本地**只读** git」，却被 `checkout -f`（写操作）复用 | `pull_code.py:127-128 / 343` |
| **P3-6** | `_net_git` 把 `errors="ignore"` 传给 `stdout=DEVNULL` 的 Popen，参数无意义 | `pull_code.py:146-152` |

---

## 三、代码规范符合性评估

| 维度 | 评估 | 依据 |
|---|---|---|
| **命名约定** | ✅ 符合 | 模块常量 `UPPER_SNAKE`（`DEFAULT_LOCAL`/`NETWORK_TIMEOUT`）、私有函数 `_` 前缀、枚举类 `PullStatus`/`PullMode` 规范、闭包 `_one`/`_chmod` 语义清晰 |
| **代码风格** | ✅ 符合 | `ruff check scripts` → `All checks passed!`；`ruff format scripts --check` → `7 files already formatted`；`from __future__ import annotations` 齐备；模块 docstring 完整（职责/设计原则/契约三段） |
| **复杂度** | ⚠️ 部分不符合 | `pull()` 超阈值（`PLR0912` + `PLR0915` 语句 63>50），当前依赖 `per-file-ignores` 豁免；其余函数粒度合理（`_mask_url`/`_kill_proc_tree`/`clone` 等单一职责） |
| **可读性** | 🟡 良好但有噪声 | 阶段化注释（阶段 0/0b/1/2~4/5）清晰、docstring 覆盖好；扣分项：`pull()` 单函数 ~125 行、`result` 用裸 dict 字面量导致类型退化、两处 `_git_repo_usable` 重复实现 |
| **类型标注** | 🟡 基本达标 | 主要函数已标注（`_local_git -> tuple[int,str]`、`pull -> dict` 等）；`errors: list` 未参数化、`result` 未用 `TypedDict` → 若纳入 mypy 将报 4 错（见 P2-1） |
| **枚举 / 数据契约** | ❌ 不符合（E1 枚举单源） | 本地 `PullStatus` 与平台 `PullStatus` 成员不一致（见 P1-3） |
| **安全** | 🟡 部分不符合 | 无 bandit 高危；但存在 URL 脱敏缺口（P1-2）、argv 凭据暴露（P3-4） |
| **测试层** | 🟡 部分符合 | 新增 12 条用例且全通过；但未覆盖默认路径/既存非 git 目录/query-token 等关键路径，且测试文件本身不在 lint 覆盖内（P2-2） |

**规范结论**：**风格与命名层面达标，结构与治理层面存在明确缺口**——复杂度靠豁免、枚举单源未达成、门禁覆盖不完整（脚本缺 mypy/bandit、测试缺 lint）。

---

## 四、功能验证结论与总体建议

### 4.1 逻辑准确性

| 项 | 结论 | 证据 |
|---|---|---|
| 主路径（显式 `--path` + worktree 模式） | ✅ 正确 | `status=ok / success=true / mode=worktree / base_sha=93aa0d8 / changed_count=573` |
| 变更集计算与原生 git 一致性 | ✅ 一致 | `git diff --name-only HEAD`=572 + 未跟踪 1 = **573**，与工具输出吻合 |
| 默认路径（无参） | ❌ 错误 | 返回 `clone_required_no_url`（见 P0-1） |
| 逃逸阻断（子目录场景） | ✅ 正确 | 指向 `research-agent/backend` → `repo_escape_blocked` |
| `--strict` 退出码 | ✅ 正确 | 默认 `RC=0`；`--strict` 且 `success=false` → `RC=1` |
| 单元测试 | ✅ 12/12 通过 | `pytest tests/test_pull_code.py -v` → `12 passed in 0.07s` |
| 全量门禁 | ✅ 8 环全绿 | `gate_all.py` → `GATE_EXIT=0`（但注意：绿是**豁免后**的绿，见 P2-1/P2-3） |

### 4.2 边界情况处理

- **已正确覆盖**：refs 不可解析（离线）→ `refs_unavailable`；无 URL 且目录缺失 → `clone_required_no_url`；git 顶层逃逸 → `repo_escape_blocked`；克隆超时 → 杀进程树 + 清理残缺目录。
- **未覆盖 / 处理有误**：既存普通非 git 目录被误拦（P1-1）；无 userinfo 的 query token 未脱敏（P1-2）；重命名路径畸形（P3-2）；`--target HEAD` 语义歧义（P3-3）。

### 4.3 对既有功能的影响

- **仓库内零耦合**：`grep` 全仓 `backend/` + `scripts/`，**无任何模块 import `pull_code` 或读取 `.pull_result.json`** → 对平台既有代码**零影响**；真正的消费方是仓库外的 `qa-test-points` 技能（经文件契约）。
- **对使用方的影响集中在默认行为**：
  1. 默认路径已坏（P0-1）——**开箱即用性回归**；
  2. 默认 `HEAD..WORKTREE` 在「工作树与 HEAD 不一致」的环境下会把变更集显著放大（本环境实测 573）——经核查，被测仓库 worktree **确实缺失 572 个已跟踪文件**（环境性异常，非本次改动所致），叠加默认模式后使「更新」标签覆盖面近乎全量，**削弱增量聚焦**。
- **副作用**：成功拉取会把目标仓库整体置只读（设计行为，实测生效：`attrib` 显示 `R`）；本次验证已在结束后清理掉自行产生的临时产物（`work/targets/_verify_out.json`、杂散 `work/test-accel/targets/`）。

### 4.4 总体建议（仅建议，未改动代码）

1. **先修 P0-1 + P1-1**：前者决定「开箱可用性」，后者决定「克隆路径完整性」，二者是本次变更最直接的功能缺口。P0-1 只需补一级 `.parent`（或将默认路径锚定到工程根）。
2. **P1-2 / P1-3 一并处理**：`_mask_url` 改为无条件剥离 userinfo + 查询串；`PullStatus` 收敛为单一来源（建议把平台枚举补全为 7 值，脚本引用之），消除双份定义漂移。
3. **补齐门禁覆盖**：把 `scripts/` 纳入 mypy 与 bandit（或在 `gate_all.py` 增脚本专项环节）；把 `tests/` 纳入 ruff 并 `--fix` 清 5 处告警。这样「迁移入工程」的治理收益才能落实。
4. **消债**：删除死代码 `is_git_repo`；统一两处 `_git_repo_usable` 语义；拆分 `pull()`；为 `result` 建 `TypedDict`；薄壳去硬编码。
5. **环境与副作用**：确认 `work/targets/research-agent` 的 worktree 与其 HEAD 是否应保持一致（当前缺 572 文件）；只读副作用建议增加开关，或收窄为「仅保护源码目录、不动 `.git`」，并评估大仓库下的 chmod 耗时。

### 4.5 结论

本次「代码获取」变更**在风格/命名/单测层面质量良好，主路径与 `--strict` 语义正确，且与平台既有代码零耦合（无回归风险）**；但存在 **1 项严重（默认路径错误导致开箱不可用）** 与 **3 项高优先级（既存非 git 目录误拦、query token 脱敏缺口、枚举单源未达成）** 问题，以及门禁覆盖不完整（脚本缺 mypy/bandit、测试缺 lint）的治理缺口。**建议按 P0 → P1 → P2 顺序修复后再视作阶段 1 闭环。**

---

## 附：本次验证所用证据（命令与实测摘要）

```text
# 1) 默认路径（错误）
python -c "…print(pc.DEFAULT_LOCAL)"  → …\work\test-accel\targets\research-agent
无参运行                               → status=clone_required_no_url, success=false

# 2) 正确路径（可用）
python scripts/pull_code.py --path …/work/targets/research-agent
  → status=ok, success=true, mode=worktree, base_sha=93aa0d8, changed_count=573, real 14.3s

# 3) 变更集与原生 git 一致性
git -C <target> diff --name-only HEAD | wc -l  → 572
git -C <target> status --porcelain   | wc -l  → 573

# 4) 边界
指向 <target>/backend                          → repo_escape_blocked（正确）
指向仓库外普通临时目录 + --url                  → repo_escape_blocked（错误，应为可克隆/明确报错）

# 5) 严格退出码
python scripts/pull_code.py --strict           → RC=1（success=false 时）
python scripts/pull_code.py                    → RC=0（默认恒 0）

# 6) 单元测试
pytest tests/test_pull_code.py -v              → 12 passed

# 7) 门禁
python scripts/gate_all.py                     → GATE_EXIT=0（8 环）
ruff check scripts                             → All checks passed!
ruff format scripts --check                    → 7 files already formatted
ruff check tests                               → 5 errors（pull_code 测试占 2 条：I001 / PLR1714）
ruff check --isolated --select C90,PLR0912,PLR0915 scripts/pull_code.py → 4 errors（pull() 复杂度）
mypy scripts/pull_code.py                      → 4 errors（scripts 不在门禁 mypy 目标内）
bandit -c pyproject.toml -r scripts -ll         → 0 issues（scripts 不在门禁 bandit 目标内）

# 8) 枚举一致性
平台 PullStatus = ['ok','repo_escape_blocked']；脚本 PullStatus = 7 值 → 不一致

# 9) 引用面
grep -rn "pull_code|pull_result" backend scripts（排除自身）→ 无引用（零耦合）
```
