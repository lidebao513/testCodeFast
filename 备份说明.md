# 备份说明（P0 · 基线冻结）

> 冻结时间：2026-09-11　冻结对象：`work/test-accel`（阶段 1–2 已交付实现）与 `~/.workbuddy/skills/qa-*`
> 目的：在启动「代码 → 测试用例」重新开发之前，把当前可用状态做成**不可变基线**，任何时刻可整体回退。

---

## 一、三层备份（代码 / 数据 / 样本）

| 层 | 内容 | 位置 | 是否入库 | 恢复方式 |
|---|---|---|---|---|
| **代码层** | 冻结基线提交 | Git 标签 `legacy-pipeline-v1` | ✅ 是 | `git checkout legacy-pipeline-v1` |
| **代码层** | 冻结分支 | `legacy-phase2-3-baseline` | ✅ 是 | `git checkout legacy-phase2-3-baseline` |
| **数据层** | 平台数据库快照 | `backup/db/test_accel_20260911.db` | ❌ 否（`.gitignore` 已排除 `backup/db/`） | 覆盖回 `work/test-accel/data/test_accel.db` |
| **样本层** | 黄金样本（TP / Case / FP JSON） | `backup/golden/20260911/` | ✅ 是 | 作为新旧实现"影子双跑"比对的固定标尺 |
| **资产层** | `qa-*` 技能快照 | `backup/skills/qa-skills-20260911.tar.gz` | ✅ 是 | `tar -xzf` 解包后拷回 `~/.workbuddy/skills/` |

> **分支名说明**：方案原文为 `legacy/phase2-3-baseline`（带斜杠）。本机沙箱环境对 `.git/refs/heads/` 下**新建子目录**会静默回滚，导致带斜杠的分支名无法创建，故改用等价的平铺名 `legacy-phase2-3-baseline`。功能完全一致。

---

## 二、校验信息（可核对）

| 项 | 值 |
|---|---|
| 基线提交 | 即本文件所在提交；查询：`git rev-parse legacy-pipeline-v1^{commit}` |
| 标签（annotated） | `legacy-pipeline-v1`（与 `legacy-phase2-3-baseline` 分支同点） |
| 数据库 SHA-256 | `1c30915b5bc5c114dbe0d5bd81f4f762f9f2953ae32bb82c366cf9f186452501` |
| 数据库大小 | 2,527,232 字节（≈2.5 MB） |
| `PRAGMA integrity_check` | `ok` |
| 导出时间 | 2026-09-11T14:38:39 |

### 数据量（快照时刻）

| 表 | 全局 | 其中 project 2（research-agent） |
|---|---|---|
| projects | 5 | — |
| functional_points | 237 | 237 |
| test_points | 415 | 415 |
| cases | 540 | 416 |
| runs | 2010 | — |
| reports | 38 | — |

> 其余项目为测试脚手架：`b43-mock`(2 用例)、`b44-browser`(2)、`b5-progress`(60)、`b5-webhook`(60)。

---

## 三、黄金样本目录结构

```
backup/golden/20260911/
├── MANIFEST.json                    # 清单：来源库 sha256、各项目条数、文件索引
├── project_2/{functional_points,test_points,cases}.json
├── project_3/…  project_4/…  project_5/…  project_6/…
```

**重新导出**（只读操作，不会改动源库）：

```bash
cd work/test-accel
python scripts/export_golden.py                    # 导出全部项目
python scripts/export_golden.py --project 2        # 只导 research-agent
python scripts/export_golden.py --date 20260920    # 指定批次目录名
```

导出工具：`work/test-accel/scripts/export_golden.py`（以 `mode=ro` 只读打开数据库）。

---

## 四、应急回退手册

**场景 1：新服务做坏了，想完全退回旧状态**
```bash
git checkout legacy-phase2-3-baseline      # 切回冻结代码
cp backup/db/test_accel_20260911.db work/test-accel/data/test_accel.db   # 恢复数据
```

**场景 2：只想看旧代码，不想切分支**
```bash
git worktree add ../legacy-view legacy-pipeline-v1
```

**场景 3：数据库被污染，但代码没问题**
```bash
cp backup/db/test_accel_20260911.db work/test-accel/data/test_accel.db
```

**场景 4：技能被改乱**
```bash
tar -xzf backup/skills/qa-skills-20260911.tar.gz -C ~/.workbuddy/skills/
```

---

## 五、注意事项

1. **DB 快照不入库**：`.db` 为二进制且可由快照重建，`.gitignore` 已排除 `backup/db/`。若换机器，需手动拷贝该文件。
2. **基线只读**：`legacy` 分支与标签**不再接受功能改动**；如需修补旧链路 bug，另开 `hotfix/*` 分支，勿直接改基线。
3. **黄金样本会随开发迭代新增**：每次重新导出请用新的 `--date` 目录，**不要覆盖**既有样本（覆盖会失去比对基准）。
4. **本机备份不等于异地备份**：`backup/` 与仓库同盘。若需抗磁盘故障，另行同步到外部存储。

---

*本文件由 P0 阶段生成，随 `legacy-pipeline-v1` 基线一并冻结。*
