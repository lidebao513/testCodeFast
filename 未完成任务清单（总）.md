# 未完成任务清单（总）

> 汇总 test-accel 平台阶段 1（取码）→ 阶段 2（测试点）→ 阶段 3（用例生成）→ 阶段 4（测试执行）的剩余事项，
> 并逐一标注**阻断性**。
> - 阶段 1/2/3：**全部剩余项均为非阻断**，所有「必须项（★）」已闭环。
> - **阶段 4（2026-09-09 评审 + 批次1~5 全部实施）**：5 个批次已落地，必须项 6 项（D-★1~★6）**全部闭环**，
>   应做项 D-②1/②2/②3/②6/②8 已完成（②3 = P5-②1 status 语义清理，阶段5 批次1 落地）；完整度由约 **30% 提升至约 88%**。
>   仅剩应做 D-②4/②5/②7/②9 与可选 D-③1~③3/③5 待办（均非阻断；D-③4 异步化+webhook 已在阶段5 批次1 落地 = P5-★4）。
> 端到端自测 `selftest_e2e.py` **31/31 通过**（2026-09-09，含全量/新增 test_type 断言）。
> - **阶段 5（执行监控与记录，2026-09-09 评审 + 批次1~2 实施）**：批次1（P5-★4 异步任务化 + webhook 回调、P5-②1 cases.status 语义清理）**已完成并自测 11/11**；
>   批次2（P5-②5 进度持久化 `run_batches` + 重启恢复 `recover_interrupted` + C-②8 用例状态增量对账）**已完成并自测 8/8**；
>   三根支柱（实时进度 P5-★1 / 可中断 P5-★2 / 历史回溯 P5-★3）此前已闭环，现对外闭环（P5-★4 回调）+ 持久化（P5-②5）均已具备；完整度约 **75%**。
>   剩余应做 P5-②2/②3/②4/②6 与可选 P5-③1~③4（均非阻断；P5-③4 即既有 C-②2）。
> - **阶段 6（报告生成，2026-09-09 评审 + 批次1~4 实施）**：基线可用（单批次 JSON+HTML、KPI、按类型、明细表、清册卡片、`/reports` 列表）；**批次1（P6-★1~★4 强制项）已全部闭环**；**批次2（P6-②1 趋势/flaky + P6-②2 覆盖率）已完成**；**批次3（P6-②3 导出 PDF/Word/Excel + P6-④ 执行摘要）已完成**；**批次4（P6-②5 章节化全功能验证报告 + P6-②6 变更摘要/绑定 commit）已完成**，完整度约 **98%**。
>   报告明细现双向追溯（TP/FP/源码，覆盖率 100%）、失败/浏览器证据（截图链接+日志）已呈现、单条报告详情端点（`/reports/{rid}` + `/html`）已就绪、失败归因聚合（按模块/状态聚类）已落地；趋势/flaky（基于 run_batches+runs）与三级覆盖率报告（FP/TP/Case）现已具备。**章节化全功能验证报告（历史风格 9 章节，VERIFICATION_REPORT.md/.html，截图 base64 内嵌）已由 `full_report.py` + `report_templates/verification_report.html` 落地**，端点 `GET /reports/full`。详见 `阶段6评审与未完成任务清单.md`。

---

## 零、新服务 testgen-service（当前主线 · 2026-09-14 核验）

> 阶段 1~6 的清单针对旧平台 `work/test-accel`（已封板）。当前开发主线已迁到**独立新服务
> `work/testgen-service`**（六层：`core` / `engine` / `service` / `cli` / `workspace` / `output`），
> 契约与门禁**只有一份**，不再与旧平台分叉。本节汇总其**当前功能**、**未完成项**与**核心待决策项**。

### 0.1 当前功能总览（已实现）

| 能力块 | 状态 | 落点 |
|---|---|---|
| 代码扫描 / 全量·增量流水线 | ✅ | `engine/scan.py`、`engine/diff_tag.py`、`engine/pipeline.py` |
| 功能点提取（接口 / 页面 / 组件 / 业务） | ✅ | `engine/fp_extract.py`（含 P1 收窄） |
| 测试点展开（正常 / 异常 / 安全 / 边界） | ✅ | `engine/tp_expand.py`（展开规则对齐表已被测试锁死） |
| 语义增强（LLM，可选） | ✅ | `engine/semantic_enrich.py`（默认关闭，规则引擎可独立跑通） |
| 用例生成（八要素 + UI 优先排序） | ✅ | `engine/case_gen.py` |
| 幂等落库与用例对账 | ✅ | `core/store.py`（三表唯一索引防撞号） |
| P1 业务功能点收窄 + UI 优先 | ✅ | `BUSINESS_EXTRACT_MODE=strict`（提交 `c55d898`） |
| P2 PRD / OpenAPI 真实解析 + 安全默认纳入 | ✅ | `engine/prd_ingest.py`（提交 `0ca1a6f`） |
| **P3 运行时 UI 发现（M3.1–M3.4）** | ✅ | `engine/runtime_ui.py` + `pipeline._merge_runtime_fps` |
| 八环质量门禁 + pre-commit / commit-msg 钩子 | ✅ | `scripts/gate_all.py`（覆盖率约 87%） |

### 0.2 P3 真实环境实测证据（被测：`http://47.97.154.50:8090/chat` 福享 Agent）

| 指标 | 实测结果 |
|---|---|
| 登录 | ✅ 「邮箱账号 + 密码 + **动态口令**」三字段表单登录成功 |
| 菜单点击发现路由 | **15 条**：`/pc/qixinji`、`/pc/discover`、`/pc/exec-hall`、`/pc/email`、`/pc/schedule`、`/pc/knowledge`、`/pc/memory`、`/pc/experts`、`/pc/tasks`、`/pc/artifacts`、`/pc/daily-perf`、`/pc/audit`、`/pc/skills`、`/pc/connectors`、`/pc/wecom` |
| 可达页面 | **16 / 16** |
| 抓取元素 | 659（工具直跑）；2498（并入 pipeline 全量展平） |
| 控制台错误 | **0** |
| 凭证泄露 | 无（密码与动态口令不出现在结果/日志/产物） |
| M3.4 并入主链路 | ✅ `runtime_ui_fp_added=32` → 功能点 **768** → 测试点 **1200** → 用例 **1200**（`tp_expand fp=768` 证明确实吃到了 UI 功能点） |

> 本次修掉的两个真缺陷（都属「静默 0 条」型）：① 逐项 `goto` 重开页面导致 SPA 菜单未挂载即取句柄 → 空；
> ② 等导航渲染预算过紧（8s）而首批菜单要等接口返回。已改为**单会话点击 + 按文本重定位**，并放宽预算。
> 另修掉「首页下载链接被当成页面」的假功能点（按静态资源扩展名过滤）。

### 0.3 未完成清单（testgen-service）

| 编号 | 事项 | 阻断性 | 说明 |
|---|---|---|---|
| **T-★1** | **测试执行器 `executor.execute_case` 仍是桩** | 🔴 **核心** | 平台目前**只生成用例、不执行**：`stage_execute` 返回 `None`。用例跑不了 → 无法产出执行结论/报告，是「文档生成器 → 验证服务」的关键缺口 |
| T-★2 | 服务化对外（异步化 / webhook / 鉴权 / 监控） | 🟠 高 | 要接入「testhub 远程调用」与「发布流水线部署后自动验证」两条链路的前提 |
| T-②1 | `engine/llm_design.py::design_cases` 仍是桩 | 🟡 中 | P2 遗留：LLM 用例设计未实现，用例业务语义仍靠规则模板 |
| T-②2 | 安全测试深化 | 🟡 中 | 安全维度只覆盖接口层、期望值是通用模板、未识别资源归属 → **鉴权/越权结论不可信**（易误报/漏报） |
| T-②3 | P3-M3.5 `requests` 降级路径未做 | 🟡 中 | 未装 Playwright 时只能「给安装指引并跳过」，无法做轻量 UI 发现 |
| T-③1 | P3 UI 元素未反向补「交互断言细节」 | ⚪ 低 | 目前 UI 测试点只到「页面可达 / 可交互元素可用」粒度 |
| T-③2 | 覆盖率门槛仍 5% | ⚪ 低 | 实测约 87%，门槛待分阶段抬高 |
| T-③3 | 旧平台 `DEFAULT_SCOPE` 分叉 | ⚪ 低 | `work/test-accel/backend/core/enums.py` 与 testgen-service 取值不一致 |
| T-③4 | 本地 main 领先 origin/main 未推送 | ⚪ 低 | 仓库卫生 |

### 0.4 核心需要完成的（请你拍板是否做、按什么顺序）

| 序号 | 事项 | 为什么核心 | 不做会怎样 |
|---|---|---|---|
| **A** | **测试执行（executor）**：把生成的用例真跑起来（接口 HTTP 判定 / UI 浏览器判定），出执行结果 + 报告 + 落库 | 决定平台是「用例文档生成器」还是「能给出结论的验证服务」 | 用例永远只是纸面产物，无法回答「这次改动到底有没有问题」 |
| **B** | **服务化对外**：独立服务 + 异步任务 + webhook 回调 + 鉴权 + 健康检查/监控 | 决定能否被 testhub 调用、能否挂到发布流水线自动跑 | 只能人工在本机命令行跑，无法进流水线 |
| **C** | **LLM 用例设计**：让用例能读懂 PRD/需求，产出业务语义更强的步骤与断言 | 决定用例「像不像人写的」，直接影响可用性 | 用例均为模板化文案，业务人员不爱看、难评审 |
| **D** | **安全测试深化**：真实鉴权态判定 + 资源归属识别（越权） | 决定安全维度的结论是否可信 | 安全用例给的是「通用模板式」结果，可能误报，反而干扰判断 |

---

## 一、阻断性总判断（以下为旧平台 test-accel，阶段 1~6）

| 维度 | 结论 | 依据 |
|---|---|---|
| 阶段1→2 衔接 | ❌ 不阻断 | 阶段2 JSON 契约（`test_points_new.json`）已打通到阶段3 `import_test_points` |
| 阶段2→3 衔接 | ❌ 不阻断 | C-★1/C-★3 已完成：TP→Case 1:1，稳定 ID，零孤儿 |
| 阶段3 内部完整度 | ❌ 不阻断 | C-★2/C-★4（8 要素 + 交付物）、C-★5（评审闭环）、C-②1（软删）已完成 |
| 口径一致性（C-★6） | ❌ 已修复 | `_analyze_project` 改用综合构建器，237 FP 行级覆盖 263 TP（unresolved=0） |
| 阶段4 执行（流程跑通） | ❌ 不阻断 | `scan_and_test` 可跑完 263 条并出报告，e2e 31/31 |
| **阶段4 执行（结论可信度）** | ✅ **已修复（批次2）** | D-★1/D-★2 已完成：服务可达时以真实 HTTP 响应为主判定并按 expect 校验（含异常反向断言）；通过率现可真实反映被测系统状态（受保护接口仍返回 `blocked_auth` 不计通过，需待 D-②1 鉴权态落地后才覆盖） |

---

## 二、阶段 1（取码）剩余

| 编号 | 事项 | 阻断性 | 说明 |
|---|---|---|---|
| S1-★5 | 真·远程基线恢复（`base..target` 双版本 diff，需 codeup 凭据） | ❌ | 目标仓库已就地 `git init`（`main`/`34f105b`）；`WORKTREE` 增量 + 全量降级可用；`changed_files=0` 时全打「全量」标签，结论正确仅聚焦度下降 |
| S1-③3 | `work/research-agent-test/` 是否纳入版控（含截图/账号信息） | ❌ | 仓库卫生问题 |

## 三、阶段 2（测试点）剩余

| 编号 | 事项 | 阻断性 | 说明 |
|---|---|---|---|
| A5 | diff 三向分类过粗（只影响「变更功能点」膨胀，不影响用例能否生成） | ❌ | 增强项 |

## 四、阶段 3（用例生成）剩余

| 编号 | 事项 | 阻断性 | 说明 |
|---|---|---|---|
| C-②2 | 24 个手写业务场景（`exec_live.py`）未沉淀入库/技能，与 fp_id/tp_id 零关联 | ❌ | 资产化增强 |
| C-②3 | mock-server 与真实路由重复生成测试点 | ❌ | 去重优化 |
| C-②4 | 无优先级冒烟子集（不能一键只跑 P1） | ❌ | 增强 |
| C-②5 | 前端断言仅"文本包含" | ❌ | 断言精度增强 |
| ~~C-②6~~ | ~~`{account_id}` 类占位符未参数化~~ | ✅ **已解决**（阶段4 批次2） | `executor._fill_path()` 按场景填充：正常填 `1`、异常填 `nonexistent` 以触发拒绝；动态探测不再必然 404 |
| C-②7 | 路径分隔符统一 | ⚠️ 已部分修复 | `code_analyzer._iter_code` 与 `case_generator._build_from_tp` 已做正斜杠归一并验证 |
| ~~**C-②8**~~ | ~~用例状态与代码变更一致性对账~~ | ✅ **已完成**（2026-09-09，阶段5 批次2） | `case_generator.generate_for_project` 重写为键(tp_id / (fp_contract_id,title))对账：功能点不再产出→`obsolete`（替代原 `archived`，保留可见且不进执行/交付物）；内容变化→`updated`；未变→`reused`(保持 `generated`)。首轮全 created；删1改1演示→created=0/updated=1/reused=413/obsolete=1/active=414。executor 排除 obsolete、export 排除 obsolete |
| 可选 | 脚本骨架产出 / LLM 增强可选开关 / 阶段3 技能化 `qa-test-cases` / TAPD·Jira 导出 | ❌ | 可选 |

## 五、阶段 4（测试执行）剩余

> 2026-09-09 新增评审，原完整度约 **30%**。详见 `阶段4评审与未完成任务清单.md`。
> 原核心问题：执行器判定基于**源码静态文本匹配**，真实 HTTP 结果只写日志、异常/预期从不校验。
> **批次1~4 已全部实施**：异常防护 + 批次（批次1）、动态判定 + expect 反向断言（批次2）、筛选/鉴权/并发（批次3）、真实浏览器 + 凭据外置（批次4）→ 完整度升至约 **80%**。

| 编号 | 事项 | 阻断性 | 说明 |
|---|---|---|---|
| ~~**D-★1**~~ | ~~动态执行结果不参与判定（假阳性）~~ | ✅ **已完成**（批次2，2026-09-09） | 服务可达时以真实 HTTP 响应为主判定并按 expect 校验；401/403→`blocked_auth`；不可达→`structural_only`（静态命中不计通过）。静态兜底 `path in text` 已收紧 |
| ~~**D-★2**~~ | ~~`expect` / 异常维度完全未校验~~ | ✅ **已完成**（批次2，2026-09-09） | 新增 `_expected()` 从自然语言解析期望码（`→ 404`/`（2xx）`/语义兜底）+ `_check_expect()`；异常维度走**反向断言**；占位符按场景填 `1`/`nonexistent`（顺带前置解决 `C-②6`） |
| ~~**D-★3**~~ | ~~平台内无真实浏览器执行与截图~~ | ✅ **已完成**（批次4，2026-09-09） | 新增 `browser.py` 封装 Playwright（系统 Chrome 回退，沙箱自带 chromium 挂死已绕过）；page 类用例真实渲染断言 + 失败自动截图写入 `runs.screenshot_path`（死字段首次被写入）；未安装则降级为静态断言 |
| ~~**D-★4**~~ | ~~执行无异常防护，单条异常中断整轮~~ | ✅ **已完成**（批次1，2026-09-09） | 单条 try/except 隔离为 `error` + 分批 commit + `finally` 保底；自测验证崩溃前 99 条仍落库 |
| ~~**D-★5**~~ | ~~缺运行批次 `batch_id`~~ | ✅ **已完成**（批次1，2026-09-09） | `runs.batch_id` 幂等加列并贯穿执行/报告；新增 `/runs?batch_id=` 与 `/batches` 端点 |
| ~~**D-★6**~~ | ~~无执行子集筛选~~ | ✅ **已完成**（批次3，2026-09-09） | `run_project(pid, filters)` 支持 `priority/module/case_type/test_type/ctype/ids` 组合筛选；新增 `POST /execute` 与 `GET /execute/options`；`test_type=新增` 可直接筛选执行（闭环 `C-②4` 冒烟子集） |
| ~~D-②1~~ / ~~D-②2~~ / ~~D-②6~~ / ~~D-②8~~ | 鉴权态复用 / 并发执行 / 执行历史进度与中断 / exec_live 明文凭据 | ✅ **已完成**（批次3/4/5） | D-②1 抽 `auth.py` 携带 token + 401 自动重试；D-②2 线程池并发 + 顺序写库；D-②6 进度注册表 + `/execute/status`(实时+runs回退)/`/execute/cancel`/`/execute/active` + `async_mode` 后台执行；D-②8 凭据外置 `.env` 并脱敏 |
| ~~**D-②3**~~ | ~~status 语义混淆~~ | ✅ **已完成**（=P5-②1，阶段5 批次1，2026-09-09） | `cases.status` 仅承载生命周期状态（generated/updated/obsolete/archived），执行结论改写 `runs.status` + `cases.last_result`，不再污染 lifecycle 语义 |
| D-②4 / ②5 / ②7 / ②9 | 无超时重试分类 / 无 fixture / 只跑 steps[0] / 无 flaky 识别 | ❌ | 应做项，见评审文档第四节；均为增强，**非阻断** |
| ~~**D-③4**~~ / D-③1~③3 / ③5 | 24 场景资产化 / 沙箱清理 / 依赖排序 / **异步化+webhook(已完成)** / 趋势看板 | ❌ | D-③4（=P5-★4）已在阶段5 批次1 落地；其余可选，D-③1 与既有 `C-②2` 同源 |

## 六、阶段 6（报告生成）剩余

> 2026-09-09 评审 + 批次1 实施，基线完整度约 **40% → 70%**。详见 `阶段6评审与未完成任务清单.md`。
> **4 项强制缺口（P6-★1~★4）已于批次1 全部闭环**，报告达到可诊断、可检索、可信的生产口径。

| 编号 | 事项 | 阻断性 | 说明 |
|---|---|---|---|
| ~~**P6-★1**~~ | 报告明细双向追溯（run↔case↔TP↔FP） | ✅ **已完成（批次1）** | `_enrich_results()` JOIN cases+test_points 挂 `tp_id/fp_contract_id/module/title/source`；HTML 明细表新增追溯列；真实报告追溯覆盖 100% |
| ~~**P6-★2**~~ | 失败/浏览器用例证据呈现（截图+日志链接） | ✅ **已完成（批次1）** | HTML 渲染 `file://` 截图链接 + `<details>` 日志；来源 `runs.screenshot_path` |
| ~~**P6-★3**~~ | 单条报告详情端点 `GET /reports/{rid}` | ✅ **已完成（批次1）** | 新增 `/reports/{rid}`（`inline` 返回 HTML 全文）+ `/reports/{rid}/html`（`FileResponse`） |
| ~~**P6-★4**~~ | 失败归因聚合（按模块/错误类型聚类） | ✅ **已完成（批次1）** | `summary.failures`（by_status/by_module/clusters 含样例 case_id）+ `by_module` 通过率；HTML「失败归因」卡片 |
| ~~P6-②1~~ | 多批次对比/趋势报告（flaky 识别） | ✅ **已完成（批次2）** | `analytics.trend()` 基于 `run_batches` 出 pass_rate 趋势+方向；`analytics.flaky()` 跨批次识别不一致用例（区分经典 flaky 与仅环境态变化）；新增 `/reports/trend`、`/reports/flaky` 端点 + 合订 HTML |
| ~~P6-②2~~ | 覆盖率报告（用例 vs TP vs FP） | ✅ **已完成（批次2）** | `analytics.coverage()` FP/TP/Case 三级覆盖率 + 未覆盖缺口（FP/TP）定位 + 模块级拆分；新增 `/coverage` 端点 + 合订 HTML |
| ~~P6-②3~~ | 报告导出 PDF/Word/Excel | ✅ **已完成（批次3）** | 新增 `exporters.py`：`export_report(pid,rid,fmt)` 产出 `report_{pid}_{rid}.xlsx/.docx/.pdf`（PDF 用 reportlab 内置 STSong-Light 中日韩字体，中文不乱码）；端点 `GET /reports/{rid}/export?fmt=`；自测 `selftest_b6_3.py` 30/30 |
| ~~P6-④~~ | 执行摘要/一页结论 | ✅ **已完成（批次3）** | `reporter.build_exec_summary()` 生成一句话结论+关键指标+建议（动态判定：全静态/无结论/健康/存在问题 + 鉴权/异常/覆盖率/趋势/flaky 提示）；综合 HTML 顶部「执行摘要」卡片 + 端点 `/reports/{rid}/summary`、`/exec_summary`；`selftest_b6_3.py` 30/30 |
| 历史报告对比 | 与 9/6·9/7 历史测试报告对比内容 | ✅ 分析已完成（见对话） | 核查结论：9/6 报告存在（早期原始版，仅 summary+results 196 条）；9/7 无报告文件（仅失效 git ref）；当前报告为其**严格超集**；历史基线 ref 已删、仅冻结 HTML 为档案，**真正可复现的历史对比待 P6-②6 版本化落地** |
| P6-②5 | 富报告模板落地（build_rich 接入/废弃） | ❌ | 现有 `build_rich()` 是幽灵代码（scan_and_test 不调用、token 未接真实数据） |
| P6-②6 | 报告版本化/关联代码变更 | ❌ | 绑定 commit + 用例版本（C-②8 已产生版本语义）；**可使历史对比可复现** |
| P6-③1~③3 | 外部推送（TAPD/Jira/企微/邮件）/分享链接 | ❌ | 可选 |

## 七、平台级（服务化，独立于本任务）

- 11 个端点同步执行 → 需异步化 + webhook + 鉴权 + 监控（见 `测试加速平台_服务化改造分析.md`）；
- `executor` 现已接入真实浏览器（`browser.check_page`，系统 Chrome 回退），`D-★3` 已闭环；服务化异步化仍未做（见下）。

## 八、自测结果总览

| 自测脚本 | 范围 | 结果 |
|---|---|---|
| `work/test-accel/selftest_b1.py` | 批次1：TP 驱动 + 稳定 ID | ✅ PASSED（263/263，孤儿0，异常26） |
| `work/test-accel/selftest_b2.py` | 批次2：8 要素 + 交付物 | ✅ PASSED（263/263，8 要素齐全，reporter 联动） |
| `work/test-accel/selftest_e2e.py` | 阶段1→3 串联 | ✅ **31/31 PASSED**（2026-09-09，含 test_type 全量/新增 断言） |
| `work/test-accel/selftest_b4_1.py` | 阶段4 批次1：异常防护 + batch_id | ✅ **13/13 PASSED**（单条 error 不中断整轮；崩溃前 99 条仍落库；reports↔runs 批次一致） |
| `work/test-accel/selftest_b4_2.py` | 阶段4 批次2：动态判定 + expect 断言 | ✅ **15/15 PASSED**（mock 服务验证 200/500/401、异常反向断言、占位符填充；集成 **pass=0**，263 条虚假通过消除） |
| `work/test-accel/selftest_b4_3.py` | 阶段4 批次3：筛选 + 鉴权态 + 并发 | ✅ **14/14 PASSED**（按 priority/case_type/test_type/ids 筛选条数正确；401 自动登录重试；并发 vs 串行结果一致；`/execute/options` 返回 test_type 维度） |
| `work/test-accel/selftest_b4_4.py` | 阶段4 批次4：浏览器 + 截图 + 降级 + 凭据 | ✅ **14/14 PASSED**（真实渲染断言 + 失败截图落盘；playwright 缺失/挂死→优雅降级不抛异常；凭据外置后缺失不报错） |
| `work/test-accel/verify_chain.py` | 阶段4 全链路：取码→就位→依赖→配置→启动→执行→回收 | ✅ **PASS 8 / BLOCK 2 / FAIL 0**（复跑稳定，8.2s；BLOCK=无 remote 拉取 / 主后端无入口，均非缺陷） |
| `work/test-accel/selftest_b5_2.py` | 阶段5 批次1：P5-★4 webhook 回调 + P5-②1 status 语义清理 | ✅ **11/11 PASSED**（webhook state=done、取消后 cancelled 不丢、无 webhook 不崩、`cases.status` 执行后仍=generated 且 `last_result`=pass、`/execute/tasks` 可见） |
| `work/test-accel/selftest_b5_3.py` | 阶段5 批次2：P5-②5 进度持久化 + C-②8 增量对账 | ✅ **8/8 PASSED**（注册→进度→终态→DB 回退 source=run_batches；recover_interrupted 标 interrupted 且 webhook 收到；首轮 3 TP 全 generated；删1改1→1 reused/1 obsolete/1 updated） |
| `work/test-accel/run_full_flow.py` | 阶段0→5 完整流程（拉取→分析→测试点→用例含 C-②8→执行+监控） | ✅ **PASS 13 / BLOCK 0 / FAIL 0**（27.7s；含 webhook 回调、run_batches 持久化、/batches /execute/tasks 可读、reports 落库） |
| `work/test-accel/selftest_b6_1.py` | 阶段6 批次1：P6-★1~★4 | ✅ **14/14 PASSED**（追溯覆盖 100%；截图 file:// 链接渲染；`/reports/{rid}`+inline+/html+404 优雅；失败聚类含模块×状态与样例 case_id） |
| `work/test-accel/selftest_b6_2.py` | 阶段6 批次2：P6-②1 趋势/flaky + P6-②2 覆盖率 | ✅ **16/16 PASSED**（趋势 2 批次方向 improving；flaky 精确识别经典 1 条且排除 fail↔error 与 pass↔pass；覆盖率 FP/TP 各 80% 缺口定位 fp_E/TP-5；4 端点 + 合订 HTML 全绿） |
| `work/test-accel/selftest_b6_3.py` | 阶段6 批次3：P6-②3 导出 + P6-④ 执行摘要 | ✅ **30/30 PASSED**（exec_summary 结构化结论；xlsx/docx/pdf 三格式产出且 PDF 嵌入 STSong-Light 中日韩字体；端点 /exec_summary、/reports/{rid}/summary、/reports/{rid}/export 三格式 200 + content-type 正确；综合 HTML 含「执行摘要」卡片） |
| `work/test-accel/selftest_b6_4.py` | 阶段6 批次4：P6-②5 章节化全功能验证报告（6.1~6.4） | ✅ **19/19 PASSED**（聚合 9 类字段/清册排除 obsolete；HTML 无残留 token 且 9 章节齐全；MD 9 个二级章节；generate 落盘 VERIFICATION_REPORT.md/.html 且截图 base64 内嵌；端点 /reports/full 三格式 + 静态路由不被 {rid} 拦截） |
| `work/test-accel/backend/modules/full_report.py` | 章节化全功能验证报告模块（6.1 聚合 / 6.2 渲染 / 6.3 九章节 MD / 6.4 交付） | ✅ 已落地（数据全部来自平台 DB；产物 `data/reports/VERIFICATION_REPORT.md/.html`，research-agent 实测 415 用例 / 12 张截图内嵌 / 复用 97.1%） |
| `work/test-accel/make_sample_report.py` | 一键从真实项目产出最终 HTML 报告 | ✅ research-agent 全量 415 条 1.4s，追溯 100%、聚类 18 组，产物 `data/reports/scan_2_20260909_132054.html` |
| `work/test-accel/make_trend_coverage_report.py` | 一键产出 趋势+flaky+覆盖率 合订 HTML | ✅ research-agent 全量历史：22 批次、FP/TP 覆盖 100%、flaky 0，产物 `data/reports/trend_coverage_2_20260909_135524.html` |
| `TestClient` 集成 | 审核端点 / analyze 行为 | ✅ import/generate/review/bulk_review 均 200；analyze→237 FP（含 business 138） |

> 三个 selftest 开头均设「干净基线」（DELETE runs/cases/test_points/functional_points），
> 避免 B3.3 软删后反复运行累积归档用例污染计数。
