# P1-2 · 资产盘点表

> 盘点对象：`work/test-accel`（基线 `legacy-pipeline-v1` / `236f44c`）+ 用户级技能 `~/.workbuddy/skills/qa-*`
> 盘点口径：本次开发范围 = **代码获取 → 代码解析 → 测试点 → 用例生成**（P4 含全量/增量双通道）。
> 处置类别：**直接引用** / **移植** / **重写** / **废弃** / **不迁移（范围外，留 legacy）**

---

## 一、总览

| 处置 | 数量 | 说明 |
|---|---|---|
| 直接引用 | 15 | 原样搬入，几无改动 |
| 移植 | 11 | 逻辑保留，改分层/改接口 |
| 重写 | 1 | 上帝模块拆分 |
| 废弃 | 5 | 使命结束或被新 CLI 取代 |
| 不迁移 | 45+ | 执行/报告/前端等本次范围外 |
| **代码总量** | 18,160 行 | 本次实际搬运约 **5,000 行量级** |

---

## 二、后端 `backend/`（21 个文件）

| 文件 | 行数 | 阶段 | 处置 | 去向 / 说明 |
|---|---|---|---|---|
| `core/enums.py` | 225 | 全局 | **直接引用** | → 新服务 `core/enums.py`，仍为**枚举唯一真值源**；由 `check_enums` 反查孤儿 |
| `core/__init__.py` | — | 全局 | **直接引用** | 导出同上 |
| `config.py` | — | 全局 | **直接引用** | → `core/config.py`，环境变量集中校验、缺失即启动失败 |
| `db.py` | 165 | 全局 | **移植** | → `core/db.py`，仅保留 FP/TP/Case 三表 + change_log；执行/报告表不带 |
| `llm_client.py` | — | 全局 | **移植 → 升级** | → `engine/semantic_enrich.py` 的 LLM 通道；新增**双引擎开关**与**证据引用** |
| `modules/code_analyzer.py` | **1583** | 阶段2 | **重写** | 拆为 `engine/{scan,fp_extract,tp_expand,semantic_enrich,diff_tag}.py` 五模块 |
| `modules/diff_analyzer.py` | 191 | 阶段2 | **移植** | 并入 `engine/diff_tag.py` |
| `modules/case_generator.py` | 598 | 阶段3 | **移植** | → `engine/case_gen.py`，**保留八要素 + 幂等对账**（本次最高价值资产之一） |
| `modules/tp_store.py` | — | 阶段2 | **移植** | → `core/tp_store.py`（测试点落库/查询） |
| `modules/project_manager.py` | — | 阶段1 | **移植** | → `core/project.py`（项目注册、`local_path` 解析） |
| `modules/executor.py` | 853 | 阶段4 | 不迁移 | 执行段，本次范围外 |
| `modules/browser.py` | 306 | 阶段4 | 不迁移 | 同上（Playwright 封装） |
| `modules/auth.py` | 172 | 阶段4 | 不迁移 | 同上（鉴权态复用） |
| `modules/smoke.py` | 167 | 阶段4 | 不迁移 | 同上 |
| `modules/batch_store.py` | 187 | 阶段5 | 不迁移 | 进度持久化 |
| `modules/reporter.py` | 510 | 阶段6 | 不迁移 | 报告 |
| `modules/html_report.py` | 121 | 阶段6 | 不迁移 | 报告渲染 |
| `modules/full_report.py` | 685 | 阶段6 | 不迁移 | 章节化验证报告 |
| `modules/analytics.py` | 513 | 阶段6 | 不迁移 | 趋势/flaky/覆盖率 |
| `modules/exporters.py` | 401 | 阶段6 | 不迁移 | PDF/Word/Excel 导出 |
| `main.py` | 939 | 平台 | **重写** | → `service/` 薄壳：只保留本段范围端点，版本化 `/api/v1/*`；执行/报告端点不带 |

---

## 三、门禁与工具 `scripts/`（10 个文件）—— **本次"规范同步"的主体**

| 文件 | 行数 | 处置 | 去向 / 说明 |
|---|---|---|---|
| `gate_all.py` | 121 | **直接引用** | → `scripts/gate_all.py`，八环门禁一条命令 |
| `check_secrets.py` | 295 | **直接引用** | → 密钥/明文扫描（ring 0） |
| `check_enums.py` | 256 | **直接引用** | 扫描根改为新服务源码目录 |
| `check_structure.py` | 135 | **直接引用** | 分层铁律机器强制（分层名按新服务调整） |
| `check_commit_msg.py` | 103 | **直接引用** | 提交信息 Convention 校验 |
| `install_hooks.sh` | 68 | **直接引用** | 纯 shell 钩子安装（规避 MSYS/native PATH 不兼容） |
| `pull_code.py` | 745 | **移植** | → `workspace/fetch.py`：只读克隆/更新、变更集、`classify_target()` 三态判定、URL 脱敏、防逃逸 |
| `export_golden.py` | 143 | **移植** | → `tools/export_golden.py`，供影子双跑比对 |
| `enable_remote_guard.sh` | — | 不迁移 | 远程门禁延期待办（任务 #151） |
| `migrate_enums.py` | 146 | **废弃** | 一次性枚举迁移工具，已完成使命 |

> **防双源分叉**：以上脚本在新服务中自带一份**同源副本**，并新增一条校验——两份门禁配置哈希不一致即门禁失败。

---

## 四、根目录存量脚本（**废弃主体**）

| 文件 | 行数 | 处置 | 说明 |
|---|---|---|---|
| `gen_test_points.py` | 745 | **废弃** | 被新服务 CLI 取代；其中 `resolve_scope()` 逻辑**移植**入 `engine/tp_expand.py` |
| `gen_structured_cases.py` | 1016 | **废弃** | 被新服务 CLI 取代 |
| `gen_report.py` / `gen_html_report.py` | — | 不迁移 | 报告段 |
| `gen_validation_reports.py` | 253 | 不迁移 | 验证报告生成 |
| `make_sample_report.py` | — | 不迁移 | 报告样例 |
| `make_trend_coverage_report.py` | — | 不迁移 | 趋势/覆盖率 |
| `run_full_flow.py` | 393 | 不迁移 | 旧全链路编排（阶段0→5）；新服务的编排见 `engine/pipeline.py` |
| `run_flow123.py` | — | 不迁移 | 旧编排 |
| `validate_full_pipeline.py` | 297 | 不迁移 | 旧验证脚本 |
| `verify_chain.py` / `verify_e2e.py` | 388 / 323 | 不迁移 | 旧验证脚本 |
| `selftest_*.py`（16 个） | ~3400 | 不迁移 | 保留为**回归参考**，新服务写等价单测 |
| `start_platform.sh` | — | 不迁移 | 旧启动脚本 |

---

## 五、测试与配置

| 项 | 处置 | 说明 |
|---|---|---|
| `tests/test_pull_code.py`（27 用例） | **移植** | 跟随 `pull_code` 迁移 |
| `tests/test_pure.py` | **移植** | 纯函数测试 |
| `tests/test_smoke.py` | 不迁移 | 旧冒烟 |
| `pyproject.toml` | **直接引用** | ruff / mypy / bandit / coverage 全量配置；覆盖率门槛分阶段抬高 |
| `requirements.txt` | **移植** | 裁剪为：fastapi / uvicorn / python-dotenv / openai / httpx / requests（执行段依赖 playwright 不带） |
| `ARCHITECTURE.md` | **直接引用** | 分层铁律，分层名按新服务调整 |
| `规范文档.md` | **直接引用（同源）** | 6 类规范总览；新服务复用同一份，不各写 |
| `CONTRIBUTING.md` | **直接引用** | 协作规范 |
| `ADR/ADR-0001-枚举单源与执行层.md` | **直接引用** | 架构决策记录 |
| `.gitignore` / `.env.example` | **直接引用** | 密钥不入库 |
| `report_templates/` | 不迁移 | 报告模板 |
| `frontend/` | 不迁移 | 空目录（前端未落地） |
| `data/`（DB/日志/截图/报告） | 不迁移 | 运行时数据；DB 已有快照 |

---

## 六、用户级技能 `~/.workbuddy/skills/`

| 技能 | 处置 | 说明 |
|---|---|---|
| `qa-code-pull` | **直接引用（薄壳）** | 薄壳已指向实现文件；新服务落地后改指向 `workspace/fetch.py` |
| `qa-test-points` | **移植** | 驱动脚本从 `work/test-accel/gen_test_points.py` 改为新服务 CLI |
| `qa-reuse-validation` | **直接引用** | 复用优先铁律，与实现解耦 |

> 快照已存入 `backup/skills/qa-skills-20260911.tar.gz`。

---

## 七、本次搬运的"价值密度"排序

1. **`enums.py` 契约单源** —— 所有跨模块语义的锚点，丢了等于重来。
2. **八环门禁 + 4 个校验脚本** —— "规范同步"的实体，新项目第一天就靠它守住底线。
3. **`case_generator` 八要素 + 幂等对账** —— 598 行里最值钱的是"重跑不重复造用例"。
4. **`pull_code` 只读 + 变更集 + 防逃逸** —— 直接决定 P4 双通道的成败。
5. **`resolve_scope` 范围解析** —— 745 行 `gen_test_points.py` 里唯一值得带走的部分。
6. **`fp_id` 稳定哈希算法** —— 契约稳定性的基石。

---

*本文件为 P1 交付物之一，处置结论将直接驱动 P2（骨架）与 P3（引擎）的实施范围。*
