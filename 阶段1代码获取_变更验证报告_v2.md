# 阶段 1「代码获取」变更验证报告（v2 · 修复后复验）

- **验证对象**：`scripts/pull_code.py`（拉取代码模块，含 P0/P1/P2/P3 全部修复）+ 关联的门禁配置扩展
- **验证时间**：2026-09-11 13:30–13:50（GMT+8）
- **验证方式**：静态门禁全量执行 + 单元测试 + **实机运行**（真实仓库、真实边界目录）
- **对照基线**：`阶段1代码获取_变更验证报告.md`（v1，发现 1×P0 + 3×P1 + 6×P2 + 6×P3）
- **结论摘要**：**上轮 16 项问题全部闭环；门禁 8 环全绿（GATE_EXIT=0）；单测 39 passed；实机默认路径可用**

---

## 一、更改范围概述

本轮改动分两类：**模块自身修复**（pull_code 落地 v1 报告全部建议）+ **门禁覆盖面扩展**（消除治理缺口）。

| 文件 | 变更性质 | 说明 |
|---|---|---|
| `work/test-accel/scripts/pull_code.py` | 重写 | 745 行；P0-1/P1-1/P1-2/P1-3/P2/P3 全部落地；`_local_git`→`_run_git` 命名修正；新增 `classify_target` / `_credential_env` / `FetchStatus` |
| `work/test-accel/tests/test_pull_code.py` | 重写 | 271 行；**27 个用例**（较上轮 12 个 +15），覆盖三态判定、重命名解析、凭据注入、枚举一致性、默认路径回归 |
| `work/test-accel/backend/core/enums.py` | 修改 | `PullStatus` 由 2 值扩为 **7 值**，与脚本逐值对齐（枚举单源） |
| `work/test-accel/scripts/check_enums.py` | 修改 | 反向校验扫描面由 `backend` 扩为 `backend + scripts`（工具脚本也是枚举使用方）；`hits` 补类型标注 |
| `work/test-accel/scripts/check_structure.py` / `check_secrets.py` | 修改 | 补类型标注（`ERRORS`/`WARNINGS`/`hits`），满足新增的 mypy 覆盖 |
| `work/test-accel/scripts/gate_all.py` | 修改 | 扫描目标扩展：ruff `+tests`、mypy `+scripts`、bandit `+scripts` |
| `work/test-accel/pyproject.toml` | 修改 | ruff 解除 `tests` 排除、新增 `tests/*=[E402]`；mypy 解除 `scripts` 排除（改为排除 `tests`）；bandit 解除 `scripts` 排除 |
| `~/.workbuddy/skills/qa-code-pull/pull_code.py` | 重写（薄壳） | 移除硬编码 `C:/Users/EDY/...`，改为「环境变量 → 从 cwd 逐级向上查找」 |
| `~/.workbuddy/skills/qa-code-pull/SKILL.md` | 修改 | §四 三态判定、§五 schema（+`target_not_empty`）、§八 覆盖范围与定位顺序 |
| `work/test-accel/规范文档.md` | 修改 | Q1/Q2/Q5/Q6/Q8 与通俗说明同步门禁覆盖面；实施进度表新增「门禁覆盖面补齐」 |

---

## 二、逐项修复结果（对照 v1 问题清单）

### P0-1 默认路径解析（v1 严重）
- **修复**：`DEFAULT_LOCAL` 由 `_THIS.parent.parent` 改为经 `_WORK_ROOT = _SCRIPT.parent.parent.parent` 解析。
- **实机证据**：`DEFAULT_LOCAL = C:\Users\EDY\WorkBuddy\testCodeFast\work\targets\research-agent` ✅（真实存在）
- **无参直跑**：`status=ok / mode=worktree / base_sha=93aa0d8`，**开箱可用**（v1 为 `clone_required_no_url`）。
- **回归守护**：新增 `test_default_local_resolves_to_work_targets`（断言末三段为 `work/targets/research-agent` 且不含 `test-accel`）。
- **杂散目录**：`work/test-accel/targets/` 不再产生（已确认不存在）。

### P1-1 目录三态判定（v1 高）
- **修复**：新增唯一入口 `classify_target()`，返回 `missing / repo / not_repo / escape`；`_git_repo_usable()` 改为严格相等语义（与 `code_analyzer._git_repo_usable` 口径一致）。
- **实机证据**：
  - 仓库子目录 `…/research-agent/backend` → `repo_escape_blocked`，报错文案准确 ✅
  - 仓库外**非空普通目录** + url → `target_not_empty`（**不再误判为逃逸**）✅
  - 仓库外**空目录** + url → 进入克隆流程（实测 `clone_failed(rc=128)`，路径可达、未被误拦）✅
- **守护**：`test_classify_target_{missing,not_repo,repo,escape}` + `test_pull_not_repo_no_url` + `test_pull_target_not_empty`。

### P1-2 URL 脱敏（v1 高）
- **修复**：不再以「存在 userinfo」为前提；一律剥离 query / fragment。
- **实机证据**：
  | 输入 | 输出 |
  |---|---|
  | `https://host/path?token=SECRET&a=1` | `https://host/path?...` ✅ |
  | `https://u:p@host:8443/r.git?tok=S#frag` | `https://host:8443/r.git?...#...` ✅ |
  | `https://github.com/o/r.git` | 原样（无敏感项）✅ |
  | `C:/work/x` / `git@github.com:o/r.git` | 原样（非 URL，不误伤）✅ |
- **守护**：新增反例 `test_mask_url_query_without_userinfo`、`test_mask_url_fragment_stripped`、`test_mask_url_local_path_passthrough`。

### P1-3 枚举单源（v1 高）
- **修复**：平台 `PullStatus` 扩为 7 值，与脚本本地枚举逐值一致；`check_enums` 反向校验纳入 `scripts/`（脚本即使用方）。
- **证据**：`test_pull_status_matches_platform` 断言两侧集合相等 ✅；`check_enums` 输出 **「反向校验：所有枚举成员均被引用 ✅」**（v1 尚有 1 个孤儿提示，现为 0）。

### P2（6 项）
| 项 | 修复 | 证据 |
|---|---|---|
| 迁入后仅被 ruff 覆盖 | mypy/bandit 扫描面加 `scripts` | `mypy backend scripts: OK`、`bandit backend scripts: OK` |
| tests 不在 lint 内 | ruff 解除 `tests` 排除（仅放行 E402） | `ruff check backend scripts tests: OK`；并修掉 tests 的 I001/PLR1714/F401/SIM115 |
| `pull()` 复杂度超阈值 | 抽出 `classify_target`/`_git_toplevel`/`_is_empty_dir`/`_detect_online`/`_credential_env`，分支收敛 | 仍需 `scripts/*` 的 PLR/C90 豁免（存量登记，未扩大） |
| 两处 `_git_repo_usable` 语义不一致 | 统一为严格相等口径 | `test_git_repo_usable_escape` 通过 |
| 薄壳硬编码个人路径 | 改为 env → cwd 向上查找 | 从仓库根运行技能脚本实测 `SHELL_RC=0`，正确命中实现 |
| 只读副作用强 / 耗时 | 保持设计（保护被测代码），未改语义 | 单次约 19.6s（见 §四 残留观察） |

### P3（6 项）
| 项 | 修复 |
|---|---|
| 死代码 `is_git_repo` | 已删除（grep 确认无引用） |
| `R old -> new` 解析畸形 | `changed_vs_worktree` 识别 `R`/`C` 后取 ` -> ` 右侧新路径；并处理 git 引号路径 → `test_changed_vs_worktree_rename_takes_new_path` |
| `--target HEAD` 语义歧义 | `_is_worktree_mode` 仅认 `WORKTREE`/空；`HEAD` 不再被当工作树别名 → `test_is_worktree_mode_sentinels` |
| 凭据经 argv 暴露（POSIX） | 新增 `_credential_env`：POSIX 剥离 URL 凭据 + 临时 `GIT_ASKPASS` 注入（退出即清理）→ 两个用例（nt 直通 / posix 剥离+清理） |
| `_local_git` 命名与写用途不符 | 重命名 `_run_git` 并订正 docstring（非只读）；全仓无残留引用 |
| `errors="ignore"` 传 DEVNULL 无意义 | 已从 `_net_git`/`clone` 的 Popen 移除（`_run_git` 的 text 模式保留，属有效用法） |

---

## 三、问题清单（修复后残留）

### 🟢 低（3 项，均非本模块缺陷或有意取舍）
1. **环境性：被测仓库 worktree 缺失 572 个已跟踪文件** → 默认 `HEAD..WORKTREE` 变更集放大到 573。属环境状态（非代码缺陷），建议核对 research-agent 工作树完整性；如需聚焦"真正改动"，可显式传 `--base <真实基线提交>` 走双 ref 模式。
2. **性能：单次拉取约 19.6s** → 主要来自 `_set_readonly` 对整仓递归置位（573+ 文件）。属既有设计（保护被测代码），如需优化可加"已是只读则跳过"的短路或按 mtime 增量。
3. **凭据缓解仅 POSIX 生效** → Windows 分支保持直通（`env=None`，行为不变）。这是有意的低风险取舍：v1 所指风险即"POSIX `ps` 可见 argv"，Windows 未纳入本次范围；若后续要覆盖 Windows，可复用同一 `_credential_env` 增加 `.bat`/askpass 变体。

### ⚪ 有意保留（不作为问题）
- `tests/` 不纳入 mypy（测试以 pytest 运行为准，且需自举 `sys.path`）——已在 `pyproject` 注释说明。
- `scripts/*` 仍保留 PLR/BLE/C90 的 per-file-ignores（存量登记，**未扩大豁免面**，见规范文档 D2/N9）。

### ✅ 上轮 16 项问题：全部闭环（0 遗留）

---

## 四、代码规范符合性评估

| 维度 | 结论 | 证据 |
|---|---|---|
| 命名约定 | ✅ 达标 | `_run_git`/`_net_git`/`classify_target`/`_credential_env` 语义清晰；无缩写歧义 |
| 代码风格 | ✅ 全绿 | `ruff check backend scripts tests` = All checks passed；`ruff format --check` 通过 |
| 复杂度 | 🟡 受控 | `pull()` 已抽子函数；`scripts/*` 仍享 PLR/C90 存量豁免（未扩大），属登记债务 |
| 可读性 | ✅ 达标 | 模块 docstring + 分阶段注释（阶段0/0b/1/2~4/5）+ 三处枚举单一真源；类型标注补齐 |
| 类型安全 | ✅ 达标 | `mypy backend scripts` = **Success: no issues found in 30 source files**（v1 有 4 处脚本类型错误） |
| 安全 | ✅ 达标 | `bandit backend scripts -ll` = High 0 / Medium 0；凭据加 argv 缓解（POSIX） |
| 枚举/契约 | ✅ 达标 | `PullStatus` 平台↔脚本逐值一致（单测守护）；`check_enums` 前向 0 违规 + 反向 0 孤儿 |
| 门禁覆盖 | ✅ 达标 | tests 入 ruff、scripts 入 mypy/bandit；8 环统一入口 `gate_all.py` |

**总评**：风格、命名、类型、安全、枚举契约**全面达标**；仅复杂度维度因"存量脚本豁免"留有登记债务（非新增）。

---

## 五、功能验证结论

| 验证项 | 结果 | 证据 |
|---|---|---|
| 单元测试 | ✅ **39 passed** | `pytest -q`（含 test_pull_code 27 项、test_pure、test_smoke） |
| 统一门禁 | ✅ **GATE_EXIT=0** | 8 环全 OK：secret scan / ruff lint / ruff format / check_enums / check_structure / mypy / bandit / pytest |
| 覆盖率 | ✅ 5.39% ≥ 5%（fail_under） | 未回退（v1 5.25%） |
| 默认路径开箱可用 | ✅ | 无参运行 `status=ok`，命中真实仓库 |
| 真实拉取（worktree） | ✅ | `changed_count=573`，与原生 git 一致（572 diff + 1 untracked） |
| `--strict` 退出码 | ✅ | 成功路径 rc=0；失败路径按 `success` 返回 1（用例覆盖） |
| 逃逸阻断 | ✅ | 祖先仓库子目录 → `repo_escape_blocked` |
| 非 git 目录 | ✅ | 非空→`target_not_empty`；空→进入克隆（不再误判逃逸） |
| 技能薄壳委托 | ✅ | 从仓库根运行技能脚本 `SHELL_RC=0`，命中工程内实现 |
| 既有功能影响 | ✅ **无** | 仓库内零耦合（backend/scripts 无引用 `pull_code`/`.pull_result.json`）；本次改动仅涉脚本/测试/门禁配置 |

**行为变更提示（需知悉）**：`--target HEAD` 由 v1 的"工作树模式"改为"双 ref 模式"（v1 该行为属缺陷）。若有外部脚本依赖旧行为，请显式改传 `WORKTREE`。

---

## 六、总体建议

1. **可视为阶段 1 闭环**：v1 全部 16 项问题已修复并有实机证据；门禁全绿且覆盖面已扩展，治理缺口消除。
2. **建议纳入版本控制**：本轮新增/修改文件（含 `scripts/pull_code.py`、`tests/test_pull_code.py`、`pyproject.toml`、`gate_all.py`、`check_enums.py`、`backend/core/enums.py`、技能薄壳与 SKILL.md、规范文档）尚未提交。建议按 `fix(pull-code): 修复默认路径/三态判定/URL脱敏并补齐门禁覆盖` 提交（会被 `commit-msg` 钩子校验通过）。
3. **下一步（可选）**：① 核对 research-agent 工作树完整性（消除 573 变更集的环境噪声）；② 视需要为 `_set_readonly` 加短路优化降耗时；③ 继续按 `阶段2未完成任务清单.md` 推进（A5 diff 三向分类细化到符号/hunk 级）。

> 验证纪律：本轮为"修复 + 复验"，已清理全部临时验证产物（`work/targets/_verify2_out.json`、`work/test-accel/targets/`）；目标仓库按工具设计保持只读；保留合法产物 `work/targets/.pull_result.json`。
