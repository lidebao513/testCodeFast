# 阶段 4（测试执行）评审与未完成任务清单

> 评审日期：2026-09-09
> 评审范围：`backend/modules/executor.py`、`smoke.py`、`main.py` 执行相关端点、`db.py` 的 `runs` 表、
> 以及 PIPELINE.md 定义的 4.1~4.4，含平台外资产 `work/research-agent-test/exec_live.py`。
> 编号规则：`D-★n` = **必须项**，`D-②n` = 应做项，`D-③n` = 可选项（沿用阶段3 的 ★/② 标记习惯）。

---

## 一、结论速览

| 维度 | 结论 |
|---|---|
| 功能完整度 | **约 30%**（骨架已通，实效严重不足） |
| 流程阻断性 | ❌ 不阻断——`scan_and_test` 能跑完 263 条并出报告，e2e 31/31 通过 |
| **结论可信度** | ⚠️ **实质阻断**——`D-★1`/`D-★2` 导致"通过率"不能代表被测系统真实可用 |

**一句话结论**：阶段 4 目前是"能跑但不真测"。执行器对 263 条用例的判定完全基于**源码静态文本匹配**，
真实 HTTP 结果只写日志不参与判定，异常/预期字段从未校验；真实浏览器 E2E 只存在于平台外的硬编码脚本。
因此阶段 2/3 辛苦产出的 263 条用例（含 26 条异常维度）在执行层被**降级为"文件里有没有这段字符串"**。

> 与阶段 3 对比：阶段 3 评审时完整度 25%、5 项必须；阶段 4 现状是"有执行器但无执行实效"，
> 必须项同样为 **6 项**，其中 `D-★1`/`D-★2` 直接决定测试结论能否被信任，建议优先于其他一切优化。

---

## 二、现状盘点

### 2.1 已具备（可用）
- 执行入口：`executor.run_project(pid)`，装载非归档用例 → 逐条执行 → 写 `runs` → 回写 `cases.status`。
- 四类断言派发：`http_probe` / `route_assert` / `component_assert` / `ui_probe`（按 `kind` 分派 page/component/business）。
- 评审门（C-★5 延伸）：`REVIEW_GATE` 开启时未审核用例置 `blocked_review`，不执行。
- 软删兼容：仅执行 `status != 'archived'` 的用例，历史 `runs` 不悬挂。
- 单次探活缓存：`_service_check` 每项目仅一次 `/health`，避免逐条超时。
- 冒烟能力（独立）：`smoke.py` 支持匿名探活 + 登录探活（含 token 获取、受保护端点校验），判定 SERVICE_UP/READY/AUTH_FAIL/UNREACHABLE。

### 2.2 名义存在、实质缺失
| 名义能力 | 实际情况 |
|---|---|
| 真实 HTTP 断言 | `_http_probe` 结果只进 `log`，**不参与 status 判定** |
| 截图 | `settings.SCREENSHOTS_DIR` 建了空目录、`runs.screenshot_path` 字段存在，**两者从未被写入** |
| 浏览器 E2E（4.3） | `backend/` 全库无 `playwright`/`browser` 代码；真实执行只存在于平台外 `exec_live.py` |
| 预期校验 | 测试点 `expect` 字段进入 `steps[0]` 后**从未被任何分支读取比对** |
| 执行批次 | `runs` 表无 `batch_id`，`reports.batch_id` 与 `runs` 无关联 |

---

## 三、必须项（★）

### D-★1　动态执行结果不参与判定 → 通过率失真（最高优先级）　✅ **已完成（批次2，2026-09-09）**
**证据**（`executor.py`）：
```python
struct_ok = bool(pat.search(text)) or (path in text)     # L135
status    = "pass" if struct_ok else "fail"              # L140
log       = f"structural={'ok' if struct_ok else 'missing'}; dynamic={dyn}"
```
**问题**：
1. `or (path in text)` 兜底过宽——路径串出现在注释、docstring、甚至无关的 mock 文件里都判 pass；
2. `dyn`（真实 HTTP 状态码）只写进 `log`，**服务返回 404/500 依然 pass**；
3. 结构性断言只证明"源码里写过"，不证明"服务真的注册了该路由"。

**风险**：当前 263 条用例全绿的结论**不可用于发布判断**，属于典型的假阳性。
**建议**：服务可达时以真实响应为主判定（可配置期望状态码集合），不可达时降级为静态并显式标记
`structural_only` 而非 `pass`；收紧 `path in text` 兜底（至少要求出现在路由装饰器上下文，或降级为 warn）。

### D-★2　`expect` / 异常维度完全未校验 → 阶段 2、3 的投入在执行层归零　✅ **已完成（批次2，2026-09-09）**
**现状**：`_api_probe`/`_route_assert`/`_component_assert`/`_ui_probe` 四个分支**均不读取 `step["expect"]`**。
26 条异常用例的语义是"非法/边界输入应返回 4xx 且不产生未捕获异常"，但执行逻辑与正常用例完全相同——存在即 pass。
**风险**：异常维度在阶段 2 产出、阶段 3 入用例，到阶段 4 被彻底丢弃；异常用例的"通过"毫无信息量。
**建议**：实现 expect 断言器，至少支持三类：状态码断言（含异常场景的**反向断言**：期望非 2xx）、
响应关键字断言、业务函数/路由存在性断言；断言失败 → `fail` 并给出 actual vs expected 差异。

### D-★3　平台内无真实浏览器执行与截图　✅ **已完成（批次4，2026-09-09）**
**现状**：`backend/` 无任何 playwright/browser/screenshot 引用（仅 `config.SCREENSHOTS_DIR` 空目录 + `db.runs.screenshot_path` 未用字段）。
真实 E2E 只有 `work/research-agent-test/exec_live.py`：**硬编码**线上 URL、账号密码/OTP、Chrome 绝对路径，
且 24 场景与 `fp_id`/`tp_id` **零关联**（即总清单中已登记的 `C-②2`）。
**风险**：阶段 4.3 名义存在，实质不在平台能力内；换项目即失效，无法被平台调度与回归。
**建议**：以可选依赖方式引入 Playwright（优雅降级：未安装则回退静态断言并标注），
优先支持 `page`/`component` 类用例做真实渲染断言 + **失败自动截图**并写入 `runs.screenshot_path`。
同步把 `exec_live.py` 的 24 场景资产化（见 `D-③1`）。

**实施（批次4）**：新增 `backend/modules/browser.py` 封装 Playwright（同步 API，强制单线程 + `_BROWSER_LOCK` 串行化；单次操作再套 daemon 超时线程，超时即降级）；启动探测候选顺序为「配置的 `BROWSER_EXECUTABLE_PATH` → 系统 Chrome/Edge → 自带 chromium」，**自带 chromium 在本沙箱会挂起**，已实测回退到系统 Chrome（1.3s 拉起）。`executor._ui_probe` 接入：page 类用例做真实渲染断言（`page.goto` + `expect_title` 字面量匹配），失败**自动截图**写入 `runs.screenshot_path`（已实测落到 `settings.SCREENSHOTS_DIR` 下的 PNG）。依赖缺失/启动超时 → 降级为静态断言，绝不中断整轮。

### D-★4　执行无异常防护，单条异常会中断整轮且丢失全部结果　✅ **已完成（批次1，2026-09-09）**
**现状**：`json.loads(c["steps"])`（L75）及各断言内部无 try 包裹；`conn.commit()` 在循环**之后**（L59）。
**风险**：263 条中任意一条 steps 非法/编码异常 → `run_project` 整体抛出 → 本轮**已执行结果全部丢失**（未 commit）。
**建议**：单条执行包 try/except，异常记 `status="error"` + 错误摘要后继续；异常路径也要 commit 已完成部分。

### D-★5　缺运行批次（batch），执行结果无法回溯与对比　✅ **已完成（批次1，2026-09-09）**
**现状**：`runs` 表无 `batch_id` 列；`reporter` 生成 `batch_id` 但只写 `reports`，与 `runs` 无关联；
多轮执行仅靠 `created_at` 区分。
**风险**：无法回答"这一轮 263 条的结果"、"上一轮 vs 本轮哪些由好变差"，也就无法做趋势、flaky 识别与复现。
**建议**：`runs` 幂等加 `batch_id`；`run_project` 开始时生成一次并贯穿本轮所有 `runs` 与后续 `reports`。

### D-★6　无执行子集筛选（新加的 `test_type` 在执行侧无入口）　✅ **已完成（批次3，2026-09-09）**
**现状**：`run_project` 只能全量执行全部生效用例，无 `priority`/`module`/`case_type`/`test_type` 过滤。
**风险**：增量迭代时无法"只跑新增用例"，昨日刚落地的 `test_type`（全量/新增）**无法在执行侧兑现价值**；
也无法跑 P1 冒烟子集（对应既有 `C-②4`）。
**建议**：`run_project(pid, filters)` 支持按优先级/模块/类型/测试类型筛选，并在端点暴露参数。

**实施（批次3）**：`run_project(pid, filters=None, max_workers=…, use_browser=False)` 支持按 `priority`/`module`/`case_type`/`test_type`/`ctype`/`ids` 组合筛选（`FILTER_FIELDS` 白名单，其余忽略）；新增 `POST /api/projects/{pid}/execute`（`filters`/`max_workers`/`use_browser`/`poll` 入参）与 `GET /api/projects/{pid}/execute/options`（返回可选维度取值）。增量流程产生的 `test_type=新增` 用例现可直接筛选执行。

---

## 四、应做项（②）

| 编号 | 事项 | 说明 |
|---|---|---|
| D-②1 | 执行不带鉴权态 | `executor` 从不带 token；`smoke.py` 已有登录/取 token 能力但未被复用。受保护接口探活必然 401，动态执行无法覆盖真实业务链路 | ✅ **已完成（批次3）**：抽 `auth.py` 共享登录能力，`executor` 携带 `Authorization` 头执行，401/403 自动刷新 token 重试 |
| D-②2 | 串行执行、无并发 | 263 条串行；一旦开启动态探测/浏览器，耗时不可接受。需线程池 + 连接复用（注意 SQLite 写锁） | ✅ **已完成（批次3）**：`ThreadPoolExecutor` + 顺序写库规避 SQLite 写锁，并发度 `EXEC_MAX_WORKERS`，每 50 条 flush |
| D-②3 | `cases.status` 语义混淆 | 同一字段承载 `generated`/`pass`/`fail`/`blocked_review`/`archived`(软删) 多重语义，且与 `review_status` 边界不清 |
| D-②4 | 无超时/重试/失败分类 | 无单条超时控制（`_http_probe` 硬编码 5s）、无重试策略、失败不区分断言失败/环境失败/数据失败 |
| D-②5 | 无前置校验与 fixture | 用例有 `precondition` 但执行时不校验；`projects.fixtures` 字段从未使用；无 setup/teardown 与数据清理 |
| D-②6 | 执行历史/进度/可中断 | 进度不可实时查、无取消/中断能力（阶段 5 监控当时完全依赖平台外脚本） | ✅ **已完成（批次5，2026-09-09）**：新增进程内进度注册表（done/total/percent/status_counts/state）+ `_CANCEL_SET` 中断信号；`run_project` 执行开始即登记、逐条更新进度、在 case/分块边界检查取消；端点 `GET /execute/status`（实时+重启后 runs 表回退）、`POST /execute/cancel`、`GET /execute/active`；`ExecuteReq.async_mode` 后台执行立即返回 batch_id 供轮询。自测 `selftest_b5_1.py` 11/11 |
| D-②7 | 只执行 `steps[0]` | `doc_steps` 的 4 步人类可读步骤完全不参与执行，多步用例实际只跑第一步，"8 要素"中的步骤是纸面的 |
| D-②8 | `exec_live.py` 明文凭据 | 账号/密码/OTP/线上 URL/Chrome 路径硬编码在脚本里（L16-L22），且位于产物目录（与 `S1-③3` 的版控与账号信息问题同源）。应外置 `.env` 并脱敏 | ✅ **已完成（批次4）**：`exec_live.py` 及产物目录同模式脚本均改为读 `RA_WEB_ACCOUNT`/`RA_WEB_PASSWORD`/`RA_WEB_OTP` 环境变量（`.env` 已 gitignore），`live_test_results.json` 中的账号已脱敏 |
| D-②9 | 无 flaky 识别 | 无历史结果对比、无重试后判定、无稳定性标注 |

---

## 五、可选项（③）

- **D-③1** 把 `exec_live.py` 的 24 业务场景沉淀为可复用资产（入库 + 关联 `fp_id`/`tp_id`），即已登记的 `C-②2`，可顺势阶段 4 技能化。
- **D-③2** 执行沙箱与资源清理（浏览器 context、临时文件、截图保留策略）。
- **D-③3** 用例依赖与排序（登录 → 业务 → 清理），支持 `depends_on`。
- **D-③4** 执行异步化 + webhook + 进度推送（与「服务化改造」一并设计，避免重复建设）。
- **D-③5** 通过率/覆盖率趋势看板与失败归因聚合报表。

---

## 六、建议实施批次

| 批次 | 内容 | 目标 | 状态 |
|---|---|---|---|
| **批次 1** | `D-★4`（异常防护 + 分批 commit）、`D-★5`（batch_id） | 先保证执行**不崩、可回溯**，是后续一切的基础 | ✅ **已完成**（2026-09-09，自测 13/13） |
| **批次 2** | `D-★1`（动态结果参与判定）、`D-★2`（expect/异常断言） | 让通过率**可信**，阶段 4 真正产生价值 | ✅ **已完成**（2026-09-09，自测 15/15） |
| **批次 3** | `D-★6`（筛选执行）、`D-②1`（鉴权态复用）、`D-②2`（并发） | 让执行**可用、可聚焦、够快** | ✅ **已完成**（2026-09-09，自测 14/14） |
| **批次 4** | `D-★3`（Playwright + 截图，可选依赖降级）、`D-②8`（凭据外置） | 补上真实 E2E 与截图，闭环阶段 4.3 | ✅ **已完成**（2026-09-09，自测 14/14） |

> 每批次完成后按既有习惯补自测（建议新增 `selftest_b4_*.py`，断言点覆盖：异常不中断整轮、
> batch 一致性、动态判定生效、expect 校验命中异常用例、筛选执行条数正确）。

---

## 七、批次 1 实施记录（2026-09-09）

**改动文件**

| 文件 | 改动 |
|---|---|
| `backend/db.py` | `runs` 表幂等加列 `batch_id TEXT` |
| `backend/modules/executor.py` | ① 新增 `__init__`（`last_batch_id`）、`_new_batch_id()`、`COMMIT_EVERY=50`；② 单条执行包 `try/except`，异常记 `status="error"` + `executor_error` 原因后继续，不中断整轮；③ `runs` INSERT 写入 `batch_id`，每条 result 携带 `batch_id`；④ 每 50 条分批 commit，`try/finally` 保证异常路径也提交已执行部分 |
| `backend/modules/reporter.py` | `reports.batch_id` 优先复用执行批次（meta → results[0] → 时间戳兜底），并写入 `summary.batch_id` |
| `backend/main.py` | `scan_and_test` 透传 `batch_id` 给 reporter 并在响应返回；新增两个只读端点 `GET /projects/{pid}/runs`（支持 `batch_id` 精确回溯）与 `GET /projects/{pid}/batches`（多轮分布概览） |

**自测**：新增 `work/test-accel/selftest_b4_1.py`，**13/13 通过**。关键断言：
- 单条非法 `steps` JSON → 仅该条 `error`（`executor_error: JSONDecodeError`），整轮仍返回 263 条；
- 模拟第 100 条抛出 `BaseException`（绕过单条 `except Exception`）→ 崩溃前**已执行的 99 条全部落库**；
- 四轮执行 `batch_id` 互不相同；`reports.batch_id == runs.batch_id`；
- `/runs?batch_id=` 精确回溯 263 条，`/batches` 正确统计 error 轮次（`errored=1`）。

**回归**：`selftest_b1` PASSED、`selftest_b2` PASSED、`selftest_e2e` **31/31 PASSED**，阶段 1–3 行为未变。

---

## 八、批次 2 实施记录（2026-09-09）

**改动文件**

| 文件 | 改动 |
|---|---|
| `backend/modules/executor.py` | ① 新增共享会话 `_session()`：`trust_env=False` + `proxies=None` **禁用系统代理**（本机透明代理会拦截 127.0.0.1 探测导致假性失败），并复用连接；`_base()` 统一 `localhost → 127.0.0.1`（避免 IPv6 ::1 超时）。② 新增 `_expected()`：从自然语言 expect 解析期望状态码，支持 `→ 404` / `（2xx）` / 语义兜底（资源不存在/非法/越权→4xx，功能可用→2xx）。③ 新增 `_check_expect()`：按 expect 校验真实响应码，**异常维度走反向断言**。④ 新增 `_expect_is_error()` / `_fill_path()`：占位符填充（正常填 `1`、异常填 `nonexistent`，`C-②6` 前置）。⑤ `_route_defined()` **收紧**：去掉裸 `path in text` 兜底，只认路由装饰器形式与引号包裹。⑥ `_api_probe()` 重写：服务可达 → 以真实 HTTP 响应为主判定；401/403 → `blocked_auth`；不可达 → 静态命中 `structural_only`、未命中 `fail`。⑦ page/component/business 静态命中由 `pass` 改为 `structural_only` |
| `backend/modules/reporter.py` | summary 新增 `structural_only` / `blocked_auth` / `error` / `blocked_review` 与 `effective_total`（真实通过率分母 = pass+fail）；HTML KPI 增加「仅静态验证」「需鉴权」两卡片，并加口径说明 |
| `backend/main.py` | `/batches` 分布补齐 `structural_only` / `blocked_auth`，保证分项之和 == total |

**新增执行状态语义**

| 状态 | 含义 | 是否计入通过 |
|---|---|---|
| `pass` / `fail` | 真实执行并得出结论 | 是 |
| `structural_only` | 服务不可达或未开动态探测，仅完成静态断言（**未发出真实请求**） | **否** |
| `blocked_auth` | 真实请求返回 401/403，当前未携带 token（`D-②1`） | 否 |
| `error` | 执行器内部异常（`D-★4`） | 否 |

**自测**：新增 `work/test-accel/selftest_b4_2.py`，**15/15 通过**。用本地 mock HTTP 服务做可控真实请求：
- 动态判定：正常 expect+200→`pass`、+500→`fail`（不再被静态断言掩盖）、401→`blocked_auth`；
- **异常反向断言**：期望 404 实际 404→`pass`；期望 404 实际 200→`fail`（非法输入未被拦截）；
- 占位符填充：实测请求路径为 `/threads/1` 与 `/threads/nonexistent`；
- 结构断言收紧：路径仅出现在注释里不再命中（旧逻辑会误判）；
- **集成回归：默认配置下 `pass == 0`、静态验证 263 条**——原先 263 条虚假通过全部消除。

> ⚠️ **重要行为变化**：默认 `ENABLE_DYNAMIC_PROBE=off` 且本地 8010 未启动，263 条会全部落到
> `structural_only`，报告「通过」显示 **0**。这是**预期且正确**的表现（不再把"没测"伪装成"通过"）。
> 要拿到真实通过率需：启动被测服务并在 `.env` 设 `ENABLE_DYNAMIC_PROBE=on`
> （可先用 `GET /api/projects/{pid}/smoke` 确认服务可达）。

**回归**：`selftest_b1` / `selftest_b2` PASSED、`selftest_b4_1` **13/13**、`selftest_e2e` **31/31**，阶段 1–3 行为未变。

---

## 十、批次 3 实施记录（2026-09-09）

**任务**：`D-★6`（执行子集筛选）、`D-②1`（鉴权态复用）、`D-②2`（并发）。

**改动文件**

| 文件 | 改动 |
|---|---|
| `backend/config.py` | 新增 `EXEC_MAX_WORKERS`（默认 7）、`AUTH_AUTO_LOGIN`、`AUTH_LOGIN_URL`、`AUTH_LOGIN_BODY`、`AUTH_TOKEN_PATH`、浏览器相关 `ENABLE_BROWSER`/`BROWSER_*` 配置项 |
| `backend/modules/auth.py`（新增） | 从 `smoke.py` 抽离共享登录能力：`login(base, user, pwd)` 取 token，`build_auth_header()` 构造 `Authorization`；支持 `AUTH_AUTO_LOGIN=off` 关闭 |
| `backend/modules/executor.py` | ① `run_project(pid, filters, max_workers, use_browser)`：`FILTER_FIELDS` 白名单支持 `priority/module/case_type/test_type/ctype/ids` 组合筛选；② `ThreadPoolExecutor` 并发执行（顺序写库规避 SQLite 写锁），并发度 `settings.EXEC_MAX_WORKERS`，每 50 条 flush；③ `_api_probe` 携带 `Authorization` 头，401/403 自动 `login()` 刷新一次重试；④ `_ui_probe` 接入 `browser.check_page`（page 类真实渲染断言 + 失败截图） |
| `backend/main.py` | 新增 `POST /api/projects/{pid}/execute`（`filters`/`max_workers`/`use_browser`/`poll`）、`GET /api/projects/{pid}/execute/options`；`/cases` 列表支持 `test_type` 过滤 |

**自测**：新增 `work/test-accel/selftest_b4_3.py`，**14/14 通过**。关键断言：
- 筛选：按 `priority=P1` / `case_type=异常` / `test_type=新增` / `ids=[…]` 各返回正确子集，且只执行子集（不污染全量）；
- 鉴权态：401 自动登录重试 → `pass`；未配凭据 → `blocked_auth`（不误判 fail）；
- 并发：20 条用例并发执行 vs 串行结果一致（status 分布相同），写库无竞态；
- `/execute/options` 返回 `test_type` 维度取值 `["全量","新增"]`。

**回归**：`selftest_b1` / `selftest_b2` PASSED、`selftest_b4_1` **13/13**、`selftest_b4_2` **15/15**、`selftest_e2e` **31/31**。

---

## 十一、批次 4 实施记录（2026-09-09）

**任务**：`D-★3`（Playwright 真实浏览器 + 截图）、`D-②8`（凭据外置）。

**改动文件**

| 文件 | 改动 |
|---|---|
| `backend/modules/browser.py`（新增） | 封装 Playwright（同步 API，强制单线程 + `_BROWSER_LOCK` 串行化；单次操作套 daemon 超时线程，超时即降级）。启动探测候选顺序：配置 `BROWSER_EXECUTABLE_PATH` → 系统 Chrome/Edge → 自带 chromium。**关键发现：自带 chromium 在本沙箱会挂起**（>10 分钟不返回），实测回退到系统 Chrome 仅需 1.3s。`check_page(url, name)`：真实渲染 + `expect_title` 字面量匹配，失败自动截图写入 `runs.screenshot_path` |
| `backend/modules/executor.py` | `_ui_probe` 的 page 类用例接入 `browser.check_page`；`use_browser=True` 时启用；浏览器不可用 → 降级为静态断言（`structural_only`）并标注降级原因，绝不中断整轮 |
| `backend/config.py` | 新增 `ENABLE_BROWSER`（默认 off，需显式开启）、`BROWSER_EXECUTABLE_PATH`（指向系统 Chrome 已实测可用）、`BROWSER_LAUNCH_TIMEOUT`/`BROWSER_PROBE_TIMEOUT`/`BROWSER_TIMEOUT_MS` 等 |
| `backend/db.py` | `runs` 表 `screenshot_path` 列已在批次1前存在，本批次**首次被实际写入**（之前为死字段） |
| `work/research-agent-test/exec_live.py` 及同目录脚本 | 明文 `EMAIL/PWD/OTP` 改为读 `RA_WEB_*` 环境变量（`.env` 注入，已 gitignore）；新增 `.env.example`；`live_test_results.json` 账号脱敏为 `te***@ft.cntaiping.com` |

**自测**：新增 `work/test-accel/selftest_b4_4.py`，**14/14 通过**。关键断言：
- 真实浏览器：`check_page` 打开 mock 页面 → 标题匹配 `pass`；标题不符 → `fail` 且 `runs.screenshot_path` 文件存在（截图真落盘）；
- **优雅降级**：模拟 playwright 未安装 → 自动降级为静态断言且不抛异常；
- **挂死超时保护**：`check_page` 在沙箱 chromium 挂死时 25s 超时返回降级结果（不拖垮整轮）；
- 凭据外置：`RA_WEB_*` 缺失时脚本不报错退出、可在 CI 中以空值安全运行。

> ⚠️ **环境阻塞点（如实记录）**：Playwright 自带 chromium 在本 WorkBuddy 沙箱**启动挂起**，无法用于真实渲染。已通过「自动回退系统 Chrome（`BROWSER_EXECUTABLE_PATH`）」绕过——`selftest_b4_4` 与 `verify_chain` 均用系统 Chrome 跑通。若部署环境无系统 Chrome，需预装 `playwright install chromium` 或设置 `BROWSER_EXECUTABLE_PATH`。

**回归**：`selftest_b1`/`b2`/`b4_1`/`b4_2`/`b4_3`/`e2e` 全部通过（详见第十二节）。

---

## 十二、全链路流程验证（2026-09-09，复跑稳定）

新增 `work/test-accel/verify_chain.py`，从**代码获取 → 代码就位 → 依赖安装 → 配置生效 → 服务启动 → 测试执行 → 资源回收**完整跑通，用 `frontend/mock-server`（16 路由、依赖仅 fastapi/uvicorn/requests）作为真实被测子服务。

**结果：PASS 8 / BLOCK 2 / FAIL 0，总耗时 8.2s**（复跑一致）。

| 步骤 | 环节 | 结果 | 耗时 | 关键证据 |
|---|---|---|---|---|
| 1 | 代码获取·拉取 | **BLOCK** | 0.3s | 当前为就地 init 的独立仓库，无 remote，无法 `git fetch`（预期，非缺陷） |
| 2 | 代码获取·检出 | PASS | 0.3s | `checkout main → 34f105b00a4f`，工作区变更 0 项 |
| 3 | 代码就位·目标路径与隔离 | PASS | 0.0s | `local_path` 存在；第三方仓库已 gitignore |
| 4a | 依赖安装·主后端 | **BLOCK** | 0.0s | 被测主后端 `server.py` 已随重构移除、无启动入口、依赖清单缺失 |
| 4b | 依赖安装·mock-server | PASS | 1.0s | `pip install -r requirements.txt` rc=0 |
| 5 | 配置生效·环境变量注入 | PASS | 0.2s | 子进程读取 `EXEC_MAX_WORKERS`/`ENABLE_DYNAMIC_PROBE` → 生效 |
| 6 | 服务启动·mock-server | PASS | 1.1s | `http://127.0.0.1:8091` 探活成功 |
| 7 | 测试执行·真实 HTTP 动态判定 | PASS | 3.3s | 20 条用例，分布 `pass:4 / fail:6 / blocked_auth:10`，真实通过率分母=10（样例：期望 2xx 实际 500→fail；期望 2xx 实际 200→pass） |
| 8 | 测试执行·真实浏览器渲染 | PASS | 1.3s | 浏览器打开 `/api/v1/health` → HTTP 200（系统 Chrome） |
| 9 | 资源回收·停止服务 | PASS | 0.0s | mock-server 已终止 |

**链路断点/不稳定点**：
- **步骤 1（拉取）BLOCK**：无 remote，属本地独立仓库设计，不影响执行；要走真·远程对比需注册 codeup 令牌（`qa-code-pull` 已有该能力）。
- **步骤 4a（主后端依赖）BLOCK**：主后端重构后 `server.py` 已不存在，当前无可用启动入口——这是**被测目标自身的现状**，平台侧 `verify_chain` 已改为用可运行的 `mock-server` 子服务验证链路。
- 链路**唯一不稳定因素**：Playwright 自带 chromium 启动挂死（已用系统 Chrome 绕过，非链路断裂）。

## 十三、批次 5 实施记录（2026-09-09，D-②6 执行历史/进度/可中断）

**任务**：D-②6（进度实时查询 + 取消/中断能力），即"阶段 5 执行监控与记录"的地基。

**改动文件**

| 文件 | 改动 |
|---|---|
| `backend/modules/executor.py` | ① 模块级 `_BATCH_REGISTRY`（`done/total/status_counts/state/started_at/finished_at`）+ `_BATCH_REGISTRY_LOCK`、`_CANCEL_SET` + `_CANCEL_LOCK`；② 新增 `register_batch/update_progress/finish_batch/cancel_batch/is_cancelled/get_batch_status/active_batches`；③ `run_project` 支持外部 `batch_id`（异步模式预生成），开始即 `register_batch`，串行/并发循环逐条 `update_progress`，case 与分块边界 `is_cancelled` 检查 → 中断在 ≤COMMIT_EVERY(50) 条边界退出，终态写入 `done/cancelled/error`；④ 顶层异常 `finish_batch(state="error")` 并保底提交已执行部分 |
| `backend/main.py` | ① `ExecuteReq` 加 `async_mode`：True 时后台线程启动 `run_project` 并立即返回 `batch_id` + `status_url/cancel_url/active_url`；② 新增 `GET /execute/status`（`batch_id` 缺省=进行中列表；命中 registry 返回实时进度并附 `percent`，未命中则从 `runs` 表重建终态，来源标注 `source`）、`POST /execute/cancel`（`Body(batch_id)` 请求中断）、`GET /execute/active`（进行中批次列表）；③ `import threading` |

**自测**：新增 `work/test-accel/selftest_b5_1.py`，**11/11 通过**。关键断言：
- 同步执行：注册表 `running→done`，`done==total`，`status_counts` 合计==total；
- 中断（同步+后台线程触发取消）：终态 `cancelled` 且 `done<total`，已执行 `runs` 数==`done`（不丢），未执行部分无 `runs`；
- API 异步：`async_mode` 立即返回 `batch_id`（不阻塞），轮询 `/execute/status` 直到 `done==total`；
- API 取消：`POST /execute/cancel` 后终态 `cancelled` 且 `done<total`；
- 活跃列表：`/execute/active` 在执行中能列出本批次（running）；
- 回退：`/execute/status` 在 registry 无记录时从 `runs` 表重建终态（`source=runs`）。

**回归**：`selftest_b4_3.py` 14/14 通过（重构 `run_project` 仅新增可选 `batch_id` 形参与进度/取消逻辑，既有筛选/鉴权/并发行为不变）。

**已知边界（如实记录）**
- 中断粒度：正在执行的分块（最多 `COMMIT_EVERY=50` 条）无法中途杀线程，取消在下个分块/下条边界生效——263 条并发时最坏延迟约 50 条；
- 进度注册表为进程内内存态，平台进程重启后"进行中活跃列表"清空，但历史进度可由 `runs` 表按 `batch_id` 重建（已内置回退）；若要"重启后仍显示进行中"，需落 `run_batches` 持久表（见阶段 5 评审 P5-②5）；
- `async_mode` 后台线程仍绑在平台进程；真正的"任务队列 + webhook 推送"属 D-③4（平台服务化），本批次仅提供轮询式最小可用监控。

## 九、与既有清单的关系

- `C-②2`（24 场景未资产化）↔ 本清单 `D-③1`：同源，建议在 `D-★3` 一并处理。
- `C-②4`（无 P1 冒烟子集）↔ 本清单 `D-★6`：执行侧筛选能力建成后即闭环。
- `C-②6`（`{account_id}` 占位符未参数化）↔ 本清单 `D-★1`：开启动态判定前必须先参数化，否则必然 404 误判为失败。
- `S1-③3`（产物目录含账号信息）↔ 本清单 `D-②8`：均应脱敏处理。
- 平台级（服务化）"executor 不跑真实浏览器"即本清单 `D-★3`。
