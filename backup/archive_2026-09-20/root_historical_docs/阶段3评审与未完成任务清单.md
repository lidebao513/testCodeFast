# 阶段 3「测试用例生成」· 评审报告 + 未完成任务清单

> 对应 `PIPELINE.md` 阶段 3。评审日期 2026-09-08。
> 评审对象：`work/test-accel/backend/modules/case_generator.py`（113 行）+ 其调用方
> `backend/main.py`（`/scan_and_test`、`/diff_analyze`）+ 实际业务场景脚本
> `work/research-agent-test/exec_live.py`（24 场景）。
> 标识：🔴 **必须**（阻塞阶段4 执行质量 / 阶段6 报告可信度）｜🟡 应做 ｜⚪ 可选

---

## 零、上游遗留项的阻断性判断（先结论）

| 遗留项 | 判定 | 依据 |
|---|---|---|
| 真·远程基线恢复（`base..target` 双版本 diff，需 codeup 凭据） | ❌ **不阻断** | 目标仓库已就地 `git init`（`main` / `34f105b`），`.pull_result.json` 契约正常产出；`WORKTREE` 增量模式 + 全量扫描降级可用，`changed_files=0` 时全部标「全量」，结论正确仅聚焦度下降。已登记 **阶段1 S1-★5** |
| A5 diff 三向分类过粗（阶段2 唯一必须项） | ❌ **不阻断** | 影响「变更功能点」膨胀，不影响用例能否生成；已在阶段2 清单登记 |
| `work/research-agent-test/` 是否纳入版控 | ❌ **不阻断** | 仓库卫生问题，已登记 **阶段1 S1-③3** |

**结论：三项均不阻断，已归档进各阶段清单，继续推进阶段 3 评审。**

---

## 一、阶段 3 现状（实测数据，非推测）

| 观测项 | 实测值 | 取证方式 |
|---|---|---|
| 生成器规模 | `case_generator.py` **113 行**，`_build()` 仅 3 个分支 | 源码 |
| 用例总数 : 功能点总数 | **270 : 270 = 1:1** | `SELECT COUNT(*)` cases / functional_points |
| 每条用例步数 | **全部 1 步**（270/270） | 解析 `cases.steps` |
| step 动作种类 | **3 种**：`http_probe` 242 / `component_assert` 20 / `route_assert` 8 | 同上 |
| step 字段 | 仅 `action` / `file` / `method` / `path` / `expect` | 同上 |
| `expect` 取值 | 仅 `route_defined` / `interactive_present` | 同上 |
| `ctype` | `api` 242 / `e2e` 28 | 同上 |
| `script_path` | **270/270 为 NULL**（未产出任何可执行脚本） | 同上 |
| `review_status` | **270/270 = pending** | 同上 |
| 阶段2 测试点被消费数 | **0 / 263**（生成器只读 `functional_points` 表） | 源码 + `test_points_new.json` |
| 用例文档产物 | **0 个**（全仓 `find -iname "*CASE*"` 只命中源码文件） | 文件系统扫描 |
| 24 业务场景与 fp_id/tp_id 关联 | **0 处**（`exec_live.py` grep `fp_id|tp_id|test_points` 无命中） | grep |
| case 相关 API 端点 | 只有 `GET /api/projects/{pid}/cases`；**无**新增/修改/审核/导出端点 | main.py 端点清单 |

### 一句话定性
> 当前阶段 3 生成的不是「测试用例」，而是**功能点的存在性自检清单**——
> 每个功能点配一条「这个路由/组件在代码里确实存在」的单步断言。
> 真正有业务价值的 24 个场景（F-A1~F-I5）是**人工硬编码**在一次性脚本里的，
> 与阶段 2 的 263 条测试点、237 个功能点**零关联**。

---

## 二、能力完整性评估

| 阶段 3 应具备的能力 | 现状 | 完整度 |
|---|---|---|
| 功能点 → 用例映射 | ✅ 已实现（api/page/component 三类） | 80% |
| 幂等增量生成 | ✅ `generate_for_fp_ids`（按 title 去重复用） | 85% |
| **测试点 → 用例映射** | ❌ 未实现（263 条测试点全部落空） | **0%** |
| **用例要素完整性**（前置/数据/步骤/预期/优先级/类型） | ❌ 仅有标题 + 单步伪步骤 | **15%** |
| **三层追溯**（测试点↔功能点↔用例） | ❌ 阶段 3 断链（存 DB 行 id 而非稳定 `fp_id`） | **20%** |
| **用例交付物**（MD/Excel/HTML 清册） | ❌ 无产物，仅存 SQLite | **0%** |
| **评审闭环**（pending → approved） | ❌ 有状态字段，无流转入口 | **10%** |
| 业务场景设计沉淀 | ❌ 硬编码在脚本，未入平台/技能 | 10% |
| 可执行脚本产出 | ❌ `script_path` 全空 | 0% |
| **阶段 3 整体完整度** | | **≈ 25%** |

---

## 三、功能缺失项

### 🔴 必须（5 项，阻塞下游质量）

| 编号 | 缺失项 | 问题实质 | 影响 |
|---|---|---|---|
| **C-★1** | **测试点未进入用例链路** | 阶段 2 已产出 263 条测试点（by_type：正常 237 / 异常 26，Scope 可扩安全+边界），阶段 3 却只读 `functional_points`。异常/安全/边界维度在用例层**100% 丢失** | 用例 = 功能点数（1:1），永远只有「正常-存在性」一维；阶段 6 报告的「覆盖率」失真 |
| **C-★2** | **用例要素不完整** | 无前置条件、无测试数据、无多步操作步骤、无预期结果文本（只有枚举值 `route_defined`）、无优先级、无用例类型、无所属模块/业务语义（阶段 2 已算好 `module`/`semantic`/`title` 却未透传） | 用例不可人工评审、不可交付给测试人员、无法作为回归资产 |
| **C-★3** | **追溯链在阶段 3 断裂** | `cases.fp_id` 存的是 `functional_points` **自增行 id**（实测 393/394/395），不是契约稳定 ID（`FP-3e755d06`）；且**无 `tp_id` 字段** | 重新 analyze 后行 id 变化 → 用例与功能点/测试点全部断链；阶段 6「用例清册」无法回指测试点 |
| **C-★4** | **无用例交付物** | 全仓无 `TEST_CASES.md/xlsx`；阶段 6 的「用例清册」章实际取自 `live_test_results.json`（手写场景） | 阶段 3 对最终报告**贡献为零**，流水线在此处实际断开 |
| **C-★5** | **评审门是死胡同** | `REVIEW_GATE=on` 时 270 条全部 `pending`，但后端**无** approve/reject/编辑端点；且 `executor` 不校验 `review_status`，pending 用例照样执行 | 评审机制形同虚设，"人工确认后再执行"的设计意图未落地 |
| **C-★6** | **阶段1/阶段2 功能点发现口径未统一** | 后端 `code_analyzer.analyze` 对被测仓库仅发现 **99 个功能点**（71 api / 8 page / 20 component，**0 business**），而阶段 2 `gen_test_points` 发现 **237 个 FP / 263 个 TP**。两条 FP 发现链路彻底分叉 → 138 条 TP 的 `fp_contract_id` 在 `functional_points` 表行级**无对应**（仅契约级可追溯） | 行级 `FP→TP→Case` 追溯永久缺 138 条；`functional_points` 表本身不完整，影响 diff_analyze / 用例 UI 清册 / 任何按 FP 行 join 的逻辑 |

### 🟡 应做（7 项）

| 编号 | 缺失/问题 | 说明 |
|---|---|---|
| C-②1 | 全量重生成破坏历史 | `generate_for_project` 先 `DELETE FROM cases`，而 `runs.case_id` 无外键/级联 → 再生成后历史执行记录悬挂（当前实测 0 孤儿，属**未触发的设计隐患**）。应改软删除或用例版本号 |
| C-②2 | 业务场景设计未沉淀 | 24 场景的 prompt / 关键词断言 / 超时 / 写类操作回滚策略是阶段 3 **最有价值的资产**，却硬编码在一次性脚本，不可复用、不入库、未技能化 |
| C-②3 | 无用例去重/合并 | `frontend/mock-server/server.py`（16 路由）与真实 `tools/*` 路由重叠，会各生成一条用例 → 用例膨胀且执行重复 |
| C-②4 | 无优先级与冒烟子集 | 未利用阶段 2 的「更新」标签 + 业务权重排序，阶段 4 只能全量跑，无法出「变更相关 P0 冒烟集」 |
| C-②5 | 页面/组件用例过弱 | `route_assert` / `component_assert` 仅做**文本包含**断言（`p in text`、正则找 `@click`），无登录前置、无参数化、无真实交互断言 |
| C-②6 | API 用例未参数化 | 路径含占位符（实测 `DELETE /accounts/{account_id}`、`{fp_id}` 类）未生成有效值 → 一旦开启动态探测必然 404/422 误报 |
| C-②7 | 路径分隔符未归一 | `_build()` 直接透传 `fp["file_path"]`；历史库中存在 `frontend\mock-server\server.py`（反斜杠），跨平台执行会 file missing。阶段 2 已统一正斜杠，阶段 3 应加兜底 `replace("\\","/")` |

### ⚪ 可选（4 项）

| 编号 | 项 | 说明 |
|---|---|---|
| C-③1 | 可执行脚本骨架产出 | 生成 pytest / Playwright 脚本文件并回填 `script_path` |
| C-③2 | LLM 增强步骤描述 | 当前确定性生成（优点：稳定、可重放）；可作为**可选开关**补业务化步骤/数据，不做默认路径 |
| C-③3 | 阶段 3 技能化 | `PIPELINE.md` 标注阶段 3 无 skill；可抽 `qa-test-cases`（场景设计 + 用例要素规范） |
| C-③4 | 外部用例系统对接 | 导出 TAPD / Jira / Xray 格式 |

---

## 四、必须项的建议改造顺序（3 批）

| 批次 | 内容 | 交付判据 |
|---|---|---|
| **批次 1（核心链路）** | C-★1 + C-★3：把生成器主链路从 `FP→Case` 改为 **`TP→Case`（FP 作分组维度）**，用例携带 `tp_id` + 稳定 `fp_id` | 用例数 ≥ 测试点数（263+），`tp_without_case = 0`，追溯矩阵三层贯通 |
| **批次 2（可交付）** | C-★2 + C-★4：补齐 8 要素（编号/模块/标题/类型/优先级/前置/步骤/预期），产出 `TEST_CASES.md` + `test_cases.json` | 抽检 20 条用例，测试人员可无需读代码直接执行 |
| **批次 3（闭环）** | C-★5 + C-②1：新增 `PATCH /cases/{id}`（审核/编辑）、`executor` 校验 `review_status`、用例软删除+版本号 | `REVIEW_GATE=on` 时 pending 用例不被执行；重生成后历史 runs 不悬挂 |

---

## 五、验收标准（用于改造后自测）

1. `tp_without_case = 0`、`case_without_tp = 0`（追溯零孤儿）；
2. 用例 `by_type` 分布与阶段 2 `by_type` 一致（正常/异常/安全/边界比例可对齐）；
3. 每条用例含非空「前置条件 / 步骤（≥1 步且非伪步骤）/ 预期结果（自然语言）/ 优先级」；
4. 重新 `analyze` 两次，用例 ID 与追溯关系稳定不变（依赖稳定 `fp_id`/`tp_id`）；
5. `REVIEW_GATE=on` 时，未 approved 用例执行数 = 0；
6. 产出 `TEST_CASES.md` 且阶段 6 报告「用例清册」章从该产物取数，而非手写脚本。

---

## 六、批次 1 改造记录（✅ 已完成 · 2026-09-08 16:xx）

> 实施范围：C-★1（测试点进入用例链路）+ C-★3（稳定 ID 追溯，契约级）。
> 实测自测 `selftest_b1.py` 全绿；新增端点经 `TestClient` 实弹联通。

### 6.1 改动文件
| 文件 | 改动 |
|---|---|
| `backend/db.py` | `init_db()` 新增 `test_points` 表；`functional_points` 加 `contract_id` 列；`cases` 加 `tp_id` / `fp_contract_id` 列；新增幂等 `_add_column()` 迁移（不丢数据） |
| `backend/modules/tp_store.py`（**新增**） | `import_test_points(pid, json_path)` 读取 `test_points_new.json` 按 `(project_id, tp_id)` 幂等 upsert；`get_test_points()` |
| `backend/modules/case_generator.py` | `generate_for_project` 优先 TP 驱动：每个 TP → 1 条用例，写入 `tp_id` + `fp_contract_id`，steps 携带 `tp_id/fp_id/source/method/path/dimension/expect`；无 TP 时回退 FP 逻辑（保证其他流程不破）；新增 `_build_from_tp()`；`generate_for_fp_ids` 同步写 `fp_contract_id` |
| `backend/main.py` | 导入 `tp_store`；`_analyze_project` 落库 `functional_points.contract_id`；新增 `POST /import_test_points`、`GET /test_points`、`POST /generate_cases`；`scan_and_test` 复用已改写的 `generate_for_project`（自动 TP 优先） |
| `selftest_b1.py`（**新增**） | 批次 1 自测：TP→Case 1:1、稳定 ID、异常维度不丢、零孤儿 |

### 6.2 实测结果
- 导入测试点 **263 条**（正常 237 / 异常 26）；
- 生成用例 **263 条**（1:1，c type 全部 api，因当前 TP 均为接口类）；
- **异常维度 26 条全部进入用例**（不再 100% 丢失）；
- 每条用例 `tp_id` 形如 `TP-xxx`、`fp_contract_id` 形如 `FP-xxxx`，steps 含 `expect` 自然语言预期；
- `case_without_tp = 0`、`tp_without_case = 0`（零孤儿）；
- 复跑 `analyze` 两次，`fp_contract_id` 稳定不变（稳定 ID 生效）。

### 6.3 暴露的新问题（已登记为 C-★6）
批次 1 改造把"行级 FP 追溯"依赖暴露出来：后端 `code_analyzer.analyze` 只发现 **99** 个功能点，
而阶段 2 `gen_test_points` 发现 **237** 个。导致 263 条 TP 引用的 237 个 `fp_id` 中，仅有 99 个能在
`functional_points` 表行级匹配，**138 条 TP 的 `fp_contract_id` 无对应 FP 行**（契约级追溯仍有效，
因 `tp.fp_id` 本身是合法的 `FP-xxxx`）。

> 根因：阶段 1（后端 `code_analyzer`）与阶段 2（离线 `gen_test_points`）是**两套独立的 FP 发现实现**，
> 且后端 analyzer 未覆盖重构后的 `tools/*` 路由与 business 维度。这是 C-★6 必须修复的底层口径差。
> 验收：修复后 `COUNT(DISTINCT fp_contract_id IN cases) ⊆ COUNT(contract_id IN functional_points)`
> 应接近 100% 覆盖（理想 = 100%）。

### 6.4 剩余批次
- **批次 2（C-★2 + C-★4）**：补 8 要素（编号/模块/标题/类型/优先级/前置/步骤/预期）+ 产出 `TEST_CASES.md`；✅ **已完成（见第七节）**
- **批次 3（C-★5 + C-②1）**：审核端点 + `executor` 校验 `review_status` + 用例软删版本号；
- **C-★6 修复**：统一阶段 1 / 阶段 2 的 FP 发现口径（建议后端 `code_analyzer` 复用 `gen_test_points` 的路由/业务发现，或阶段 2 改消费后端 FPs）。

---

## 七、批次 2 改造记录（✅ 已完成 · 2026-09-08 17:xx）

> 实施范围：C-★2（用例 8 要素完整）+ C-★4（用例交付物 `TEST_CASES.md`）。
> 实测 `selftest_b2.py` 全绿；交付物 `work/research-agent-test/TEST_CASES.md` + `test_cases.json` 已落地。

### 7.1 改动文件
| 文件 | 改动 |
|---|---|
| `backend/db.py` | `cases` 表新增 6 列：`tc_no`(编号) / `module`(模块) / `case_type`(类型) / `priority`(优先级) / `precondition`(前置) / `doc_steps`(人类可读多步 JSON)；`_add_column` 幂等迁移 |
| `backend/modules/case_generator.py` | `_build_from_tp` 补 8 要素：编号=`tp_id`、模块=TP.module、类型=category、优先级=`_priority()` 派生、前置=`_precondition()` 派生、`doc_steps`=前置→准备→执行→断言四步（断言步带自然语言预期）；新增 `_priority/_precondition/_build_doc_steps` 派生函数；FP 回退分支 `_build_legacy` 也填 8 要素默认值；新增 `export_test_cases(pid)` 产出 `TEST_CASES.md` + `test_cases.json`；新增统一 `_insert_case` |
| `backend/modules/reporter.py` | `build()` 增加「用例清册」卡片：若 `<name>-test/test_cases.json` 存在则引用其汇总（类型/优先级计数 + 说明链接），满足 C-★4 第 6 条"阶段 6 从产物取数" |
| `backend/main.py` | `generate_cases`、`scan_and_test` 生成后自动调用 `export_test_cases`；新增 `POST /export_test_cases` 端点 |
| `selftest_b2.py`（**新增**） | 8 要素非空 / 多步非伪 / 优先级派生 / 交付物齐全 / reporter 联动 |

### 7.2 派生规则（确定性、可解释）
- **优先级**：异常/安全/边界 → `P1`；正常用例中模块含 鉴权/登录/支付/交易/权限/审批 → `P1`；其余 `P2`。实测：P1=81（含异常 26 + 鉴权类等），P2=182。
- **前置条件**：异常类 → "服务可达且已明确非法输入构造方式"；正常类 → "服务可达、鉴权接口需有效 token"。
- **步骤**：固定四步（前置/准备/执行/断言）；机器可执行步仍留在 `steps[0]`（executor 仅读 steps[0]），人类叙事放 `doc_steps`，执行链路零回归。

### 7.3 实测结果
- 263 条用例 **8 要素全部非空**（编号/模块/标题/类型/优先级/前置/步骤≥3/预期自然语言）；
- `TEST_CASES.md`（263 条明细 + 按类型/优先级/模块汇总）、`test_cases.json`（结构化，8 要素齐全）落盘 `work/research-agent-test/`；
- 阶段 6 报告 HTML 含「用例清册」卡片，从 `test_cases.json` 取数（不再取自手写 `live_test_results.json`）；
- `TestClient` 实弹 `generate_cases` 返回 `export` 字段（md/json 路径 + 计数）。

### 7.4 剩余
- ~~批次 3（C-★5 + C-②1）~~ ✅ 已完成（见第八节）；
- ~~C-★6 修复~~ ✅ 已完成（见第九节）。

---

## 八、批次 3 改造记录（✅ 已完成 · 2026-09-08）

> 实施范围：C-★5（评审闭环）+ C-②1（用例软删版本号）+ 执行派发（ui_probe）。
> 实测 `selftest_e2e.py` 端到端 **28/28 全绿**；`selftest_b1` / `selftest_b2` 回归全绿。

### 8.1 改动文件
| 文件 | 改动 |
|---|---|
| `backend/db.py` | `functional_points` 加 `title`/`module`（C1 契约完整化）；`cases` 加 `version INTEGER DEFAULT 1`（软删版本号）+ `review_comment TEXT`（C-★5）；`_add_column` 幂等 |
| `backend/modules/executor.py` | `run_project` 仅处理 `status!='archived'` 的用例；**评审门**：`REVIEW_GATE=on` 时跳过 `review_status!='approved'` 的用例（置 `blocked_review`）；新增 `_ui_probe` 按 `step.kind` 派发到 路由/组件/业务函数断言，使 page/component/business 用例均可真实执行（不再 `skipped`） |
| `backend/modules/case_generator.py` | `generate_for_project` 改为**软删**：每轮先归档上一轮（`status='archived'`）再生成新轮（`version=上轮+1`），保留历史 `runs`；`_build_from_tp` 为 page/component/business 用例设置 `action='ui_probe'` + `kind`/`file`/`func`，使 executor 可派发；`_insert_case` 写入 `version`/`review_comment`；`export_test_cases` 仅导出非归档用例 |
| `backend/main.py` | 新增 `POST /cases/{cid}/review`（单条 approve/reject+意见）+ `POST /cases/bulk_review`（批量，ids 空则审核全部非归档）；`_analyze_project` 改用 `generate_comprehensive_test_points()`（C-★6）；`scan_and_test` 一键闭环先尝试导入阶段2 测试点（走 TP 驱动） |
| `selftest_e2e.py`（**新增**） | 阶段1→3 串联 28 项断言 |

### 8.2 评审闭环验证（C-★5）
- `REVIEW_GATE` 默认 **on** → 生成用例为 `pending`；
- `executor.run_project` 在门开时**全部 `blocked_review`**（263/263），不执行未审核用例；
- `bulk_review(approve)` 后重跑 → `blocked_review==0`，全部执行；
- 反向：单条 `reject` 后该用例仍 `blocked_review`（评审门有效，不误放行）。

### 8.3 软删版本号（C-②1）
- 每轮 `generate_for_project` 把上一轮置 `archived`，新轮 `version=+1`；
- 实测：首轮 生效 263 / 归档 0；二次生成 生效 263 / 归档 263（旧轮保留，历史 `runs` 不悬挂）；
- `list_cases` 默认只返回非归档；`export_test_cases` 交付物仅含生效用例。

### 8.4 执行派发
- 非 API 用例（page/component/business）统一以 `ui_probe` 动作透出，executor 按 `kind` 派发；
- 端到端执行结果 `skipped==0`，前端/业务类(e2e)用例无一 `skipped`。

---

## 九、C-★6 修复记录（✅ 已完成 · 2026-09-08）

### 9.1 根因与修法
- **根因**：阶段 1 后端 `code_analyzer.analyze()` 仅发现 **99** 个 FP（71 api / 8 page / 20 component，**0 business**），而阶段 2 `gen_test_points`（同源 `generate_comprehensive_test_points`）发现 **237** 个（含 business 138）。两套独立 FP 发现实现导致 263 条 TP 中 138 条 `fp_contract_id` 在 `functional_points` 行级无对应。
- **修法**：`_analyze_project` 改用 `generate_comprehensive_test_points()` 的统一功能点清单落库（同源同算法，`fp_id` 完全一致），落库时写入 `contract_id`/`title`/`module`。
- **结果**：`analyze` 现产出 **237 个 FP（api 71 / business 138 / component 20 / page 8）**；263 条用例的 `fp_contract_id` **全部能在 functional_points 行级解析（unresolved=0/263）**，孤儿问题彻底消除。

### 9.2 验收（端到端 E2E-S3d / 6.3 验收判据）
> `COUNT(DISTINCT fp_contract_id IN cases) ⊆ COUNT(contract_id IN functional_points)` → **覆盖 100%**。

---

## 十、剩余未完成任务清单（非阻断项汇总）

> 结论：**阶段 1/2/3 所有「必须项（★）」均已闭环**，剩余均为**非阻断**的增强 / 卫生类项，不阻碍整体流程与阶段间衔接。

### 10.1 阶段 1（取码）
| 项 | 说明 | 阻断性 |
|---|---|---|
| S1-★5 | 真·远程基线恢复（需 codeup 凭据做 `base..target` 双版本对比） | ❌ 不阻断（已就地 git init，`WORKTREE` 增量 + 全量降级可用，`changed_files=0` 时全打「全量」标签，结论正确仅聚焦度下降） |
| S1-③3 | `work/research-agent-test/` 是否纳入版控（含截图/账号信息） | ❌ 不阻断（仓库卫生问题） |

### 10.2 阶段 2（测试点）
| 项 | 说明 | 阻断性 |
|---|---|---|
| A5 | diff 三向分类过粗（只影响「变更功能点」膨胀，不影响用例能否生成） | ❌ 不阻断 |

### 10.3 阶段 3（用例生成）
| 项 | 说明 | 阻断性 |
|---|---|---|
| C-②2 | 24 个手写业务场景（`exec_live.py`）未沉淀入库/技能，与 fp_id/tp_id 零关联 | ❌ 不阻断（属于资产化增强） |
| C-②3 | mock-server 与真实路由重复生成测试点 | ❌ 不阻断（去重优化） |
| C-②4 | 无优先级冒烟子集（不能一键只跑 P1） | ❌ 不阻断（增强） |
| C-②5 | 前端断言仅"文本包含" | ❌ 不阻断（断言精度增强） |
| C-②6 | `{account_id}` 类占位符未参数化（开动态探测必 404） | ❌ 不阻断（动态探测前置） |
| C-②7 | 路径分隔符统一（历史库反斜杠跨平台 file missing，已在 `_iter_code`/`_build_from_tp` 做归一并已验证） | ⚠️ 已部分修复 |
| 可选 | 脚本骨架产出 / LLM 增强可选开关 / 阶段3 技能化 `qa-test-cases` / TAPD·Jira 导出 | ❌ 不阻断 |

### 10.4 平台级（服务化，独立于本任务）
- 11 个端点同步执行 → 需异步化 + webhook + 鉴权 + 监控（见 `测试加速平台_服务化改造分析.md`）；
- `executor` 目前**不跑真实浏览器**（仅结构化断言 + 探活），真实 UI 验证待补 Playwright。

### 10.5 验证总览
| 自测 | 结果 |
|---|---|
| `selftest_b1.py`（批次1 · TP驱动+稳定ID） | ✅ PASSED（263/263，孤儿0，异常26） |
| `selftest_b2.py`（批次2 · 8要素+交付物） | ✅ PASSED（263/263，8要素齐全，reporter联动） |
| `selftest_e2e.py`（阶段1→3 串联） | ✅ 28/28 PASSED |
| `TestClient` 审核端点集成 | ✅ import/generate/review/bulk_review 均 200，analyze→237 FP(含business) |
