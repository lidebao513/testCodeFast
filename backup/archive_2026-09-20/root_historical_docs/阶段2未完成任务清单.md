# 阶段2（代码静态分析）未完成任务清单

> 对应 `PIPELINE.md` 阶段2「代码静态分析：功能点 + 测试点」。
> 已完成：B1 hunk 级精准打标、B2 测试点业务语义、**A1 功能点业务语义**、
> **A2 功能点↔测试点双向追溯**、**C1 统一数据契约 + 去重扫描**（契约 v1.0）、
> **业务模块发现（显式清单 + 动态补足，修复业务函数维度命中 0 的缺陷）**（自测 29/29 通过）。
> 状态图例：✅ 已完成 ｜ 🔴 必须（阻塞阶段3/阶段6 质量）｜ 🟡 应做 ｜ ⚪ 可选

## 一、已完成（本次）

| 编号 | 任务 | 落点 |
|---|---|---|
| B1 | hunk 级精准打标（替代整文件误标） | `compute_diff_hunks` / `_tag_point` / `_SourceIndex(aligned)` |
| B2 | 测试点业务语义（semantic/impl + 业务化 name/expect） | `_enrich_semantic` + 语义词典 |
| A1 | 功能点业务语义（fp_id / module / action / semantic / impl / loc / title） | `_enrich_fps` / `_enrich_api_fp` / `_enrich_plain_fp` |
| A2 | 功能点 ⇄ 测试点双向追溯（fp_id、fp_index、traceability） | `_ComprehensiveBuilder.build` |
| C1 | 统一数据契约 v1.0 + 单次去重扫描 | `FP_CONTRACT_VERSION` / `fp_id_of` / `_SourceIndex` |
| A6(部分) | 功能点稳定 ID（md5，代码不变 ID 不变） | `fp_id_of()` |
| ✅ 已修复 | **业务模块发现**：显式清单优先 + 按业务密度动态补足，修复 business 维度命中 0 | `discover_business_modules` / `_biz_of_path` / `_top_symbols` |
| — | 自测脚本（受控 fixture + 真实仓库回归，**29 断言**） | `work/test-accel/selftest_a1_a2_c1.py` |

> **已修复（2026-09-08）**：业务函数维度在真实仓库命中 0 条的缺陷。
> 方案：`discover_business_modules()` —— ① 显式 `_CORE_MODULES` 命中即用（候选目录新增
> `backend/research-agent-source/tools`、`tools`、`src`）；② 不足 12 个模块时按「顶层公共符号数」
> 动态排序补足，并排除噪声文件（tests/migrations/mock/workspace…）与**路由主导文件**（已由 api 维度覆盖）；
> ③ 业务域由「末两段路径 + 关键词词典」推导（`models.py` 特判为「数据模型定义」，避免 `model` 误判成对话域）；
> ④ 只取 `_` 之外的公共符号，避免把内部实现当业务功能点。
> 效果：真实仓库 business 功能点 **0 → 138**（8 个业务域），测试点 **125 → 263**，模块数 10 → 18。

## 二、未完成

### 🔴 必须（高）
1. **A5 diff 三向分类过粗**：`diff_analyzer` 的 `updated` = 「两边都有 + 文件被改」，
   与测试点已实现的 hunk 级精度不一致。应细化到符号/hunk 级，否则「变更功能点」仍会膨胀。
   *必要性*：增量回归的聚焦度取决于此；这是阶段2 目前**唯一**遗留的必须项。

### 🟡 应做（中）
2. **业务模块发现的精选度**：现按「顶层公共符号数」取前 12 个模块，仍可能选中配置/工具类
   （如 `config.py`）。可加权：docstring 覆盖率、是否被路由模块 import、是否含业务关键词。
3. **A3 前端抽取脆弱**：page/component 依赖 `router` 关键字与 `<script|template>` 正则，
   Vue/React 其他写法（`<script setup>`、动态 import 路由表）会漏抽。
4. **B3 标签与 Scope 耦合**：过滤后统计口径依赖调用顺序，建议把 Scope 与 Tag 做成正交的两组独立维度。
5. **B4 业务函数维度单一**：FUNC 测试点目前只有「正常」一维，缺少异常/边界（空输入、异常分支）。
6. **B5 前端无安全/异常维度**：前端测试点只有「页面可达 / 交互可用」，缺 401 跳转、权限隐藏、错误态。
7. **A7 其他类功能点**：定时任务 / 消息消费 / 配置开关 / DB 迁移等非 HTTP 功能形态尚未纳入功能点类型。

### ⚪ 可选（低）
8. **A4 语言/框架覆盖**：目前仅 Python（AST）+ JS/TS/Vue（正则）；Java/Go 需新增解析器。
9. **B6 Scope 枚举校验**：非法 scope 输入静默忽略，建议显式报错或回退默认并提示。

## 三、已知环境风险（非代码缺陷）
- ✅ **已修复（2026-09-08）**：被测仓库 `.git` 原本不完整（缺 HEAD/config），git 命令会向上命中父仓库
  `testCodeFast`。现已「就地 `git init`」重建为独立仓库（分支 `main`，基线提交 `34f105b`，1033 文件，
  排除 `.venv`），并在 `pull_code.py` 加入 `_git_repo_usable()`（toplevel 必须等于自身，逃逸即阻断）。
- ⚠️ **遗留（不阻断）**：仓库只有单一基线提交，`base..target` 真实双版本 diff 仍不可用
  （变更文件数=0，全部标「全量」，聚焦度下降但结论正确）。恢复需 codeup 凭据，
  已登记为**阶段1 清单 S1-★5**，判定不阻断整体进度。
- 原基线 ref `test-20260906` / `test-20260907` 已不存在（仅 `main`），引用前务必 `git rev-parse` 核对。
