# 测试用例样本展示（真实产出）

> 数据来源：`work/testgen-service/data/testgen.db`　项目：research-agent（id=1）
> 全库用例总数：**1715**　契约版本：1.0

本文件由 `work/testgen-service/tools/dump_case_samples.py` 直接从数据库导出，
未经手工修改，用于核对「代码 → 功能点 → 测试点 → 用例」链路的真实产出。

## 执行层与维度总览

| 执行层 | 用例类型 | 条数 |
|---|---|---|
| 接口 | e2e | 848 |
| 接口 | api | 722 |
| UI | e2e | 145 |

| 维度 | 子维度 | 条数 |
|---|---|---|
| 正常 | 业务函数-逻辑可用 | 848 |
| 边界 | 边界-参数缺失/非法 | 254 |
| 安全 | 安全-鉴权缺失 | 186 |
| 正常 | 正常-可用性 | 186 |
| 正常 | 交互元素可用 | 85 |
| 异常 | 异常-资源不存在 | 68 |
| 正常 | 页面/路由可达 | 60 |
| 安全 | 安全-越权 | 28 |

## A. 接口层 · HTTP 接口（共 186 条，下列 3 条）

> 代码里的路由装饰器（`@router.get("/x")` 等）→ 用 HTTP 请求直接打接口。

#### `TP-8c5dce8c` [正常] research-agent-source · 创建/api/sql/translate

| 字段 | 值 |
|---|---|
| 编号 | `TP-8c5dce8c` |
| 模块 | research-agent-source |
| 类型（维度） | 正常 |
| 优先级 | P2 |
| 测试类型 | 全量 |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-f18dcb4a` → `TP-8c5dce8c` |
| 用例 ctype | api |

**前置条件**：被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）

**步骤**

1. [前置] 被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）
2. [准备] 构造请求：POST /api/sql/translate，来源文件 audit_safeguard/snapshot/backend/research-agent-source/audit_pipeline.py
3. [执行] 发送 POST /api/sql/translate 请求并捕获响应
4. [断言] 校验：响应状态码符合预期，关键业务字段完整
   - 预期：返回 2xx，资源创建成功，响应体包含新建对象标识

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "api",
  "tp_id": "TP-8c5dce8c",
  "fp_id": "FP-f18dcb4a",
  "source": "audit_safeguard/snapshot/backend/research-agent-source/audit_pipeline.py",
  "file": "audit_safeguard/snapshot/backend/research-agent-source/audit_pipeline.py",
  "method": "POST",
  "path": "/api/sql/translate",
  "func": null,
  "dimension": "正常-可用性",
  "expect": "返回 2xx，资源创建成功，响应体包含新建对象标识"
}
```

#### `TP-8b91ce23` [正常] research-agent-source · 创建/api/sql_explanation/retry

| 字段 | 值 |
|---|---|
| 编号 | `TP-8b91ce23` |
| 模块 | research-agent-source |
| 类型（维度） | 正常 |
| 优先级 | P2 |
| 测试类型 | 全量 |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-542e0ea3` → `TP-8b91ce23` |
| 用例 ctype | api |

**前置条件**：被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）

**步骤**

1. [前置] 被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）
2. [准备] 构造请求：POST /api/sql_explanation/retry，来源文件 audit_safeguard/snapshot/backend/research-agent-source/audit_pipeline.py
3. [执行] 发送 POST /api/sql_explanation/retry 请求并捕获响应
4. [断言] 校验：响应状态码符合预期，关键业务字段完整
   - 预期：返回 2xx，资源创建成功，响应体包含新建对象标识

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "api",
  "tp_id": "TP-8b91ce23",
  "fp_id": "FP-542e0ea3",
  "source": "audit_safeguard/snapshot/backend/research-agent-source/audit_pipeline.py",
  "file": "audit_safeguard/snapshot/backend/research-agent-source/audit_pipeline.py",
  "method": "POST",
  "path": "/api/sql_explanation/retry",
  "func": null,
  "dimension": "正常-可用性",
  "expect": "返回 2xx，资源创建成功，响应体包含新建对象标识"
}
```

#### `TP-41de8bb3` [正常] research-agent-source · 查询列表/api/audit/selfcheck

| 字段 | 值 |
|---|---|
| 编号 | `TP-41de8bb3` |
| 模块 | research-agent-source |
| 类型（维度） | 正常 |
| 优先级 | P2 |
| 测试类型 | 全量 |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-851882d6` → `TP-41de8bb3` |
| 用例 ctype | api |

**前置条件**：被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）

**步骤**

1. [前置] 被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）
2. [准备] 构造请求：GET /api/audit/selfcheck，来源文件 audit_safeguard/snapshot/backend/research-agent-source/audit_pipeline.py
3. [执行] 发送 GET /api/audit/selfcheck 请求并捕获响应
4. [断言] 校验：响应状态码符合预期，关键业务字段完整
   - 预期：返回 2xx，响应体为列表结构，分页字段可用

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "api",
  "tp_id": "TP-41de8bb3",
  "fp_id": "FP-851882d6",
  "source": "audit_safeguard/snapshot/backend/research-agent-source/audit_pipeline.py",
  "file": "audit_safeguard/snapshot/backend/research-agent-source/audit_pipeline.py",
  "method": "GET",
  "path": "/api/audit/selfcheck",
  "func": null,
  "dimension": "正常-可用性",
  "expect": "返回 2xx，响应体为列表结构，分页字段可用"
}
```

## B. 接口层 · 后端业务函数（共 848 条，下列 2 条）

> 不是 HTTP 接口，是被调用到的业务函数／工具脚本，归在接口层用函数级断言验证。

#### `TP-aa86bf86` [正常] audit_safeguard · main

| 字段 | 值 |
|---|---|
| 编号 | `TP-aa86bf86` |
| 模块 | audit_safeguard |
| 类型（维度） | 正常 |
| 优先级 | P2 |
| 测试类型 | 全量 |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-691572e6` → `TP-aa86bf86` |
| 用例 ctype | e2e |

**前置条件**：被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）

**步骤**

1. [前置] 被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）
2. [准备] 准备调用上下文：main，来源文件 audit_safeguard/gen_manifest.py
3. [执行] 调用函数 main，捕获返回值与异常
4. [断言] 校验：返回值符合语义，异常分支被正确捕获
   - 预期：函数返回符合语义的结果，异常分支被正确捕获

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "business",
  "tp_id": "TP-aa86bf86",
  "fp_id": "FP-691572e6",
  "source": "audit_safeguard/gen_manifest.py",
  "file": "audit_safeguard/gen_manifest.py",
  "method": "FUNC",
  "path": "main",
  "func": "main",
  "dimension": "业务函数-逻辑可用",
  "expect": "函数返回符合语义的结果，异常分支被正确捕获"
}
```

#### `TP-6aba5e38` [正常] audit_safeguard · compute_drift

| 字段 | 值 |
|---|---|
| 编号 | `TP-6aba5e38` |
| 模块 | audit_safeguard |
| 类型（维度） | 正常 |
| 优先级 | P2 |
| 测试类型 | 全量 |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-f8ab54d4` → `TP-6aba5e38` |
| 用例 ctype | e2e |

**前置条件**：被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）

**步骤**

1. [前置] 被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）
2. [准备] 准备调用上下文：compute_drift，来源文件 audit_safeguard/restore_audit.py
3. [执行] 调用函数 compute_drift，捕获返回值与异常
4. [断言] 校验：返回值符合语义，异常分支被正确捕获
   - 预期：函数返回符合语义的结果，异常分支被正确捕获

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "business",
  "tp_id": "TP-6aba5e38",
  "fp_id": "FP-f8ab54d4",
  "source": "audit_safeguard/restore_audit.py",
  "file": "audit_safeguard/restore_audit.py",
  "method": "FUNC",
  "path": "compute_drift",
  "func": "compute_drift",
  "dimension": "业务函数-逻辑可用",
  "expect": "函数返回符合语义的结果，异常分支被正确捕获"
}
```

## C. UI 层 · 页面可达（共 60 条，下列 3 条）

> 前端路由（React `<Route path="/pc/chat">` / `path: '/x'`）→ 打开页面看能否渲染。

#### `TP-d5384a1c` [正常] src · 页面/

| 字段 | 值 |
|---|---|
| 编号 | `TP-d5384a1c` |
| 模块 | src |
| 类型（维度） | 正常 |
| 优先级 | P2 |
| 测试类型 | 全量 |
| 期望执行层 | UI（ui_probe） |
| 来源功能点 | `FP-5ef4b1b1` → `TP-d5384a1c` |
| 用例 ctype | e2e |

**前置条件**：被测服务已启动且前端可访问；已准备可用浏览器（Playwright）与测试账号（如需登录）

**步骤**

1. [前置] 被测服务已启动且前端可访问；已准备可用浏览器（Playwright）与测试账号（如需登录）
2. [准备] 打开页面：/，来源文件 frontend/app/src/App.tsx
3. [执行] 访问 /，等待首屏渲染完成并收集控制台错误
4. [断言] 校验：页面正常渲染（无白屏／无控制台报错），关键元素可见
   - 预期：页面/路由可正常渲染，无白屏或控制台报错

**机器步（执行器只读第 1 步）**

```json
{
  "action": "ui_probe",
  "layer": "UI",
  "kind": "page",
  "tp_id": "TP-d5384a1c",
  "fp_id": "FP-5ef4b1b1",
  "source": "frontend/app/src/App.tsx",
  "file": "frontend/app/src/App.tsx",
  "method": "PAGE",
  "path": "/",
  "func": null,
  "dimension": "页面/路由可达",
  "expect": "页面/路由可正常渲染，无白屏或控制台报错"
}
```

#### `TP-7b3551c3` [正常] src · 页面/chat

| 字段 | 值 |
|---|---|
| 编号 | `TP-7b3551c3` |
| 模块 | src |
| 类型（维度） | 正常 |
| 优先级 | P2 |
| 测试类型 | 全量 |
| 期望执行层 | UI（ui_probe） |
| 来源功能点 | `FP-4c57a26f` → `TP-7b3551c3` |
| 用例 ctype | e2e |

**前置条件**：被测服务已启动且前端可访问；已准备可用浏览器（Playwright）与测试账号（如需登录）

**步骤**

1. [前置] 被测服务已启动且前端可访问；已准备可用浏览器（Playwright）与测试账号（如需登录）
2. [准备] 打开页面：/chat，来源文件 frontend/app/src/App.tsx
3. [执行] 访问 /chat，等待首屏渲染完成并收集控制台错误
4. [断言] 校验：页面正常渲染（无白屏／无控制台报错），关键元素可见
   - 预期：页面/路由可正常渲染，无白屏或控制台报错

**机器步（执行器只读第 1 步）**

```json
{
  "action": "ui_probe",
  "layer": "UI",
  "kind": "page",
  "tp_id": "TP-7b3551c3",
  "fp_id": "FP-4c57a26f",
  "source": "frontend/app/src/App.tsx",
  "file": "frontend/app/src/App.tsx",
  "method": "PAGE",
  "path": "/chat",
  "func": null,
  "dimension": "页面/路由可达",
  "expect": "页面/路由可正常渲染，无白屏或控制台报错"
}
```

#### `TP-a52b384f` [正常] src · 页面/chat/:id

| 字段 | 值 |
|---|---|
| 编号 | `TP-a52b384f` |
| 模块 | src |
| 类型（维度） | 正常 |
| 优先级 | P2 |
| 测试类型 | 全量 |
| 期望执行层 | UI（ui_probe） |
| 来源功能点 | `FP-89e57531` → `TP-a52b384f` |
| 用例 ctype | e2e |

**前置条件**：被测服务已启动且前端可访问；已准备可用浏览器（Playwright）与测试账号（如需登录）

**步骤**

1. [前置] 被测服务已启动且前端可访问；已准备可用浏览器（Playwright）与测试账号（如需登录）
2. [准备] 打开页面：/chat/:id，来源文件 frontend/app/src/App.tsx
3. [执行] 访问 /chat/:id，等待首屏渲染完成并收集控制台错误
4. [断言] 校验：页面正常渲染（无白屏／无控制台报错），关键元素可见
   - 预期：页面/路由可正常渲染，无白屏或控制台报错

**机器步（执行器只读第 1 步）**

```json
{
  "action": "ui_probe",
  "layer": "UI",
  "kind": "page",
  "tp_id": "TP-a52b384f",
  "fp_id": "FP-89e57531",
  "source": "frontend/app/src/App.tsx",
  "file": "frontend/app/src/App.tsx",
  "method": "PAGE",
  "path": "/chat/:id",
  "func": null,
  "dimension": "页面/路由可达",
  "expect": "页面/路由可正常渲染，无白屏或控制台报错"
}
```

## D. UI 层 · 交互组件（共 85 条，下列 2 条）

> 带交互钩子的前端组件（onClick / onChange / onSubmit / <button> 等）→ 点击输入看响应。

#### `TP-4ece9255` [正常] pc · 交互组件PcAuditDocPanel.tsx

| 字段 | 值 |
|---|---|
| 编号 | `TP-4ece9255` |
| 模块 | pc |
| 类型（维度） | 正常 |
| 优先级 | P2 |
| 测试类型 | 全量 |
| 期望执行层 | UI（ui_probe） |
| 来源功能点 | `FP-8885a7ea` → `TP-4ece9255` |
| 用例 ctype | e2e |

**前置条件**：被测服务已启动且前端可访问；已准备可用浏览器（Playwright）与测试账号（如需登录）

**步骤**

1. [前置] 被测服务已启动且前端可访问；已准备可用浏览器（Playwright）与测试账号（如需登录）
2. [准备] 定位交互点：PcAuditDocPanel.tsx，来源文件 audit_safeguard/snapshot/frontend/app/src/pages/pc/PcAuditDocPanel.tsx
3. [执行] 触发 PcAuditDocPanel.tsx 的交互（点击／输入），观察界面响应
4. [断言] 校验：交互后界面按预期变化，无报错提示或状态残留
   - 预期：交互元素可用，点击后产生预期状态变化

**机器步（执行器只读第 1 步）**

```json
{
  "action": "ui_probe",
  "layer": "UI",
  "kind": "ui",
  "tp_id": "TP-4ece9255",
  "fp_id": "FP-8885a7ea",
  "source": "audit_safeguard/snapshot/frontend/app/src/pages/pc/PcAuditDocPanel.tsx",
  "file": "audit_safeguard/snapshot/frontend/app/src/pages/pc/PcAuditDocPanel.tsx",
  "method": "UI",
  "path": "PcAuditDocPanel.tsx",
  "func": null,
  "dimension": "交互元素可用",
  "expect": "交互元素可用，点击后产生预期状态变化"
}
```

#### `TP-27504fb5` [正常] pc · 交互组件PcAuditScenePage.tsx

| 字段 | 值 |
|---|---|
| 编号 | `TP-27504fb5` |
| 模块 | pc |
| 类型（维度） | 正常 |
| 优先级 | P2 |
| 测试类型 | 全量 |
| 期望执行层 | UI（ui_probe） |
| 来源功能点 | `FP-51423a8f` → `TP-27504fb5` |
| 用例 ctype | e2e |

**前置条件**：被测服务已启动且前端可访问；已准备可用浏览器（Playwright）与测试账号（如需登录）

**步骤**

1. [前置] 被测服务已启动且前端可访问；已准备可用浏览器（Playwright）与测试账号（如需登录）
2. [准备] 定位交互点：PcAuditScenePage.tsx，来源文件 audit_safeguard/snapshot/frontend/app/src/pages/pc/PcAuditScenePage.tsx
3. [执行] 触发 PcAuditScenePage.tsx 的交互（点击／输入），观察界面响应
4. [断言] 校验：交互后界面按预期变化，无报错提示或状态残留
   - 预期：交互元素可用，点击后产生预期状态变化

**机器步（执行器只读第 1 步）**

```json
{
  "action": "ui_probe",
  "layer": "UI",
  "kind": "ui",
  "tp_id": "TP-27504fb5",
  "fp_id": "FP-51423a8f",
  "source": "audit_safeguard/snapshot/frontend/app/src/pages/pc/PcAuditScenePage.tsx",
  "file": "audit_safeguard/snapshot/frontend/app/src/pages/pc/PcAuditScenePage.tsx",
  "method": "UI",
  "path": "PcAuditScenePage.tsx",
  "func": null,
  "dimension": "交互元素可用",
  "expect": "交互元素可用，点击后产生预期状态变化"
}
```

## E. 安全 · 鉴权缺失（共 186 条，下列 2 条）

> 未携带或携带无效凭证访问接口，应被拒绝。

#### `TP-f67cf65f` [安全] research-agent-source · 创建/api/sql/translate

| 字段 | 值 |
|---|---|
| 编号 | `TP-f67cf65f` |
| 模块 | research-agent-source |
| 类型（维度） | 安全 |
| 优先级 | P1 |
| 测试类型 | 全量 |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-f18dcb4a` → `TP-f67cf65f` |
| 用例 ctype | api |

**前置条件**：被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）

**步骤**

1. [前置] 被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）
2. [准备] 构造请求：POST /api/sql/translate，来源文件 audit_safeguard/snapshot/backend/research-agent-source/audit_pipeline.py
3. [执行] 发送 POST /api/sql/translate 请求并捕获响应
4. [断言] 校验：无凭证/越权访问被拒绝（401/403），且不泄露资源内容
   - 预期：未携带或携带无效凭证时返回 401/403，且不泄露资源内容（越权访问同样被拒）

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "api",
  "tp_id": "TP-f67cf65f",
  "fp_id": "FP-f18dcb4a",
  "source": "audit_safeguard/snapshot/backend/research-agent-source/audit_pipeline.py",
  "file": "audit_safeguard/snapshot/backend/research-agent-source/audit_pipeline.py",
  "method": "POST",
  "path": "/api/sql/translate",
  "func": null,
  "dimension": "安全-鉴权缺失",
  "expect": "未携带或携带无效凭证时返回 401/403，且不泄露资源内容（越权访问同样被拒）"
}
```

#### `TP-dec32198` [安全] research-agent-source · 创建/api/sql_explanation/retry

| 字段 | 值 |
|---|---|
| 编号 | `TP-dec32198` |
| 模块 | research-agent-source |
| 类型（维度） | 安全 |
| 优先级 | P1 |
| 测试类型 | 全量 |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-542e0ea3` → `TP-dec32198` |
| 用例 ctype | api |

**前置条件**：被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）

**步骤**

1. [前置] 被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）
2. [准备] 构造请求：POST /api/sql_explanation/retry，来源文件 audit_safeguard/snapshot/backend/research-agent-source/audit_pipeline.py
3. [执行] 发送 POST /api/sql_explanation/retry 请求并捕获响应
4. [断言] 校验：无凭证/越权访问被拒绝（401/403），且不泄露资源内容
   - 预期：未携带或携带无效凭证时返回 401/403，且不泄露资源内容（越权访问同样被拒）

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "api",
  "tp_id": "TP-dec32198",
  "fp_id": "FP-542e0ea3",
  "source": "audit_safeguard/snapshot/backend/research-agent-source/audit_pipeline.py",
  "file": "audit_safeguard/snapshot/backend/research-agent-source/audit_pipeline.py",
  "method": "POST",
  "path": "/api/sql_explanation/retry",
  "func": null,
  "dimension": "安全-鉴权缺失",
  "expect": "未携带或携带无效凭证时返回 401/403，且不泄露资源内容（越权访问同样被拒）"
}
```

## F. 安全 · 越权访问（共 28 条，下列 2 条）

> 带路径参数的写操作（PUT/PATCH/DELETE），用非属主身份访问。

#### `TP-8d4d43f5` [安全] scheduler · 更新/api/v1/schedules/{schedule_id}

| 字段 | 值 |
|---|---|
| 编号 | `TP-8d4d43f5` |
| 模块 | scheduler |
| 类型（维度） | 安全 |
| 优先级 | P1 |
| 测试类型 | 全量 |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-4c8d6da9` → `TP-8d4d43f5` |
| 用例 ctype | api |

**前置条件**：被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）

**步骤**

1. [前置] 被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）
2. [准备] 构造请求：PUT /api/v1/schedules/{schedule_id}，来源文件 audit_safeguard/snapshot/backend/research-agent-source/tools/scheduler/api.py
3. [执行] 发送 PUT /api/v1/schedules/{schedule_id} 请求并捕获响应
4. [断言] 校验：无凭证/越权访问被拒绝（401/403），且不泄露资源内容
   - 预期：以他人身份/越权凭证操作该资源 → 403，且不产生越权修改

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "api",
  "tp_id": "TP-8d4d43f5",
  "fp_id": "FP-4c8d6da9",
  "source": "audit_safeguard/snapshot/backend/research-agent-source/tools/scheduler/api.py",
  "file": "audit_safeguard/snapshot/backend/research-agent-source/tools/scheduler/api.py",
  "method": "PUT",
  "path": "/api/v1/schedules/{schedule_id}",
  "func": null,
  "dimension": "安全-越权",
  "expect": "以他人身份/越权凭证操作该资源 → 403，且不产生越权修改"
}
```

#### `TP-64f9cb2a` [安全] scheduler · 删除/api/v1/schedules/{schedule_id}

| 字段 | 值 |
|---|---|
| 编号 | `TP-64f9cb2a` |
| 模块 | scheduler |
| 类型（维度） | 安全 |
| 优先级 | P1 |
| 测试类型 | 全量 |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-86d986b6` → `TP-64f9cb2a` |
| 用例 ctype | api |

**前置条件**：被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）

**步骤**

1. [前置] 被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）
2. [准备] 构造请求：DELETE /api/v1/schedules/{schedule_id}，来源文件 audit_safeguard/snapshot/backend/research-agent-source/tools/scheduler/api.py
3. [执行] 发送 DELETE /api/v1/schedules/{schedule_id} 请求并捕获响应
4. [断言] 校验：无凭证/越权访问被拒绝（401/403），且不泄露资源内容
   - 预期：以他人身份/越权凭证操作该资源 → 403，且不产生越权修改

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "api",
  "tp_id": "TP-64f9cb2a",
  "fp_id": "FP-86d986b6",
  "source": "audit_safeguard/snapshot/backend/research-agent-source/tools/scheduler/api.py",
  "file": "audit_safeguard/snapshot/backend/research-agent-source/tools/scheduler/api.py",
  "method": "DELETE",
  "path": "/api/v1/schedules/{schedule_id}",
  "func": null,
  "dimension": "安全-越权",
  "expect": "以他人身份/越权凭证操作该资源 → 403，且不产生越权修改"
}
```

## G. 异常（共 68 条，下列 2 条）

> 仅对有路径参数的接口展开：资源不存在／状态非法。

#### `TP-da45d405` [异常] scheduler · 更新/api/v1/schedules/{schedule_id}

| 字段 | 值 |
|---|---|
| 编号 | `TP-da45d405` |
| 模块 | scheduler |
| 类型（维度） | 异常 |
| 优先级 | P1 |
| 测试类型 | 全量 |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-4c8d6da9` → `TP-da45d405` |
| 用例 ctype | api |

**前置条件**：被测服务已启动且 base_url 可达；已明确合法/非法输入的构造方式

**步骤**

1. [前置] 被测服务已启动且 base_url 可达；已明确合法/非法输入的构造方式
2. [准备] 构造请求：PUT /api/v1/schedules/{schedule_id}，来源文件 audit_safeguard/snapshot/backend/research-agent-source/tools/scheduler/api.py
3. [执行] 发送 PUT /api/v1/schedules/{schedule_id} 请求并捕获响应
4. [断言] 校验：接口对非法/边界输入返回预期错误（4xx/5xx），且不产生未捕获异常或数据损坏
   - 预期：返回 4xx（资源不存在 404 / 状态非法 409），响应体为结构化错误信息，服务不抛未捕获异常

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "api",
  "tp_id": "TP-da45d405",
  "fp_id": "FP-4c8d6da9",
  "source": "audit_safeguard/snapshot/backend/research-agent-source/tools/scheduler/api.py",
  "file": "audit_safeguard/snapshot/backend/research-agent-source/tools/scheduler/api.py",
  "method": "PUT",
  "path": "/api/v1/schedules/{schedule_id}",
  "func": null,
  "dimension": "异常-资源不存在",
  "expect": "返回 4xx（资源不存在 404 / 状态非法 409），响应体为结构化错误信息，服务不抛未捕获异常"
}
```

#### `TP-68568325` [异常] scheduler · 删除/api/v1/schedules/{schedule_id}

| 字段 | 值 |
|---|---|
| 编号 | `TP-68568325` |
| 模块 | scheduler |
| 类型（维度） | 异常 |
| 优先级 | P1 |
| 测试类型 | 全量 |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-86d986b6` → `TP-68568325` |
| 用例 ctype | api |

**前置条件**：被测服务已启动且 base_url 可达；已明确合法/非法输入的构造方式

**步骤**

1. [前置] 被测服务已启动且 base_url 可达；已明确合法/非法输入的构造方式
2. [准备] 构造请求：DELETE /api/v1/schedules/{schedule_id}，来源文件 audit_safeguard/snapshot/backend/research-agent-source/tools/scheduler/api.py
3. [执行] 发送 DELETE /api/v1/schedules/{schedule_id} 请求并捕获响应
4. [断言] 校验：接口对非法/边界输入返回预期错误（4xx/5xx），且不产生未捕获异常或数据损坏
   - 预期：返回 4xx（资源不存在 404 / 状态非法 409），响应体为结构化错误信息，服务不抛未捕获异常

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "api",
  "tp_id": "TP-68568325",
  "fp_id": "FP-86d986b6",
  "source": "audit_safeguard/snapshot/backend/research-agent-source/tools/scheduler/api.py",
  "file": "audit_safeguard/snapshot/backend/research-agent-source/tools/scheduler/api.py",
  "method": "DELETE",
  "path": "/api/v1/schedules/{schedule_id}",
  "func": null,
  "dimension": "异常-资源不存在",
  "expect": "返回 4xx（资源不存在 404 / 状态非法 409），响应体为结构化错误信息，服务不抛未捕获异常"
}
```

## H. 边界（共 254 条，下列 2 条）

> 参数缺失／非法：所有 API 路由各一条，写操作再补一条凑对。

#### `TP-9b110955` [边界] research-agent-source · 创建/api/sql/translate

| 字段 | 值 |
|---|---|
| 编号 | `TP-9b110955` |
| 模块 | research-agent-source |
| 类型（维度） | 边界 |
| 优先级 | P1 |
| 测试类型 | 全量 |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-f18dcb4a` → `TP-9b110955` |
| 用例 ctype | api |

**前置条件**：被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）

**步骤**

1. [前置] 被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）
2. [准备] 构造请求：POST /api/sql/translate，来源文件 audit_safeguard/snapshot/backend/research-agent-source/audit_pipeline.py
3. [执行] 发送 POST /api/sql/translate 请求并捕获响应
4. [断言] 校验：响应状态码符合预期，关键业务字段完整
   - 预期：参数缺失或越界时返回 400/422，校验信息明确指出非法字段

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "api",
  "tp_id": "TP-9b110955",
  "fp_id": "FP-f18dcb4a",
  "source": "audit_safeguard/snapshot/backend/research-agent-source/audit_pipeline.py",
  "file": "audit_safeguard/snapshot/backend/research-agent-source/audit_pipeline.py",
  "method": "POST",
  "path": "/api/sql/translate",
  "func": null,
  "dimension": "边界-参数缺失/非法",
  "expect": "参数缺失或越界时返回 400/422，校验信息明确指出非法字段"
}
```

#### `TP-6a938728` [边界] research-agent-source · 创建/api/sql_explanation/retry

| 字段 | 值 |
|---|---|
| 编号 | `TP-6a938728` |
| 模块 | research-agent-source |
| 类型（维度） | 边界 |
| 优先级 | P1 |
| 测试类型 | 全量 |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-542e0ea3` → `TP-6a938728` |
| 用例 ctype | api |

**前置条件**：被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）

**步骤**

1. [前置] 被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）
2. [准备] 构造请求：POST /api/sql_explanation/retry，来源文件 audit_safeguard/snapshot/backend/research-agent-source/audit_pipeline.py
3. [执行] 发送 POST /api/sql_explanation/retry 请求并捕获响应
4. [断言] 校验：响应状态码符合预期，关键业务字段完整
   - 预期：参数缺失或越界时返回 400/422，校验信息明确指出非法字段

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "api",
  "tp_id": "TP-6a938728",
  "fp_id": "FP-542e0ea3",
  "source": "audit_safeguard/snapshot/backend/research-agent-source/audit_pipeline.py",
  "file": "audit_safeguard/snapshot/backend/research-agent-source/audit_pipeline.py",
  "method": "POST",
  "path": "/api/sql_explanation/retry",
  "func": null,
  "dimension": "边界-参数缺失/非法",
  "expect": "参数缺失或越界时返回 400/422，校验信息明确指出非法字段"
}
```

## I. 增量命中：更新标签（共 395 条，下列 2 条）

> 本次 diff（base..target）改动到的文件所涉测试点，标为「更新」。

#### `TP-9311ed10` [正常] research-agent-source · build_tool_registry

| 字段 | 值 |
|---|---|
| 编号 | `TP-9311ed10` |
| 模块 | research-agent-source |
| 类型（维度） | 正常 |
| 优先级 | P2 |
| 测试类型 | 更新 |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-22a08e2f` → `TP-9311ed10` |
| 用例 ctype | e2e |

**前置条件**：被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）

**步骤**

1. [前置] 被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）
2. [准备] 准备调用上下文：build_tool_registry，来源文件 backend/research-agent-source/main.py
3. [执行] 调用函数 build_tool_registry，捕获返回值与异常
4. [断言] 校验：返回值符合语义，异常分支被正确捕获
   - 预期：函数返回符合语义的结果，异常分支被正确捕获

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "business",
  "tp_id": "TP-9311ed10",
  "fp_id": "FP-22a08e2f",
  "source": "backend/research-agent-source/main.py",
  "file": "backend/research-agent-source/main.py",
  "method": "FUNC",
  "path": "build_tool_registry",
  "func": "build_tool_registry",
  "dimension": "业务函数-逻辑可用",
  "expect": "函数返回符合语义的结果，异常分支被正确捕获"
}
```

#### `TP-9c4c5a9d` [正常] research-agent-source · setup_checkpointer

| 字段 | 值 |
|---|---|
| 编号 | `TP-9c4c5a9d` |
| 模块 | research-agent-source |
| 类型（维度） | 正常 |
| 优先级 | P2 |
| 测试类型 | 更新 |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-c50d72a6` → `TP-9c4c5a9d` |
| 用例 ctype | e2e |

**前置条件**：被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）

**步骤**

1. [前置] 被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）
2. [准备] 准备调用上下文：setup_checkpointer，来源文件 backend/research-agent-source/main.py
3. [执行] 调用函数 setup_checkpointer，捕获返回值与异常
4. [断言] 校验：返回值符合语义，异常分支被正确捕获
   - 预期：函数返回符合语义的结果，异常分支被正确捕获

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "business",
  "tp_id": "TP-9c4c5a9d",
  "fp_id": "FP-c50d72a6",
  "source": "backend/research-agent-source/main.py",
  "file": "backend/research-agent-source/main.py",
  "method": "FUNC",
  "path": "setup_checkpointer",
  "func": "setup_checkpointer",
  "dimension": "业务函数-逻辑可用",
  "expect": "函数返回符合语义的结果，异常分支被正确捕获"
}
```

## 产物文件清单

| 文件 | 内容 |
|---|---|
| `outputs/1/test_cases.json` | 全部用例（机器可读，含机器步） |
| `outputs/1/TEST_CASES.md` | 全部用例（人类可读清单） |
| `outputs/1/test_points.json` | 全部测试点（含证据引用） |
| `outputs/1/summary.json` | 本次运行汇总（计数／标签／追溯体检） |
