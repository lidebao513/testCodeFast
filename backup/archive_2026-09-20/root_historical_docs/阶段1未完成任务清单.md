# 阶段 1「代码获取与就位」· 未完成任务清单

> 范围：qa-code-pull 技能（拉取代码 + 就位检查）。
> 当前状态取值说明：`未开始` / `部分实现` / `待优化`。
> 说明：单线对比（单 base→target）当前**可用且已验证**（64 变更文件正确、离线降级稳定、退出码恒 0）；以下未完成任务均面向「多分支对比」与「生产化」目标，**不阻断当前单线流水线**，但需在后续「Stage 1 强化冲刺」中闭环。

## 一、已完成（基线，供对照）

| 能力 | 说明 |
|---|---|
| 仓库克隆 | 本地缺失时 `--depth 1` 起步克隆；已存在则跳过 |
| 浅克隆加深 | `--deepen` + `protocol.version=0`，规避 git diff 失真 |
| ref 确认 | 确保 base / target 两个 ref 在本地可解析（不可解析则 fetch） |
| 工作树就位 | `checkout -f target`，使工作树 = 待测目标版本 |
| 变更集计算 | 计算 base..target 变更文件集合（changed_files） |
| 结果持久化 | 写入 `.pull_result.json`（含脱敏 url） |
| 离线降级 | 网络不可用时 `status=ok_offline`，退出码恒 0 |
| 凭据脱敏 | git URL 中的 `oauth2:token@` 已脱敏为 `oauth2:***@` |

## 二、未完成任务（按优先级）

### ★ 必须项（后续「Stage 1 强化冲刺」须完成）

| 编号 | 任务 | 简要描述 | 当前状态 |
|---|---|---|---|
| S1-★1 | 多分支拉取能力 | 当前仅 `fetch base/target` 两个 ref，**不支持**拉取仓库全部分支；「将多个分支下载到本地后期自由对比」的目标尚未真正成立 | 未开始 |
| S1-★2 | fetch 与 checkout 解耦 + 去破坏性 | 当前 `checkout -f target` **破坏式**改写共享工作树，无 `--no-checkout` 模式；纯对比本不需 checkout，当前每次换对比对都触发 checkout thrashing | 部分实现（有 checkout，无解耦/去破坏） |
| S1-★3 | 对比结果持久化（注册表） | 当前单 `.pull_result.json` 每次**整体覆盖**，多组 base..target 对比无法并存，无法复现历史选定的基线 | 未开始 |
| S1-★4 | merge-base 可达性校验 | 当前**无**校验；浅克隆 / 分支分叉早时 `A..B` 找不到真实 merge-base → diff 失真（比单线场景更严重） | 未开始 |

### 应做项（应完成）

| 编号 | 任务 | 简要描述 | 当前状态 |
|---|---|---|---|
| S1-②1 | 配置泛型化 | `DEFAULT_LOCAL/BASE/TARGET` 硬编码 research-agent；应支持任意仓库 + 自动识别默认分支 | 待优化 |
| S1-②2 | 幂等安全检查 | 无 `--force` 重建逻辑；ref 已存在仍每次 fetch，缺「已就位则跳过」判断 | 未开始 |
| S1-②3 | 多对比对批量支持 | 入参只支持单对 (base,target)，不支持一次传多对批量产出多份 changed_files | 未开始 |
| S1-②4 | worktree/archive 物化模式 | 无；需同时物化多版本时应走 `git worktree add` / `git archive`（共享对象库、零冗余），而非整库按分支克隆 | 未开始 |
| S1-②5 | 对比效率优化 | 纯 `git diff` 走对象库高效，但每次换对比对仍触发 checkout；纯对比应完全避免 checkout | 待优化 |

### 可选项（后续增强）

| 编号 | 任务 | 简要描述 | 当前状态 |
|---|---|---|---|
| S1-③1 | 清理策略 | 旧 worktree / 过期对比注册表 GC | 未开始 |
| S1-③2 | 就绪报告 | 输出「已就绪分支清单 + 可对比矩阵」 | 未开始 |

### 待决策项（2026-09-08 新增，**已判定不阻断**）

| 编号 | 任务 | 现状与判定 | 阻断性 |
|---|---|---|---|
| S1-★5 | **真·远程基线恢复**（`base..target` 双版本对比） | 被测仓库 `.git` 已用「就地 `git init`」修复（分支 `main`，基线提交 `34f105b`，1033 文件），阶段1→2 契约 `.pull_result.json` 已可正常产出；但**只有单一基线提交**，无法做「历史版本 vs 目标版本」的真实 diff。恢复它需要 codeup 凭据（令牌或注册 SSH key），当前匿名 HTTPS/SSH 均被拒。<br>**判定：不阻断。** 现有 `--target WORKTREE` 增量模式（工作树 vs 基线提交）+ 全量扫描降级已覆盖当前流水线；`changed_files=0` 时全部打「全量」标签，结论仍正确，只是聚焦度下降。 | ❌ 不阻断 |
| S1-③3 | 产物目录是否纳入版本管理 | `work/research-agent-test/`（截图 + 日志 + 执行结果，含被测账号信息）当前 untracked。建议加入父仓库 `.gitignore`，仅保留 `TEST_POINTS_*.md` / `FUNCTIONAL_POINTS.md` 等文本产物。<br>**判定：不阻断**，属仓库卫生问题。 | ❌ 不阻断 |

## 三、小结

- 4 个必须项（S1-★1~★4）是「多分支下载 → 后期自由对比」链路的实质缺口；其改造需**保持 `.pull_result.json` 的 `changed_files` 形状向后兼容**，以免破坏阶段 2 消费方（`gen_test_points._load_changed_files`）。
- 当前阶段 1 在「单 base→target」场景已完成约 80%，可作为阶段 2 的稳定上游；必须项建议作为受控冲刺推迟，触发条件为「多分支对比正式立项」或「流水线接入生产 CI 之前」。
