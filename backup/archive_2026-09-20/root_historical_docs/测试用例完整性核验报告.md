# 测试用例完整性核验报告

- 核验时间：2026-09-11
- 核验对象：`work/testgen-service`（新服务，P0–P4 已完成）
- 核验方式：在被测真实仓库上端到端实跑 + 直接查库核对，**不采信代码声明**
- 被测仓库：`work/targets/research-agent`，增量基线 `cacbd6b` → `722e198`

---

## 一、结论速览

| 问题 | 核验时的状态 | 修复后 |
|---|---|---|
| 链路是否打通（获取代码 → 生成用例）？ | 已打通 | 不变 |
| 是否生成**接口层**用例？ | 是 | 1570 条（HTTP 接口 722 + 后端业务函数 848） |
| 是否生成**UI 层**用例？ | **仅「页面可达」，交互组件整类缺失** | 145 条（页面 60 + 交互组件 85） |
| 是否有**安全**用例？ | 只有「鉴权缺失」，**默认不生成**，越权维度丢失 | 214 条（鉴权缺失 186 + 越权 28） |

核验一共抓出 **7 个静默缺陷**（全部为「不报错、结果错/缺」，只有真跑才暴露），
其中 4 个是**迁移时丢失旧平台已有能力**的回归。

---

## 二、链路完整性（已打通）

```
获取代码 → 扫描(scan) → 功能点提取(fp_extract) → 增量打标(diff_tag)
        → 测试点展开(tp_expand) → 语义增强/防胡说(semantic_enrich)
        → 用例生成(case_gen) → 落库对账(store) → 产物落盘(output)
```

修复后实测（全范围 `正常,异常,安全,边界`，增量模式）：

| 指标 | 数值 |
|---|---|
| 扫描文件 | 416 |
| 功能点 | 1179（business 848 / api 186 / component 85 / page 60） |
| 测试点 | 1715 |
| 用例 | 1715（1:1） |
| 追溯孤儿 | tp=0 / case=0 |

---

## 三、缺陷清单（核验中发现并全部修复）

### 3.1 新服务自身的缺陷

#### D-8 · 扫描器漏掉 `.tsx`，整个 UI 层不可见
- **现象**：UI 层功能点恒为 0，**无任何告警**。
- **根因**：`Scanner.exts` 白名单为 `.py/.js/.ts/.vue/.html`，**没有 `.tsx`/`.jsx`**。
  被测仓库磁盘上有 147 个 `.tsx`（整个 React 页面层）从未进入分析；
  而 `fp_extract` 侧却已写支持 `.tsx` —— 两侧判定不一致，形成死分支。
- **修法**：扩展名收敛为**单一真值源** `scan.FRONTEND_EXTS / SOURCE_EXTS`，
  `fp_extract` 改为 import 同一常量，从根上杜绝再次分叉。

#### D-9 · 测试点编号随「用户请求的范围」漂移
- **现象**：先按 `正常,异常` 跑，再扩到全范围，**117 条已有用例被判 `obsolete`**、另建 117 条。
- **根因**：`tp_id` 的 `ordinal` 取**过滤后的循环下标**。范围一变（中间插进「安全」），
  同一条「异常」测试点序号从 1 变 2，编号随之改变 —— 与契约「同一份代码编号不变」直接冲突。
- **修法**：`ordinal` 改为取「(维度, 子维度) 在规范顺序中的固定位置」，与请求范围无关。
- **验证**：窄范围 1211 条 → 扩到全范围 = `reused 1211 / created 442 / obsolete 0`。

#### D-10 · 「边界凑对」破坏编号契约格式
- **现象**：编号用字符串拼接 `tp_id + "-M"`，产出 `TP-1a2b3c4d-M` —— 长度 13 位、
  格式不符契约 `TP-` + 8 位十六进制，按 `^TP-[0-9a-f]{8}$` 校验的下游会整体拒收。
- **修法**：新增 `contracts.tp_id_variant()` 派生仍符合格式的变体编号。

#### D-11 · 后端业务函数被派成 `ui_probe`
- **现象**：848 条业务函数用例的机器步 `action=ui_probe`，但按契约它们属**接口层**。
  执行器照此派发会去拉浏览器「打开一个后端函数」。
- **根因**：`case_gen` 用「是否 HTTP 动词」当执行层判据，忽略了契约的 `verify_layer`。
- **修法**：执行层以 `verify_layer` 为准；并在 `steps[0]` 新增 `layer` 字段（只增不破）。
- **附带**：`test_business_case_is_ui_probe` 把**错误行为当契约钉住**，正是该缺陷长期未被发现的原因，已改写。

### 3.2 迁移时丢失旧平台已有能力（回归）

#### D-12 · 「越权」维度整条丢失
- **旧平台有**：`code_analyzer._expand_api` 对「含 `{id}` 且方法为 PUT/PATCH/DELETE」的接口
  补一条 `tp_type=安全 / dimension=安全-越权` 的测试点（`操作他人资源 → 403`）。
- **新服务**：`Dimension.PRIV_ESC` 定义了但**零处引用** → 安全用例只剩「鉴权缺失」。
- **修法**：按旧规则补回。本仓库实测产出 **28 条越权用例**。

#### D-13 · 「交互组件」测试点整类丢失
- **旧平台有**：`_scan_component` 按交互钩子（`@click/v-on/el-button/button/input/upload/form`）
  识别关键交互组件，产出 `tp_type=正常 / dimension=交互元素可用` 测试点。
- **新服务**：`FType.COMPONENT` 定义了但**零处产出** → UI 层只剩「页面可达」。
- **修法**：补回组件提取，并把来源扩展名从旧平台的**仅 `.vue`** 扩展到 `.tsx/.jsx`
  （旧平台在 React 仓上一个组件都提不出来）。本仓库实测产出 **85 个交互组件**。

#### D-14 · 「异常」维度范围比旧平台宽，产生语义不符用例
- **旧平台**：只有含路径参数 `{id}` 的接口才生成「资源不存在」。
- **新服务**：写方法（POST/PUT/PATCH/DELETE）**即便没有路径参数**也生成，
  于是 `POST /api/x` 会得到一条「访问不存在的 {id} → 404」—— 语义不符。
- **修法**：恢复旧平台规则（仅 `{id}`）。本仓库异常用例 117 → **68**，去掉 49 条不成立的用例。

#### 另两处与旧平台的范围差异（已对齐）
- **边界**：旧平台覆盖**所有 API 路由**，新服务曾限制为仅 `{id}` → 已恢复（68 → 186）。
- **页面**：旧平台页面只有「页面可达」，新服务多加了一条「边界」维度 →
  页面无参数校验语义，属噪声，已移除（-60 条）。

---

## 四、安全用例现状（重点）

### 4.1 数量与范围的关系

| 请求范围 | 安全用例 | 说明 |
|---|---|---|
| **默认（不指定）** | **0** | `DEFAULT_SCOPE = {正常, 边界}`，**不含安全** |
| 显式含 `安全` | **214** | 鉴权缺失 186 + 越权 28 |

> 默认范围不含安全这件事，比安全用例少更危险 —— 容易被当成「已覆盖」。

### 4.2 修复后的覆盖情况

| 子维度 | 条数 | 触发条件 |
|---|---|---|
| 安全-鉴权缺失 | 186 | 所有 HTTP 接口 |
| 安全-越权 | 28 | 含 `{id}` 且方法为 PUT/PATCH/DELETE |

### 4.3 仍然存在的缺口（未修，需决策）

1. **只覆盖接口层**：页面、交互组件、业务函数**都不产出安全用例**（与旧平台一致）。
2. **期望值是通用模板，不是代码事实**：预期统一为「未携带凭证返回 401/403」，
   **并未读取被测代码里真实的鉴权接线**（哪些路由挂了 `Depends(...)`、哪些是公开接口），
   执行时对公开接口容易产生误报。
   实测该仓库有 111 处 `Depends(...)`，具备做定向判断的数据基础。
3. **越权语义是推断的**：只按「写操作 + 路径参数」推断，未识别「资源归属」，
   故 `DELETE /users/{id}` 与 `DELETE /my-drafts/{id}` 一视同仁。

---

## 五、其它遗留项

| 项 | 状态 |
|---|---|
| `verify_layer` 未独立落库 | 已用 `cases.steps[0].layer` 承载（新增字段，只增不破） |
| Windows 下只读加固不生效 | 实测仍可写，工具如实报 `enforced=false`；生产须容器 `:ro` + 非 root |
| 符号级 hunk 打标未接线 | 函数已实现已单测，需先 `checkout target` 保证行号对齐 |
| 接口鉴权（`/api/v1`）仍为占位 | 服务化阶段再补 |
| 覆盖率门槛 | 当前实测 86.57%，门槛仍为 5%，建议抬到 30% |

---

## 六、质量基线（修复后）

- 八环质量门禁：**全绿**
- 测试用例数：**87**
- 覆盖率：**86.57%**
- 每个修复都补了**回归测试**；其中还专门加了
  「展开规则与旧平台逐条对齐」的对账测试（`test_expansion_plan_matches_legacy_parity_table`），
  今后任何迁移丢维度的改动都会在这里失败。

---

## 七、给下一步的建议

1. **安全范围应进默认范围**：建议默认改为 `正常 + 异常 + 安全`。
2. **让安全期望从代码读出来**：扫描路由的鉴权依赖（`Depends(...)`/中间件/白名单），
   区分「应鉴权」与「公开接口」，避免对公开接口生成必失败的 401 用例。
3. **越权要识别资源归属**：从路由与参数命名推断「他人资源」，降低误报。
4. **UI 层继续扩面**：当前覆盖「页面可达 + 组件可交互」；
   下一步可细到「表单校验」「上传流程」等交互链路。
