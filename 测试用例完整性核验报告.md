# 测试用例完整性核验报告

- 核验时间：2026-09-11
- 核验对象：`work/testgen-service`（新服务，P0–P4 已完成）
- 核验方式：在被测真实仓库上端到端实跑 + 直接查库核对，**不采信代码声明**
- 被测仓库：`work/targets/research-agent`，增量基线 `cacbd6b` → `722e198`

---

## 一、结论速览

| 问题 | 结论 |
|---|---|
| 链路是否打通（获取代码 → 生成用例）？ | **已打通**，可端到端跑通 |
| 是否生成**接口层**用例？ | **是**，1473 条（HTTP 接口 625 + 后端业务函数 848） |
| 是否生成**UI 层**用例？ | **核验时是 0，已修复**；修复后 120 条（60 个页面路由 × 2 维度） |
| 是否有**安全**用例？ | **有，但默认不生成**；显式指定后 186 条，且只覆盖接口层、只覆盖「鉴权缺失」一个子维度 |

---

## 二、链路完整性

管线各阶段均已落地并跑通：

```
获取代码 → 扫描(scan) → 功能点提取(fp_extract) → 增量打标(diff_tag)
        → 测试点展开(tp_expand) → 语义增强/防胡说(semantic_enrich)
        → 用例生成(case_gen) → 落库对账(store) → 产物落盘(output)
```

实测产出（全范围 `正常,异常,安全,边界`，增量模式）：

| 指标 | 数值 |
|---|---|
| 扫描文件 | 416 |
| 功能点 | 1094（api 186 / business 848 / page 60） |
| 测试点 | 1593 |
| 用例 | 1593（1:1） |
| 追溯孤儿 | tp=0 / case=0 |
| 标签 | 全量 1219 / 更新 306 |

---

## 三、核验中发现并修复的 4 个静默缺陷

> 共同特征：**不报错、不崩溃，结果是错的或缺失的**。只有真跑一遍才会暴露。

### D-8 · 扫描器漏掉 `.tsx`，整个 UI 层不可见（已修）

- **现象**：UI 层功能点恒为 0，且**无任何告警**。
- **根因**：`Scanner.exts` 白名单为 `.py/.js/.ts/.vue/.html`，**没有 `.tsx`/`.jsx`**。
  被测仓库磁盘上有 **147 个 `.tsx`**（整个 React 页面层），从未进入分析；
  而 `fp_extract` 侧却已写明支持 `.tsx` —— 两侧判定不一致，形成死分支。
- **修法**：把扩展名收敛为**单一真值源** `scan.FRONTEND_EXTS / SOURCE_EXTS`，
  `fp_extract` 改为 import 同一常量，从根上杜绝两侧再次分叉。
- **效果**：扫描文件 269 → 416；页面功能点 0 → **60**；UI 层用例 0 → **120**。

### D-9 · 测试点编号随「用户请求的范围」漂移（已修）

- **现象**：先按 `正常,异常` 跑，再扩到全范围跑，**117 条已有用例被判 `obsolete`**、另建 117 条新用例。
- **根因**：`tp_id` 里的 `ordinal` 取的是**过滤后的循环下标**。
  范围一变（中间插进「安全」维度），同一条「异常」测试点的序号就从 1 变 2，编号随之改变。
  这与契约硬承诺「同一份代码重复跑，编号不变」**直接冲突**，是 D-2（位置相关编号）的同类残留。
- **修法**：`ordinal` 改为取**维度在规范顺序中的固定位置**（正常/异常/安全/边界），与请求范围无关。
- **效果**：窄范围 1211 条 → 扩到全范围 = `reused 1211 / created 442 / obsolete 0`。
  人工审核结论、执行历史、缺陷关联不再断链。

### D-10 · 「边界凑对」破坏编号契约格式（已修）

- **现象**：规则引擎为边界测试点补「参数缺失」凑对用例时，编号用字符串拼接 `tp_id + "-M"`，
  产出 `TP-1a2b3c4d-M` —— 长度 13 位、格式不符契约 `TP-` + 8 位十六进制。
  任何按 `^TP-[0-9a-f]{8}$` 校验的下游会**整体拒收**。
- **同时**：该规则对**页面路由**也生效，给 60 个页面路由各补了一条
  「缺少必填路径/查询参数时返回 400/422」—— 页面没有路径参数校验语义，属**无意义用例**。
- **修法**：新增 `contracts.tp_id_variant()` 派生仍符合格式的变体编号；凑对仅对 HTTP 接口生效。
- **效果**：编号格式不合规数 0；边界测试点 256 → 196（去掉 60 条页面噪声）。

### D-11 · 后端业务函数被派成 `ui_probe`（已修）

- **现象**：848 条后端业务函数用例的机器步 `action=ui_probe`，
  但按契约它们属**接口层**（`derive_verify_layer: business → INTERFACE`）。
  执行器照此派发会去拉浏览器「打开一个后端函数」。
- **根因**：`case_gen` 用「是否 HTTP 动词」当执行层判据，忽略了契约的 `verify_layer`。
- **修法**：执行层以 `verify_layer` 为准（缺失时按 method 标记回退推导）；
  并在 `steps[0]` **新增 `layer` 字段**（契约「只增不破」），让执行层可被机器读取与分组。
- **附带发现**：`tests/test_engine.py::test_business_case_is_ui_probe` 把**错误行为当契约钉住**，
  正是该缺陷长期未被发现的原因。已改写为正确语义。

---

## 四、安全用例现状（重点）

### 4.1 数量与范围的关系

| 请求范围 | 安全用例 | 说明 |
|---|---|---|
| **默认（不指定）** | **0** | `DEFAULT_SCOPE = {正常, 边界}`，**不含安全** |
| `正常,异常` | **0** | 之前各次实测用的就是这组，所以一份安全用例都没有 |
| 显式含 `安全` | **186** | 每个 HTTP 接口功能点各 1 条 |

### 4.2 覆盖面的三个真实缺口

1. **只覆盖接口层**：`dimensions_of()` 只对 `ftype=api` 追加安全维度；
   页面、UI 组件、业务函数**都不产出安全用例**。实测 186 条全部落在接口层。
2. **只覆盖「鉴权缺失」一个子维度**：`Dimension.PRIV_ESC`（安全-越权）**已定义但全项目零处引用**
   —— 越权场景是「有字段、没实现」。
3. **期望值是通用模板，不是代码事实**：安全用例的预期统一为
   「未携带或携带无效凭证时返回 401/403」，
   **并未读取被测代码里真实的鉴权接线**（如哪些路由挂了 `Depends(...)`、
   哪些是公开接口）。因此无法判断「这条接口到底该不该 401」，
   执行时容易产生误报。

---

## 五、其它已确认的遗留项

| 项 | 状态 |
|---|---|
| `cases` 表无 `verify_layer` 列 | 执行层只在测试点与机器步里，未独立落库（`steps[0].layer` 已可用） |
| `verify_layer` 落到用例层缺独立字段 | 已用 `steps[0].layer` 承载，未加列 |
| Windows 下只读加固不生效 | 实测仍可写，工具如实报 `enforced=false`；生产须容器 `:ro` + 非 root |
| 符号级 hunk 打标未接线 | 函数已实现已单测，需先 `checkout target` 保证行号对齐 |
| 断言鉴权（`/api/v1`）仍为占位 | 服务化阶段再补 |
| 覆盖率门槛 | 当前实测 86.49%，门槛仍为 5%，建议抬到 30% |

---

## 六、质量基线（修复后）

- 八环质量门禁：**全绿**
- 测试用例数：**83**
- 覆盖率：**86.49%**
- 核验过程中的每一条修复都补了**回归测试**（含把「错误行为断言」改写为正确语义）

---

## 七、给下一步的建议

1. **安全范围应进入默认范围**：安全用例默认 0 条这件事，比安全用例少更危险 —— 容易被当成「已覆盖」。
   建议默认范围改为 `正常 + 异常 + 安全`，边界按需。
2. **接线「越权」子维度**：把 `PRIV_ESC` 用起来，需要先从代码里识别「同一资源的不同归属/角色」信息。
3. **让安全期望从代码读出来**：扫描路由的鉴权依赖（`Depends(...)`、中间件、白名单配置），
   区分「应当鉴权」与「公开接口」，避免对公开接口生成必失败的 401 用例。
4. **UI 层扩面**：当前 UI 层仅覆盖「页面可达」；交互组件（`ftype=ui`，按钮/表单/校验）
   尚未提取，是 UI 层用例量的下一个增长点。
