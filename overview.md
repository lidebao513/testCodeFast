# 本次任务概览（P0–P4：封存 → 契约 → 骨架 → 引擎 → 双通道）

**日期**：2026-09-11  ·  **提交**：`4bfc245`（新服务）、`71cbc85`（技能索引）
**结论**：新服务 `work/testgen-service/` 已建成并跑通「代码 → 功能点 → 测试点 → 测试用例」全链路，八环质量门禁全绿。按要求**停在用例生成**，执行/报告（P5）与第三方引擎（P6）未做。

---

## 做了什么

| 阶段 | 产出 |
|---|---|
| **P0 封存** | 老项目打标签 `legacy-pipeline-v1` + 冻结分支 `legacy-phase2-3-baseline`；数据库副本；黄金样本；技能备份 |
| **P1 契约** | 契约 v1.0 冻结说明、资产盘点表、升版策略（只增不破） |
| **P2 骨架** | 六层结构（`core`/`engine`/`service`/`cli`/`workspace`/`output`）+ **八环质量门禁同源同步** + FastAPI 服务壳 |
| **P3 引擎** | 扫描 / 功能点 / 测试点 / 语义增强 / 用例生成 / 编排；契约 v1.0（稳定编号 + 八要素 + 双向追溯）；规则引擎默认，大模型可选且带防胡说层 |
| **P4 双通道** | 全量 + 增量（`base..target` diff 打「全量/更新」标签）+ 仓库逃逸阻断 + 只读加固（真实写入探测） |

## 关键决定

- **新项目落在当前仓库** `work/testgen-service/`，不另起仓库 —— 质量门禁与规范只有一份，避免两边分叉。
- **大模型沿用现有配置**（通义千问），并预留 DeepSeek 开关。
- **老项目全程只读未动**，新目录删掉即"什么都没发生"，随时可回滚。
- **只停在用例生成**，执行/报告环节不做，但用例已带机器可执行步（`steps[0]`）供将来直接消费。

## 验收证据

- 门禁八环全绿；**73 个测试**；覆盖率 **86.16%**
- 真实仓库实测（research-agent，`cacbd6b`→`722e198`）：**269 文件 / 1034 功能点 / 1151 测试点 / 1151 用例**，标签 **全量 968 + 更新 183**，**孤儿 0**
- 幂等复核：再跑一次 `created 0 / reused 1151 / obsolete 0`
- 服务冒烟：`/health`、`/ready`、`POST /api/v1/pipeline` 均 200；非法入参 422 结构化错误
- 提交时 pre-commit 钩子**真的跑了完整八环门禁并通过**

## ⚠️ 验证中抓出并修掉的 5 个静默缺陷（本次最有价值的产出）

| 编号 | 症状 | 后果 |
|---|---|---|
| D-3 | 日志写到了 stdout | 污染 CLI 的 JSON 契约，下游解析直接崩 |
| D-4 | `git rev-parse --verify` 传了 2 个 ref | 增量通道 **100% 静默降级为全量**，"更新"标签恒为 0 |
| D-5 | 业务功能点用裸函数名做编号 | 不同类的同名方法撞号被静默合并（1034 → 1023） |
| D-6 | `log_extra` 未防 logging 保留属性 | 传 `name=` 直接抛 `KeyError` |
| D-7 | `cases` 表缺唯一约束 | 编号碰撞即插重复行 → **永久孤儿用例**（实测 11 条） |

> D-5 与 D-7 是同一条因果链：编号碰撞 → 重复行 → 缺唯一约束 → 永久孤儿。现在每一环都有防线。

## 遗留事项 / 下一步

1. **接符号级打标**：函数已实现已单测但未接线（需先 `checkout target` 保证行号对齐，硬接会给出错的行号）。
2. **抬覆盖率门槛**：5% → 30%（当前实际 86.16%）。
3. **P5 影子双跑**：老项目与新服务同输入逐条比对，差异清零后再谈切换。
4. **P6 服务化**：端点异步化 + webhook（发布流水线触发）+ 鉴权 + 监控。
5. 已知限制：断言鉴权为占位；Windows 下 `chmod` 只读**不生效**（生产走容器 `:ro` + 非 root）。

## 主要文件

| 文件 | 说明 |
|---|---|
| `P2-P4_新服务交付说明.md` | 完整交付说明（十节，含缺陷清单与已知限制） |
| `work/testgen-service/` | 新服务源码（40 个 Python 文件 / 6133 行） |
| `work/testgen-service/ARCHITECTURE.md` | 分层铁律 + 只读加固生产建议 |
| `work/testgen-service/规范文档.md` | 六类规范 + 通俗说明 |
| `P1-1_契约冻结说明v1.0.md` | 契约字段表、D-1/D-2 缺陷、枚举真值表 |
| `备份说明.md` | P0 封存细节（标签 / 分支 / 库副本 / 黄金样本校验值） |
