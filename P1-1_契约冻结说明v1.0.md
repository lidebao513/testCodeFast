# P1-1 · 契约冻结说明 v1.0

> 冻结时间：2026-09-11　契约版本：`FP_CONTRACT_VERSION = "1.0"`　基线：`legacy-pipeline-v1`（`236f44c`）
> 适用范围：本契约只覆盖 **代码 → 功能点 → 测试点 → 用例生成** 这一段（本次开发范围）。
> 执行、报告、调度不在本契约内。

---

## 一、契约管什么

```
源码仓库 ──扫描──▶ 功能点 FP ──展开──▶ 测试点 TP ──生成──▶ 用例 Case
                        ▲                  ▲                ▲
                        └────── 双向追溯（fp_contract_id / tp_id）──────┘
```

**三条硬承诺**（任何新实现都必须满足）：
1. **产物可被旧消费方直接读取**——字段名、取值域、编号格式完全一致。
2. **编号稳定**——同一份代码重复跑，编号不变（详见 §5 的两处缺陷说明）。
3. **追溯双向且无孤儿**——每条用例都能上溯到测试点、功能点；反之亦然。

---

## 二、对象 1：功能点 FP

### 2.1 字段

| 字段 | 类型 | 必填 | 含义 |
|---|---|---|---|
| `fp_id` | str | ✅ | 稳定唯一编号，形如 `FP-3f9a2c81`（落库列名 `contract_id`） |
| `ftype` | enum | ✅ | 来源类型：`api` / `page` / `ui` / `business` / `component` |
| `file_path` | str | ✅ | 相对仓库根路径，**统一正斜杠** |
| `name` | str | ✅ | **机器键**，如 `POST /api/v1/tasks`（不可改，消费方依赖） |
| `title` | str | ✅ | 业务化展示名（人类可读） |
| `module` | str | ✅ | 业务模块名 |
| `semantic` | str | ➖ | 语义描述（做什么） |
| `description` | str | ➖ | 实现说明（函数名 + docstring 摘要） |
| `commit_ref` | str | ➖ | 产出该功能点的代码版本 |

### 2.2 `fp_id` 生成规则（**冻结，不可改**）

```
fp_id = "FP-" + md5(f"{ftype}|{file_path}|{name}").hexdigest()[:8]
```

- 用 md5 而非自增，是为了**跨运行、跨机器稳定**，且规避路径中的中文/空格/分隔符干扰。
- md5 在此**仅作指纹用途，非安全用途**（代码中已标注 `# nosec B324`）。
- ⚠️ `name` 参与哈希，因此 **`name` 的语义一旦变更，等价于新功能点**——这是有意设计。

---

## 三、对象 2：测试点 TP

### 3.1 字段

| 字段 | 类型 | 必填 | 取值/说明 |
|---|---|---|---|
| `tp_id` | str | ✅ | 形如 `TP-001`。**见 §5 缺陷 D-2** |
| `fp_contract_id` | str | ✅ | 指向来源功能点 `fp_id`（TP → FP 方向） |
| `category` | enum | ✅ | 行为维度：`正常` / `异常` / `安全` / `边界` |
| `module` | str | ✅ | 业务模块 |
| `title` | str | ✅ | 人类可读标题 |
| `semantic` | str | ➖ | 该测试点要验证的语义 |
| `source` | str | ✅ | 来源文件，**统一正斜杠**（与 git diff 路径形态一致） |
| `method` | str | ✅ | HTTP 动词，或标记 `PAGE` / `UI` / `FUNC` |
| `area` | str | ✅ | 路由路径 / 页面路径 / 函数名 |
| `expect` | str | ✅ | 预期结果文案（人类可读，执行层再解析） |
| `dimension` | enum | ➖ | 细分子维度，见 §6 枚举表 |
| `tag` | enum | ✅ | 增量标签：`全量` / `更新`（由 hunk 命中决定） |
| `review_status` | enum | ✅ | `pending` / `approved` |
| `verify_layer` | enum | ➖ | `接口` / `UI`（内存字段，由 `ftype` 推导） |

### 3.2 标签判定规则（`tag`）

- 有 diff hunk 数据 → **符号行区间 ∩ hunk 区间**命中即 `更新`，否则 `全量`；
- 无 hunk 数据（离线/解析失败）→ **文件级降级**：文件在变更集内即 `更新`；
- 无 diff 上下文（全量扫描）→ 全部 `全量`。

### 3.3 FP ↔ TP 双向追溯

- 正向：TP 携带 `fp_contract_id`；
- 反向：FP 侧维护 `fp_index[fp_id]["test_points"] = [tp_id, ...]`；
- **验收指标：孤儿测试点（无 FP 归属）恒为 0。**

---

## 四、对象 3：用例 Case

### 4.1 八要素（**冻结，缺一不可**）

| # | 要素 | 字段 | 派生规则 |
|---|---|---|---|
| 1 | 编号 | `tc_no` | `tp_id`，拆分模式下追加 `::<维度>` 后缀 |
| 2 | 模块 | `module` | 继承自 TP |
| 3 | 标题 | `title` | `[{category}] {tp.title or tp.tp_id}` |
| 4 | 类型 | `case_type` | 取 TP 的 `category`（`正常/异常/安全/边界`） |
| 5 | 优先级 | `priority` | 见下 |
| 6 | 前置条件 | `precondition` | 见下 |
| 7 | 步骤 | `doc_steps` | 固定 4 步：前置 → 准备 → 执行 → 断言 |
| 8 | 预期结果 | `steps[0].expect` | 继承自 TP 的 `expect` |

**优先级规则（确定性，无 LLM）**：
- `category ∈ {异常, 安全, 边界}` → `P1`
- 否则，若 `module` 含 `鉴权/登录/支付/交易/权限/审批` 任一关键词 → `P1`
- 其余 → `P2`

**前置条件规则**：
- `category == 异常` → 「被测服务已启动且 base_url 可达；已明确合法/非法输入的构造方式」
- 其余 → 「被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）」

### 4.2 机器可执行步（`steps[0]`）

> **硬约束**：执行器只读 `steps[0]`。所有机器需要的字段必须放在这里，人类可读多步放 `doc_steps`。

| 字段 | 说明 |
|---|---|
| `action` | `http_probe`（接口类）/ `ui_probe`（前端/业务类） |
| `kind` | `page` / `component` / `business`（非接口类才有） |
| `tp_id` / `fp_id` | 追溯键 |
| `source` / `file` | 来源文件（两者同值，历史遗留双写） |
| `method` / `path` | 请求方法与路径 |
| `func` | 业务类用例的目标函数名 |
| `dimension` | 子维度 |
| `expect` | 预期结果 |

`ctype` 取值：接口类 = `api`，其余 = `e2e`。

### 4.3 生命周期与增量对账

- 生命周期状态（`cases.status`，**仅表达生命周期，不混执行结论**）：
  `generated` / `updated` / `obsolete` / `archived`
- 增量对账键：`tp_id`，回退键 `(fp_contract_id, title)`
- 对账结果：新出现 → `created`；内容变化 → `updated`；未变 → `reused`；功能点消失 → `obsolete`
- **幂等承诺**：同一份输入重复生成，不得新增重复用例。

---

## 五、已发现的契约缺陷（本次新服务必须修）

| 编号 | 缺陷 | 现状 | 影响 | 新服务处理 |
|---|---|---|---|---|
| **D-1** | **标签取值不一致** | 测试点层用 `全量/更新`（`Tag` 枚举）；用例层 `test_type` 却写死 `全量/新增` | 同一概念两套词，跨层筛选会漏 | **统一为 `全量/更新`**；`新增` 作为历史别名兼容读取 |
| **D-2** | **`tp_id` 不稳定** | `tp_id = f"TP-{序号:03d}"`，**按枚举顺序生成** | 文件顺序/功能点顺序一变，编号全错位；历史用例对不上账 | 改为稳定派生：`TP-` + `md5(fp_id\|category\|dimension\|area\|method)` 前 8 位 |
| **D-3** | `steps[0]` 中 `source` 与 `file` 双写同值 | 历史兼容遗留 | 冗余但无害 | 保留双写（兼容旧消费方），标注为「兼容字段」 |

> D-1 / D-2 属于**行为修正**，需在 P5 影子双跑中量化差异（预期：用例条数不变，编号与标签词表变化）。

---

## 六、枚举真值表（冻结）

| 枚举 | 取值 | 用途 |
|---|---|---|
| `TPType`（行为维度） | `正常` `异常` `安全` `边界` | 测试点 `category` / 用例 `case_type` |
| `Tag`（增量标签） | `全量` `更新` | 测试点 `tag` / 用例 `test_type`（**统一后**） |
| `FType`（来源类型） | `api` `page` `ui` `business` `component` | 功能点 `ftype` |
| `MethodMarker` | `PAGE` `UI` `FUNC` | 测试点 `method` 的非 HTTP 取值 |
| `HTTP_METHODS` | `GET` `POST` `PUT` `DELETE` `PATCH` `HEAD` `OPTIONS` | 测试点 `method` 的 HTTP 取值 |
| `Dimension` | `正常-可用性` `安全-鉴权缺失` `安全-越权` `边界-参数缺失/非法` `异常-资源不存在` `业务函数-逻辑可用` `页面/路由可达` `交互元素可用` | 测试点 `dimension` |
| `VerifyLayer` | `接口` `UI` | 测试点 `verify_layer` |
| `CaseLifecycleStatus` | `generated` `updated` `obsolete` `archived` | 用例 `status` |
| `ExecStatus` | `pass` `fail` `error` `skipped` `structural_only` `blocked_auth` `blocked_review` `cancelled` | 执行结论（本次范围外，保留定义） |

**范围常量（唯一真值）**
- `ALL_TP_TYPES` = 四种全选
- `DEFAULT_SCOPE` = `{正常, 边界}`　← **实测值，注意：不是"正常+异常"**
- `FULL_SCOPE` = 四种全选
- `SCOPE_KEY_ALL` = `全部/所有/全量/完整/all/ALL`（识别为全选）

---

## 七、冻结边界（不变量）

1. `name` 是机器键、`title` 是展示名——**两者不可互换**。
2. 路径一律**正斜杠**。
3. 功能点的 `fp_id` 算法、八要素字段名、追溯键名——**改不得**。
4. 执行器只读 `steps[0]`。
5. `cases.status` 只表示生命周期，**不得写入执行结论**（执行结论写 `last_result`）。

---

*本文件为 P1 交付物之一，配套《P1-2 资产盘点表》《P1-3 契约升版策略》。*
